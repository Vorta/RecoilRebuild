{
  "name": "briefing_thread_main",
  "description": "Briefing::ThreadMain initializes briefing thread state, runs the briefing render/update loop until the run flag is cleared, then destroys the briefing sound set and restores video/HUD modes before marking the thread exited.",
  "address": "0x404280",
  "source_from": "src/Battlesport/Briefing.cpp",
  "smoke_tests": [
    "briefing_thread_main_one_iteration_smoke"
  ],
  "known_limits": [
    "Dedicated VC5SP3 COFF byte target briefing_thread_main now covers this thread entry, but tier S remains deferred to the coherent Briefing runtime/source-cluster pass because 0x404180 and dependency 0x403930 still have owner-level byte drift."
  ],
  "functional_evidence": [
    "Binary Ninja assembly for 0x404280 validates the thread-run and allow-advance setup, network-gated skip path, Time/zSnd/zVideo/HUD update loop, adjust-surface loop continuation test, sound-set destroy, half-res/HUD restore, and thread-exited flag.",
    "The native smoke runs one real loop iteration with the real video present dispatch clearing the run flag, verifies thread state transitions, sample-set cleanup, HUD invalidate restoration, frame tick increment, and cleanup path execution."
  ],
  "vc5_attempt": "python tools/recoil.py verify vc5 briefing_thread_main"
}
