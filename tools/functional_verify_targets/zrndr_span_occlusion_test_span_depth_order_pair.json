{
  "name": "zrndr_span_occlusion_test_span_depth_order_pair",
  "description": "zRndr_SpanOcclusion::TestSpanDepthOrderPair compares two overlapping span-depth ranges with the recovered depth-bias thresholds.",
  "address": "0x4907c0",
  "source_from": "src/GameZRecoil/zRender/zrndr_draw.c",
  "smoke_tests": [
    "zrndr_span_occlusion_test_depth_order_pair_smoke"
  ],
  "known_limits": [
    "Binary Ninja keeps a limited reconstruction marker because dense x87 depth interpolation and bias comparisons still render with HLIL artifacts.",
    "Local isolated VC5SP3 byte comparison now covers this helper and fails with 726 unmasked mismatches after 60 relocation-masked bytes and 11 trimmed VC NOPs; BN size 793, VC5 size 528."
  ],
  "functional_evidence": [
    "Binary Ninja assembly for 0x4907c0 validates the rhs single-sample branch, lhs single-sample branch, overlap min/max handling, depth-slope interpolation, use of the recovered span depth-bias globals, start/end/midpoint comparisons, and boolean return.",
    "The native span-depth smoke verifies single-point and multi-sample overlap cases where the lhs span is in front, behind, or intersects across the overlap midpoint."
  ],
  "vc5_attempt": "python tools/recoil.py verify vc5 0x4907c0 (2026-06-16, vc5_o2_ob0_md_facs, local ignored target zrndr_span_occlusion_test_span_depth_order_pair) fails with 726 unmasked mismatches after 60 relocation-masked bytes and 11 trimmed VC NOPs; BN size 793, VC5 size 528."
}
