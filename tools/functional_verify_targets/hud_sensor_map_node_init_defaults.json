{
  "address": "0x415b40",
  "name": "hud_sensor_map_node_init_defaults",
  "description": "Resets a sensor-map node to the default color, empty point array, disabled state, no selection, and both blink colors set to white.",
  "source_from": "src/Battlesport/map.cpp",
  "smoke_tests": [
    "hud_sensor_map_node_basics_smoke"
  ],
  "known_limits": [
    "No isolated VC COFF byte comparison is recorded; tier S verification is deferred to the coherent HudSensorTracker/map.cpp source-cluster pass."
  ],
  "functional_evidence": [
    "Binary Ninja assembly for 0x415b40 shows direct default field stores with no authored callees; the native smoke validates default color bytes, counters, pointers, flags, selection, blink timer, and packed color pair."
  ]
}
