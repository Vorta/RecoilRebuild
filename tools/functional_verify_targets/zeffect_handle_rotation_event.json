{
  "name": "zeffect_handle_rotation_event",
  "description": "zEffect::HandleRotationEvent applies camera position updates or object rotation updates using optional basis offsets.",
  "address": "0x459ae0",
  "source_from": "src/GameZRecoil/zEffect/zeff_anim_run.c",
  "smoke_tests": [
    "zeffect_handle_rotation_event_smoke"
  ],
  "known_limits": [
    "No isolated VC COFF byte comparison is recorded for this event handler; tier S verification is deferred to a coherent zEffect event-handler/source-cluster pass."
  ],
  "functional_evidence": [
    "Binary Ninja evidence for 0x459ae0 validates target node lookup, camera class handling through translate/set position, object3D handling through set/translate rotation, optional basis-node resolution from nodeRefList or reset scratch, basis rotation via gwObject3DGetRotation for flag 0x02, basis matrix Euler extraction for flag 0x04, and return value 2.",
    "The native zEffect smoke verifies camera translate and set-position branches, object3D translate and set-rotation branches, basis rotation offset, matrix-derived basis path, and caller-visible rotation/dirty-state effects."
  ],
  "vc5_attempt": ""
}
