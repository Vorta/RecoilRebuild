{
  "name": "hud_ui_check_toggle_widget_scalar_deleting_destructor",
  "description": "HudUiCheckToggleWidget::ScalarDeletingDestructor runs DestructorCore, optionally frees the object for scalar-delete flags, and returns the original this pointer.",
  "address": "0x4b7000",
  "source_from": "src/GameZRecoil/zUI/zui.cpp",
  "smoke_tests": [
    "zhud_check_toggle_widget_helpers_smoke"
  ],
  "known_limits": [
    "tier S verification remains deferred to the HudUiCheckToggleWidget source-cluster pass."
  ],
  "functional_evidence": [
    "Binary Ninja assembly/decompilation for 0x4b7000 validates the call to HudUiCheckToggleWidget::DestructorCore, the conditional operator delete call when flags bit 0 is set, returning the saved this pointer, and callee stack cleanup for the flags argument.",
    "Native smoke zhud_check_toggle_widget_helpers_smoke verifies the non-deleting path restores the base HudUiElement dispatch state through DestructorCore and the heap scalar-delete path returns the original object pointer while freeing the allocation."
  ],
  "vc5_attempt": ""
}
