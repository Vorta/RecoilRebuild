{
  "name": "hud_sensor_map_node_set_enabled",
  "description": "HudSensorMapNode::SetEnabled updates the enabled state only when changed, refreshes packed color, clears selected point, and reports whether the state changed.",
  "address": "0x415ae0",
  "source_from": "src/Battlesport/map.cpp",
  "smoke_tests": [
    "hud_sensor_map_node_basics_smoke"
  ],
  "known_limits": [
    "tier S verification is deferred to a coherent HudSensor map-node helper source-cluster pass."
  ],
  "functional_evidence": [
    "Binary Ninja assembly for 0x415ae0 validates thiscall ECX=self, stack enabled argument, no-op return when isEnabled at offset 0x18 already matches, changed-state write, SetColorRgb(nullptr), SelectPoint(-1), return 1, and callee stack cleanup.",
    "The native HudSensorMapNode basics smoke verifies state-change return, isEnabled update, selected-point clearing, and unchanged-state return."
  ]
}
