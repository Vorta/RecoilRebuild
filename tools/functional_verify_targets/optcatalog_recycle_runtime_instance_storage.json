{
  "name": "optcatalog_recycle_runtime_instance_storage",
  "description": "OptCatalog::RecycleRuntimeInstanceStorage recycles expired runtime projectile storage by detaching parent/child links, handling attach clones, pushing the runtime onto the free list, resetting transforms, and clearing the damage handler.",
  "address": "0x4ae590",
  "source_from": "src/GameZRecoil/zWeapon/zwep_init.c",
  "smoke_tests": [
    "zweapon_optcatalog_runtime_free_list_helpers_smoke"
  ],
  "known_limits": [
    "tier S verification is deferred to a coherent OptCatalog/zWeapon runtime-storage pass; the original lifetime gate uses x87 status-word codegen."
  ],
  "functional_evidence": [
    "Binary Ninja assembly for 0x4ae590 validates the lifetime <= 0 gate, parent detach loop, attach clone/template cleanup, child detach loop, g_OptCatalogFreeRuntimeInstanceList push, scale/rotation/position reset, and projectile damage-handler clear.",
    "The native zWeapon runtime free-list smoke verifies expired runtime recycling detaches parent and child relationships, relinks the runtime free list, resets object transform fields, and clears the projectile damage handler."
  ]
}
