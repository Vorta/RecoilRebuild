{
  "name": "zeffect_anim_queue_cmd_type2_velocity",
  "description": "zEffectAnim::QueueCmdType2Velocity conditionally queues or dispatches a command type 2 activation record containing the animation name, validated bound-node token, and velocity vector.",
  "address": "0x461aa0",
  "source_from": "src/GameZRecoil/zEffect/zeff_anim_save.c",
  "smoke_tests": [
    "zeffect_anim_activation_record_queue_smoke"
  ],
  "known_limits": [
    "tier S verification was not attempted in this session; coverage is functional through queue, dispatch, and per-entry override cases."
  ],
  "functional_evidence": [
    "Binary Ninja assembly for 0x461aa0 validates validated-node-token lookup, global queue/dispatch gates, per-entry queue/dispatch override bits, no-record cases for disabled gates, type 2 record allocation, name copy, node token storage, velocity parameter storage, optional callback dispatch, discard when dispatch-only, and return of the dispatched record.",
    "The native zEffect activation-record smoke verifies disabled queue behavior, queued type 2 record fields and packed size, dispatch-only callback behavior, and entry override behavior."
  ]
}
