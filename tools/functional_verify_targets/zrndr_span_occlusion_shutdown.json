{
  "name": "zrndr_span_occlusion_shutdown",
  "description": "zRndr::SpanOcclusionShutdown frees the span-occlusion column-head table and span-node pool, clears those two globals, and returns.",
  "address": "0x490780",
  "source_from": "src/GameZRecoil/zRender/zrndr_draw.c",
  "smoke_tests": [
    "zrndr_span_occlusion_reset_shutdown_smoke"
  ],
  "tier_s_evidence": [
    "VC5SP3 cl 11.00.7022 x86 /O2 /Ob0 /MD /Zp4 /FAcs COFF byte comparison for ?SpanOcclusionShutdown@zRndr@@YAHXZ at 0x490780 passes with zero unmasked byte mismatches after masking 20 relocation bytes and trimming 3 NOP bytes, BN size 61 bytes, VC object symbol size 64 bytes.",
    "The retained int return type and return 0 source shape match BN's xor eax before pop esi/retn; the only current caller RecoilApp::ShutdownEngine invokes the shutdown helper as a statement. The native smoke verifies reset behavior, shutdown frees, global nulling, and repeated null shutdown."
  ],
  "vc5_attempt": "python tools/recoil.py verify vc5 0x490780"
}
