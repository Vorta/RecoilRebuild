{
  "name": "zgeometry_weiler_create_forward_segment_pair_at_point",
  "description": "zGeometry_Weiler::CreateForwardSegmentPairAtPoint splits two forward contour segments at a shared point and relinks the inserted segments before each original successor.",
  "address": "0x468650",
  "source_from": "src/GameZRecoil/zGeometry/zgeo_weiler.cpp",
  "smoke_tests": [
    "zgeometry_vec3array_clip_leaf_helpers_smoke"
  ],
  "known_limits": [
    "tier S verification is deferred to the coherent zGeometry Weiler source-cluster pass."
  ],
  "functional_evidence": [
    "Binary Ninja assembly for 0x468650 validates the two GetAppendSpace iterations, ORed contour-type flags, split-point start assignment, end-point and end-crossing copies, null contour output, bounds update calls, and relinking between each segment and its prior successor.",
    "The native clip leaf smoke verifies the two inserted segment links, contour-type masks, split-point start pointers, copied end points, copied crossing pointer, null contour output, and segment-buffer count."
  ]
}
