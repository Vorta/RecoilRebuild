{
  "name": "zreader_build_resolved_parent_dir",
  "description": "zReader::BuildResolvedParentDir resolves a zReader-locatable filename, expands it to an absolute path, splits it with CRT path semantics, and writes the drive plus directory with the trailing separator preserved.",
  "address": "0x421e20",
  "source_from": "src/Battlesport/player.cpp",
  "smoke_tests": [
    "zreader_resolve_and_open_file_smoke"
  ],
  "known_limits": [
    "tier S verification was not attempted in this session; final byte matching should be handled with the coherent zReader path-resolution source-cluster pass."
  ],
  "functional_evidence": [
    "Binary Ninja assembly for 0x421e20 validates fastcall ECX=filename, EDX=outParentDir, zReader::TryResolvePath(filename, NULL), _fullpath(..., 0x104), _splitpath into drive/dir/name/ext locals, and sprintf(outParentDir, \"%s%s\", drive, dir).",
    "The native zReader resolve/open smoke creates a temporary file, verifies direct and search-path resolution, calls zReader::BuildResolvedParentDir, computes the expected parent directory with _fullpath and _splitpath, and compares the resulting drive-plus-directory string."
  ],
  "owner_data_evidence": [
    "Source owner candidate: 0x421e20 is part of the zReader namespace path-resolution source cluster and calls zReader::TryResolvePath before CRT _fullpath/_splitpath/sprintf provider calls.",
    "Data gate candidate: 0x421e20 has only stack-local path buffers and no direct authored global writes; any data dependency is through zReader::TryResolvePath and the accepted zReader search-path closure."
  ],
  "vc5_attempt": ""
}
