{
  "name": "czrecoil_frame_create_object",
  "description": "CZRecoilFrame::CreateObject is the MFC runtime factory for CZRecoilFrame: allocate frame storage, construct it when allocation succeeds, and return the constructed object or null.",
  "address": "0x4301e0",
  "source_from": "src/Battlesport/RecoilApp.cpp",
  "smoke_tests": [
    "czrecoil_frame_create_object_smoke",
    "czrecoil_frame_constructor_smoke"
  ],
  "known_limits": [
    "Tier C only; no VC COFF byte-comparison target exists yet, so tier S remains open.",
    "Binary Ninja reconstruction is limited by compiler-generated EH allocation cleanup around the constructor call; the source preserves the original-era new/constructor factory shape with an exception cleanup path."
  ],
  "functional_evidence": [
    "Binary Ninja assembly/decompilation at 0x4301e0 validates MSVC EH setup, operator new(0x230), null-allocation return 0, CZRecoilFrame::Constructor call on allocated storage, and return of the constructed frame pointer.",
    "The dependency frontier for 0x4301e0 has no visible source blockers; CZRecoilFrame::Constructor is accepted functional evidence and operator new/EH glue are compiler/runtime providers.",
    "The native smoke calls CZRecoilFrame::CreateObject directly, verifies constructor side effects and returned object shape, then runs the destructor and frees the allocation.",
    "The metadata native smoke verifies CZRecoilFrame::classCZRecoilFrame.m_pfnCreateObject points at CZRecoilFrame::CreateObject."
  ]
}
