{
  "name": "zvideo_module_init",
  "description": "zVideo::ModuleInit seeds zVideo global defaults, initializes pixel-pack descriptors, binds the software fullscreen renderer dispatch, performs DirectDraw default selection, registers teardown with atexit, and returns success.",
  "address": "0x4a7530",
  "source_from": "src/GameZRecoil/zVideo/zvid_main.c",
  "smoke_tests": [
    "zvideo_module_init_smoke"
  ],
  "known_limits": [
    "VC5SP3 11.00.7022 /O2 /Ob0 /Zp4 COFF byte compare fails with 81 unmasked mismatches after masking 64 relocation bytes; Binary Ninja body is 162 bytes and the VC object body is 160 bytes with 3 trailing VC NOPs trimmed.",
    "The reviewed failure is register-zero selection, store encoding, argument setup ordering, and call relocation drift across already accepted pixel-pack, dispatch-binding, DirectDraw enumeration, and CRT atexit dependencies.",
    "The native smoke verifies success return, bulk zVideo module-global zeroing through the sorted/overwrite queue state, key zeroed defaults, palette/fullscreen/dither defaults, texture pixel-pack seed state, default hardware API selection, and cleared selected D3D device pointer."
  ],
  "vc5_attempt": "python tools/recoil.py verify vc5 0x4a7530"
}
