{
  "name": "player_tick_remote_network_player",
  "description": "Player::TickRemoteNetworkPlayer updates network pose state, remote HUD position, damage handling, and root transform. Direct production host: mission.cpp.",
  "address": "0x41b950",
  "source_from": "src/Battlesport/mission.cpp",
  "smoke_tests": [
    "player_tick_remote_network_player_smoke"
  ],
  "known_limits": [
    "Binary Ninja assembly/HLIL inspection in the current session verified the netUpdateReceived ground-sample gate, fxOffsetWorld pre-update from worldPos plus fxOffsetLocal, GameNet::UpdateRemotePlayerHudWidgetScreenPos call, cameraTransitionTimer direct-copy path from offsets 0x3f8 and 0x3c8, zero-transition Vec3Lerp at 0.649999976, destroyed-lifecycle skip of alt-gun dispatch helpers, ResetDamageVisualsAndTimedStatus plus ApplyDamageLocal, cameraTransitionTimer set after depleted damage, and final root position/rotation writes.",
    "The native smoke covers the transition direct-copy path and the zero-transition lerp path with destroyed lifecycle damage feedback, verifying the network-received vector fields, pre-update fxOffsetWorld, output angles, camera-transition preservation, and root transform writes.",
    "The smoke disables name-tag HUD projection through the functional GameNet status flag so the dependency call is exercised only through the documented early-return behavior.",
    "The smoke does not drive the netUpdateReceived ground-sampling branch because that path is covered by Player::SampleGroundAndAlignRootToSurface and depends on DI scene pick-candidate fixtures.",
    "No VC object-byte manifest covers 0x41b950 yet, so tier S verification is deferred to a coherent Player class/source-cluster pass."
  ],
  "vc5_attempt": "python tools/recoil.py verify vc5 0x41b950"
}
