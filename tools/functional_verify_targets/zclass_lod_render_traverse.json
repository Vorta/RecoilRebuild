{
  "name": "zclass_lod_render_traverse",
  "description": "zClass_Lod::RenderTraverse gates by distance state, manages LOD render state, and dispatches active children.",
  "address": "0x44b8c0",
  "source_from": "src/GameZRecoil/zClass/Seq.c",
  "smoke_tests": [
    "zclass_render_traverse_dispatch_smoke"
  ],
  "known_limits": [
    "Binary Ninja keeps a limited reconstruction marker for the LOD traversal due dense range/fade arithmetic and nested render-state paths.",
    "No isolated VC COFF byte comparison is recorded for LOD render traversal; tier S verification is deferred to a coherent zClass render traversal source-cluster pass."
  ],
  "functional_evidence": [
    "Binary Ninja/source review validates the visible gate, distance-window acceptance, clip-mask stack push, child dispatch, LOD stack management, range-fade reset, and cleanup order.",
    "The native render traversal dispatch smoke verifies a distance-accepted LOD node dispatches its child and restores the clip and LOD stack state."
  ]
}
