{
  "name": "player_update_modal_loop_sfx",
  "description": "zUtil_SaveGameState::UpdateModalLoopSfx starts, stops, and updates the active modal loop SFX blend.",
  "address": "0x4386c0",
  "source_from": "src/Battlesport/player.cpp",
  "smoke_tests": [
    "player_update_modal_loop_sfx_smoke"
  ],
  "known_limits": [
    "tier S verification is deferred to a coherent Player modal-SFX/source-cluster pass; this function depends on neighboring modal helper methods and zSound backend/provider dispatch shape."
  ],
  "functional_evidence": [
    "Binary Ninja assembly for 0x4386c0 validates the thiscall save-state ABI, disabled stop order for modal slots 2, 0, 1 and master-type slot 3, startup checks for modal SFX slots 0, 1, and 2, speed/throttle blend selection, [0, 1] clamping, pitch-scale multiplication, enable-scale dispatch, and world-position Update3DDispatch calls.",
    "The native smoke exercises the active update path with existing DirectSound-backed handles, verifies the speed-derived modeLoopBlend, verifies two pitch updates and six pan/volume dispatches from SetEnableScale plus explicit Update3DDispatch, and verifies the disabled cleanup path clears all affected handle slots."
  ]
}
