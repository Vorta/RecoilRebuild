{
  "name": "zclass_node_set_di_flag_bit0_recursive",
  "description": "zClass_Node::SetDiFlagBit0Recursive fetches each node's user data, toggles DI flag bit 0 when a display instance exists, and recursively applies the same value to listB children.",
  "address": "0x437ea0",
  "source_from": "src/GameZRecoil/zClass/Class.c",
  "smoke_tests": [
    "zclass_node_predicate_helpers_smoke"
  ],
  "known_limits": [
    "tier S verification is deferred to the zClass node/display-instance source-cluster pass."
  ],
  "functional_evidence": [
    "Binary Ninja assembly for 0x437ea0 validates the fastcall node/enabled arguments, gwNodeGetUserData call into a stack output, conditional zDi::SetFlagBit0 call, and forward recursion over listB children.",
    "The native zClass predicate helper smoke verifies both set and clear operations propagate through a parent/child/grandchild tree and skip nodes without display-instance user data."
  ]
}
