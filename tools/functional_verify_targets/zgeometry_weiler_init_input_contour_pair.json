{
  "name": "zgeometry_weiler_init_input_contour_pair",
  "description": "zGeometry_Weiler::InitInputContourPair builds forward and reverse contour segment rings, ensures contour outputs, and updates the forward segment bounds.",
  "address": "0x464b90",
  "source_from": "src/GameZRecoil/zGeometry/zgeo_weiler.cpp",
  "smoke_tests": [
    "zgeometry_vec3array_clip_leaf_helpers_smoke"
  ],
  "known_limits": [
    "fprintf is provider behavior for failure paths. tier S verification is deferred to the coherent zGeometry Weiler source-cluster pass."
  ],
  "functional_evidence": [
    "Binary Ninja frontier for 0x464b90 shows all direct authored callees are functional-equivalent: GetAppendSpace, InitFromPointList, EnsureContourOutput, and UpdateBounds.",
    "The native clip leaf smoke verifies segment-buffer allocation for forward/reverse rings, contour-buffer output creation for both rings, forward contour type/bounds, reverse contour type, and closed-ring next pointers."
  ]
}
