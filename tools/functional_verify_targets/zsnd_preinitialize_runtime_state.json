{
  "name": "zsnd_preinitialize_runtime_state",
  "description": "zSnd_PreInitializeRuntimeState performs one-time sound runtime reset, CD defaults, option binding, mute depth, and backend-handle preservation/reset by backend type.",
  "address": "0x4a12c0",
  "source_from": "src/GameZRecoil/zSound/zsnd_init.cpp",
  "smoke_tests": [
    "zsnd_preinitialize_runtime_state_smoke"
  ],
  "known_limits": [
    "VC5SP3 11.00.7022 /MD /O2 /Ob1 /GX /Zp4 COFF byte compare currently fails for zSnd_PreInitializeRuntimeState: 98 unmasked mismatches after masking 156 relocation bytes; Binary Ninja body is 270 bytes and the VC object body is 288 bytes with 7 trailing VC NOPs trimmed.",
    "The reviewed byte drift is initialization ordering/control-flow shape around backend reset, CD defaults, and option lookup binding; the native smoke verifies one-time return behavior, CD/runtime resets including g_zSndCdTrackListCount while preserving the separate static track-list lifecycle count, backend reset or preservation by backend type, option fallback binding, mute depth, active sample/voice pointer clearing without marker cursor reset, and volume option binding."
  ],
  "vc5_attempt": "python tools/recoil.py verify vc5 zsnd_preinitialize_runtime_state"
}
