{
  "name": "czgame_frame_on_move",
  "description": "CZGameFrame::OnMove performs default MFC move handling and refreshes cached video client-rect screen coordinates when requested.",
  "address": "0x443a50",
  "source_from": "src/CZGameFrame/CZGameFrame.cpp",
  "smoke_tests": [
    "zvid_cached_client_rect_smoke"
  ],
  "known_limits": [
    "The native smoke covers the authored zVid cached-client-rect side effect through the accepted helper because direct CZGameFrame::OnMove invocation requires MFC dispatch thread state for CWnd::Default.",
    "tier S remains open until the coherent CZGameFrame/MFC provider source-file verification pass fixes the shared VC compile blocker and accepts byte comparison."
  ],
  "functional_evidence": [
    "Binary Ninja assembly/decompilation at 0x443a50 validates CWnd::Default(self) followed by zVid::UpdateCachedClientRectIfUpdateMaskEnabled(), then a thiscall-style return that pops the x and y move arguments.",
    "The dependency frontier for 0x443a50 has no visible source blockers; CWnd::Default is an MFC42 provider call and zVid::UpdateCachedClientRectIfUpdateMaskEnabled is already accepted functional source.",
    "The native zvid_cached_client_rect_smoke verifies the cached rect remains unchanged when the update mask is zero and refreshes to the window client dimensions when the mask is nonzero."
  ],
  "vc5_attempt": "python tools/recoil.py verify vc5 0x443a50: local VC5SP3 CZGameFrame.cpp manifest compile currently fails in the shared CZGameFrame MFC provider/source-file shape before byte comparison; existing CZGameFrame targets such as 0x443b50 fail the same header/message-map compile preflight."
}
