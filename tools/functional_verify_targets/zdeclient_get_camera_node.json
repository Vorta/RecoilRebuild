{
  "name": "zdeclient_get_camera_node",
  "description": "zDEClient::GetCameraNode returns the current zDEClient camera node global.",
  "address": "0x458ae0",
  "source_from": "src/GameZRecoil/zDEClient/zdec_crater.cpp",
  "smoke_tests": [
    "zdeclient_feature_leaf_helpers_smoke"
  ],
  "known_limits": [
    "No isolated VC COFF byte comparison is recorded; tier S verification is deferred to the coherent zDEClient initialization/source-cluster pass."
  ],
  "functional_evidence": [
    "Binary Ninja assembly for 0x458ae0 validates a single load of g_zDEClient_CameraNode and return.",
    "The native zDEClient feature leaf smoke installs a camera node global and verifies GetCameraNode returns it exactly."
  ]
}
