{
  "name": "zsnd_snapshot_create_from_active_samples",
  "description": "zSndPlayHandleSnapshot::CreateFromActiveSamples captures the global-volume anchor plus active DirectSound and A3D primary and duplicate play handles into the snapshot list.",
  "address": "0x49fff0",
  "source_from": "src/GameZRecoil/zSound/zsnd_play.cpp",
  "smoke_tests": [
    "zsnd_snapshot_create_from_active_samples_smoke"
  ],
  "known_limits": [
    "VC5SP3 /O2 /Ob1 /GX /Zp4 COFF byte compare fails with 317 unmasked mismatches after relocation masking; Binary Ninja body is 778 bytes and the VC object body is 784 bytes with 10 trailing VC NOPs trimmed.",
    "A refreshed 2026-06-03 full profile sweep against the current src/GameZRecoil/zSound/zsnd_play.cpp source found vc5_o2_ob1_gx_facs, vc5_o2_ob1_md_gx_facs, vc5_o2_ob1_gx_uintptr_facs, vc5_o2_ob1_md_gx_afx_uintptr_win32ie_facs, and vc5_o2_ob1_gx_facs tied as closest at 317 mismatches; /Ob0 profiles were 675-677 mismatches, VC5SP3 /Oy /Ob0 was 677 mismatches with a 576-byte object, plain VC5 /Ob1 without /GX was 687 mismatches, VC5/VC5SP3 /Ob2 /GX profiles were 707 mismatches, and VC5 /Ob2 without /GX was 718 mismatches.",
    "Reviewed drift is control-flow and register scheduling around backend GetStatus and append paths; current classified asm drift has 61 exact/normalized instruction matches, 56 byte-identical spelling differences, 10 relocation-sensitive differences, 0 accepted schedule-equivalent pairs, 7 branch-displacement mismatches, 16 register-allocation mismatches, 2 structural LCS drift blocks, and 131 remaining instruction mismatches. The diagnostic LCS section is shape-based only and reports structural drift without changing acceptance: the A3D duplicate splice has BN test edi, edi; mov [edx], eax; je where the VC object has lea esi, payload; mov [ecx], eax, followed by BN-only payload-source lea esi, [esp+0x2c]. The COD extractor now stops at the main _TEXT ENDS boundary so the separate text$x EH helper is not included in classified text diagnostics; the COFF byte comparison was already using the .text function section bytes and remains unchanged. Smoke covers anchor payload, DirectSound and A3D active voice capture, duplicate voice skipping, item count, and circular list links.",
    "Rejected source-shape attempts: adding the BN-visible payload-pointer guard to the inlined A3D duplicate append worsens to 489 mismatches by perturbing loop register allocation; marking the inlined A3D duplicate node local volatile worsens to 436 mismatches and expands the object body to 816 bytes; materializing the A3D duplicate payload destination as a volatile nodePayload local worsens to 343 mismatches; changing the inline backend-status helpers from bool to int is neutral at 317 mismatches and adds VC5SP3 bool-conversion warnings; changing only DirectSoundBufferIsPlaying to accept LPDWORD status and moving the (LPDWORD)&status casts to the DirectSound call sites is neutral at 317 mismatches; inlining the two DirectSound GetStatus calls directly in CreateFromActiveSamples is neutral at 317 mismatches; changing the shared backend-status helper status scratches from int to DWORD was compile-rejected because adjacent A3D provider GetStatus paths require int * and the probe would need unrelated casts; materializing node->prev into a nodePrev local is neutral at 317 mismatches; replacing the saved A3D duplicate predecessor local with direct listHead->prev expressions worsens to 338 mismatches; saving &listHead->prev in a prevLink pointer-to-link local and assigning through it is byte-neutral at 317 mismatches; spelling the manual A3D duplicate itemCount update as itemCount = itemCount + 1 is byte-neutral at 317 mismatches; adding duplicateVoices locals for the duplicate loops worsens to 532 mismatches and expands the object body to 816 bytes; adding a narrower A3D-only duplicateVoices local worsens to 522 mismatches, 76 relocation-masked bytes, an 800-byte object body, and 8 trimmed trailing VC NOPs; rewriting the A3D duplicate loop as an explicitly initialized while loop with a tail increment is byte-neutral at 317 mismatches; rewriting the DirectSound duplicate loop as an early continue for null voices is byte-neutral at 317 mismatches; swapping the private two-argument backend-status helper parameter order to pass status before the backend buffer is byte-neutral at 317 mismatches; adding register hints to the inlined A3D backend-status helper source local or vtable local is byte-neutral at 317 mismatches; adding a register hint to the inlined DirectSound backend-status helper buffer local is byte-neutral at 317 mismatches; spelling the A3D primary GetStatus call with explicit vtable locals triggers a VC5SP3 C1001 internal compiler error; naming status pointer locals in the DirectSound/A3D backend-status helpers is byte-neutral at 317 mismatches; removing const from the manual A3D duplicate listHead/prev/node locals is byte-neutral at 317 mismatches; replacing the inline C++ constructor call with manual operator-new plus field initialization worsens to 702 mismatches, 64 relocation-masked bytes, BN size 778, VC object size 736, and 14 trailing VC NOPs trimmed; changing sample-set/sample loop counters and counts to unsigned int is byte-neutral at 317 mismatches; placing case 0 before case 1 in the backend switch is byte-neutral at 317 mismatches."
  ],
  "vc5_attempt": "python tools/recoil.py verify vc5 0x49fff0"
}
