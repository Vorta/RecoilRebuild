{
  "name": "zmodel_instance_update_scrolling_textures_if_needed",
  "description": "zModel_Instance::UpdateScrollingTexturesIfNeeded updates scrolling material UVs once per video frame tick and skips null or already-updated instances.",
  "address": "0x478fc0",
  "source_from": "src/GameZRecoil/zModel/gmod_init.c",
  "smoke_tests": [
    "zmodel_scrolling_texture_update_smoke"
  ],
  "known_limits": [
    "tier S verification was not attempted in this session. Functional coverage targets the per-frame guard and material flag dispatch; the wrapped UV arithmetic is covered by zmodel_instance_update_scrolling_textures."
  ],
  "functional_evidence": [
    "Binary Ninja assembly for 0x478fc0 validates null return -1, frame-tick guard at instance offset 0x2c, surface-entry loop, material flag bit 0 gate, vertex-count low-byte argument, scroll-state pointer at instance offset 0x24, and call into zModel_Instance::UpdateScrollingTextures.",
    "The native zModel smoke verifies the frame tick is stored, one flagged material entry is updated through the helper, the second same-frame call is skipped, and null input returns -1."
  ]
}
