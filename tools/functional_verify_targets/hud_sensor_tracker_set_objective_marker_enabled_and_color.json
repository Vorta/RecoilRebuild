{
  "name": "hud_sensor_tracker_set_objective_marker_enabled_and_color",
  "description": "HudSensorTracker::SetObjectiveMarkerEnabledAndColor applies a color and enabled state to every map node matching an objective index.",
  "address": "0x4172c0",
  "source_from": "src/Battlesport/map.cpp",
  "smoke_tests": [
    "hud_sensor_objective_marker_enable_color_smoke"
  ],
  "known_limits": [
    "tier S verification is deferred to a coherent HudSensor objective/map source-cluster pass."
  ],
  "functional_evidence": [
    "Binary Ninja assembly for 0x4172c0 validates traversal from mapNodeListHead at tracker offset 0x40, matching objectiveIndex at node offset 0x10, SetColorRgb(colorRgb24), SetEnabled(enabled), next traversal at node offset 0, return 1, and callee stack cleanup for the three arguments.",
    "The native HudSensor objective marker smoke verifies that every matching node receives the color and enabled state while nonmatching nodes are left unchanged."
  ]
}
