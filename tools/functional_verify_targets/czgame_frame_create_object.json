{
  "name": "czgame_frame_create_object",
  "description": "CZGameFrame::CreateObject is the MFC runtime factory for CZGameFrame: allocate frame storage, construct it with the game app id, and return the constructed frame or null.",
  "address": "0x443730",
  "source_from": "src/CZGameFrame/CZGameFrame.cpp",
  "smoke_tests": [
    "czgame_frame_create_object_smoke",
    "czgame_frame_constructor_smoke"
  ],
  "known_limits": [
    "Tier C only; no VC COFF byte-comparison target exists yet, so tier S remains open.",
    "The original body contains compiler-generated EH allocation cleanup around the constructor call; source preserves the original-era new/constructor factory shape with an exception cleanup path."
  ],
  "functional_evidence": [
    "Binary Ninja assembly/decompilation at 0x443730 validates operator new(0xcc), null-allocation return 0, CZGameFrame::Constructor call with the literal \"gamez\", and return of the constructed frame pointer.",
    "The dependency frontier for 0x443730 has no visible source blockers; CZGameFrame::Constructor is accepted functional evidence and operator new/EH glue are compiler/runtime providers.",
    "The native smoke calls CZGameFrame::CreateObject directly, verifies constructor side effects and returned object shape, then runs the destructor and frees the allocation.",
    "The metadata native smoke verifies CZGameFrame::classCZGameFrame.m_pfnCreateObject points at CZGameFrame::CreateObject."
  ]
}
