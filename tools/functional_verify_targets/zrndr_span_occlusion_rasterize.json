{
  "name": "zrndr_span_occlusion_rasterize",
  "description": "zRndr::SpanOcclusionRasterizeOccluderPoly converts occluder polygons into per-column span nodes through the active span-list builder.",
  "address": "0x4927d0",
  "source_from": "src/GameZRecoil/zRender/zrndr_draw.c",
  "smoke_tests": [
    "zrndr_span_occlusion_rasterize_smoke"
  ],
  "known_limits": [
    "VC5SP3 cl 11.00.7022 x86 /O2 /Ob0 /Zp4 /FAcs COFF byte comparison for ?SpanOcclusionRasterizeOccluderPoly@zRndr@@YIXPAUSpanOccluderPolyPartial@1@H@Z at 0x4927d0 still fails: 1675 unmasked mismatches, 204 relocation bytes masked, no trailing VC NOP bytes trimmed, BN size 1828 bytes, VC object symbol size 2000 bytes.",
    "Current source follows the BN-proven reduced-vertex and dual zRndr_ScanConvertEdge[64] fixed-point edge-table rasterization shape. It uses source/destination cursors for the duplicate-filter reduced-vertex loop, uses a 0x504 scratch overlay for the reduced-vertex buffer and span-list pointer scratch passed to gRndr_pfnBuildSpanList, uses the BN-observed VC5 double-bias 16.16 conversion inline, and mirrors the duplicated scan-mode edge-builder branch structure with no local y-clip or edge-count-zero guards. Functional smoke covers rectangular and triangular occluders, span allocation, per-column min/max coverage, depth values, and empty-column behavior through the real span-list builder. Explicit sourcePoly pointer-local spelling was byte-neutral at 1675 mismatches and was reverted. Remaining tier S drift is reduced-vertex and edge-builder loop codegen."
  ],
  "vc5_attempt": "python tools/recoil.py verify vc5 zrndr_span_occlusion_rasterize"
}
