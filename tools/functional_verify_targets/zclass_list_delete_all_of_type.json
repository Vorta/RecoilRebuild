{
  "name": "zclass_list_delete_all_of_type",
  "description": "zClass_List::DeleteAllOfType processes deferred work, deletes all nodes in a type bucket, verifies the bucket is empty, clears the bucket tail, and reports failure when undeleted nodes remain.",
  "address": "0x44f120",
  "source_from": "src/GameZRecoil/zClass/List.c",
  "smoke_tests": [
    "zclass_shutdown_core_smoke"
  ],
  "known_limits": [
    "Tier C only; no VC COFF byte-comparison target exists yet, so tier S remains open.",
    "The native smoke reaches DeleteAllOfType through zClass::Shutdown/zClass::ShutdownCore on an empty bucket; non-empty deletion/error paths are accepted from Binary Ninja evidence and accepted zClass list dependencies."
  ],
  "functional_evidence": [
    "Binary Ninja assembly at 0x44f120 calls zClass::ProcessDeferredWork, scans the selected type-list bucket, calls zClass_List::gwListDeleteANode for nodes until no deletable nodes remain, repeats deferred processing, reports old zError failures when nodes remain, verifies CountNodes is zero, clears the bucket tail slot, and returns 0 on success.",
    "Binary Ninja decompilation for 0x44f120 has no warnings or tags and shows the same deferred-work loop, gwListDeleteANode calls, error reporting, CountNodes check, tail clear, and success/failure returns.",
    "The dependency frontier for 0x44f120 has no visible source blockers; direct list/delete/error helpers are accepted.",
    "The native zclass_shutdown_core_smoke reaches DeleteAllOfType(6) through zClass::ShutdownCore and verifies the shutdown path succeeds with bucket cleanup complete."
  ],
  "vc5_attempt": ""
}
