{
  "name": "zinput_keyboard_resume_from_suspend",
  "description": "zInput::Keyboard_ResumeFromSuspend resets keyboard transition state when suspended, then clears the keyboard suspend flag.",
  "address": "0x471cd0",
  "source_from": "src/GameZRecoil/zInput/zin_init.cpp",
  "smoke_tests": [
    "zinput_keyboard_reset_and_resume_smoke"
  ],
  "known_limits": [
    "Tier C only; no VC COFF byte-comparison target exists yet, so tier S remains open."
  ],
  "functional_evidence": [
    "Binary Ninja evidence validates the suspend-bit test, conditional Keyboard_ResetTransitionState call, and suspend-bit clear.",
    "The dependency frontier has no visible source blockers; Keyboard_ResetTransitionState is accepted functional evidence.",
    "The native smoke verifies keyboard transition-state reset and flag clearing."
  ]
}
