{
  "name": "hud_sensor_tracker_constructor",
  "description": "HudSensorTracker::Constructor initializes the tracker base state, default-constructs mission path strings, clears late HUD and pending-save fields, resets mission state, and returns self. Direct production host: mission.cpp.",
  "address": "0x417390",
  "source_from": "src/Battlesport/mission.cpp",
  "smoke_tests": [
    "hud_sensor_tracker_constructor_smoke"
  ],
  "known_limits": [
    "Tier C only; no VC COFF byte-comparison target exists yet, so tier S remains open."
  ],
  "functional_evidence": [
    "Binary Ninja decompilation and disassembly at 0x417390 show InitNoBounds, default construction of missionDataPath, zbdPath, and missionGsPath, clearing fxPass3Obj, setting hudScale to 1.0, clearing raceCheckpointMode, hasPendingPlayerSave, and pendingPlayerSave.skipTimerResetOnStart, then ResetMissionState and return self.",
    "Direct authored callees InitNoBounds and ResetMissionState are dependency-ready and functional-equivalent; CString construction is an accepted MFC provider boundary.",
    "The native constructor smoke verifies the returned self pointer, base tracker initialization including SetTrackedSaveState(0)'s mapZoom reset, cleared late HUD/pending-save fields, and ResetMissionState outputs."
  ],
  "vc5_attempt": "python tools/recoil.py verify vc5 --explain-missing 0x417390 reported no VC verification target currently covers this address."
}
