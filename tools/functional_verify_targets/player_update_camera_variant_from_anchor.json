{
  "name": "player_update_camera_variant_from_anchor",
  "description": "Player::UpdateCameraVariantFromAnchor merges a selected camera anchor variant tag with the current player tag, falls back to the last valid camera variant when incomplete, and updates camera/effect overrides.",
  "address": "0x406510",
  "source_from": "src/Battlesport/player.cpp",
  "smoke_tests": [
    "player_update_camera_variant_from_anchor_smoke"
  ],
  "known_limits": [
    "Binary Ninja disassembly/HLIL for 0x406510 validates fastcall ECX=candidates, EDX=cameraPos, stack selectedCandidateIndex, current player variant load, selected candidate variant fallback when candidateCount <= 0 or selected tag count is zero, merging missing player tags into g_VariantTag_Current up to count 3, rejecting merged tags containing 0xff, updating g_Player_LastValidCameraVariantTag only for complete merged tags, copying final tag to g_VariantTag_Current and g_Variant_CurrentTag, and calling zClass_Camera::gwCameraSetVariantTagOverride and zEffect::SetVariantOverridePackedIdsIfComplete.",
    "The native smoke verifies complete merge/update behavior, camera and zEffect override updates, incomplete-tag fallback to the last valid camera variant, and empty-candidate fallback.",
    "No VC object-byte manifest covers 0x406510 yet, so tier S verification is deferred to a coherent Player camera/source-cluster pass."
  ],
  "vc5_attempt": "python tools/recoil.py verify vc5 0x406510"
}
