{
  "name": "zvideo_dd3d_set_fog_enable",
  "description": "zVideo_dd3d::SetFogEnable updates cached Direct3D fog enable and fog mode state through provider calls only when needed.",
  "address": "0x4aa9e0",
  "source_from": "src/GameZRecoil/zVideo/zvid_main.c",
  "smoke_tests": [
    "zvideo_dd3d_set_fog_enable_smoke"
  ],
  "tier_s_evidence": [
    "VC5SP3 cl 11.00.7022 x86 /O2 /Ob0 /MD /Zp4 /FAcs COFF byte comparison for ?SetFogEnable@zVideo_dd3d@@YIXH@Z passes with zero unmasked mismatches after 24 relocation-masked bytes and 12 trimmed VC NOP bytes."
  ],
  "functional_evidence": [
    "Binary Ninja assembly for 0x4aa9e0 validates fastcall ECX=enable, compares against g_zVideo_CachedFogEnableRenderState, calls provider IDirect3DDevice2::SetRenderState through vtable offset 0x5c with D3DRENDERSTATE_FOGENABLE and enable when changed, updates g_zVideo_CachedFogEnableRenderState, then compares g_zVideo_CachedFogModeLightState against D3DFOG_LINEAR, calls provider IDirect3DDevice2::SetLightState through vtable offset 0x64 with D3DLIGHTSTATE_FOGMODE and D3DFOG_LINEAR when changed, and updates g_zVideo_CachedFogModeLightState.",
    "Binary Ninja decompilation for 0x4aa9e0 has no warnings and matches the existing source implementation. There are no authored callees; SetRenderState and SetLightState are Direct3DDevice2 provider calls.",
    "The dependency frontier shows no remaining authored source blockers.",
    "The native smoke installs a fake Direct3DDevice2 provider and verifies first-call render/light provider updates and cache writes, full cache-hit suppression of provider calls, and changed-enable render-state-only update while fog mode remains cached."
  ],
  "vc5_attempt": "python tools/recoil.py verify vc5 0x4aa9e0 passed with zero unmasked byte mismatches after 24 relocation-masked bytes and 12 trimmed VC NOP bytes under vc5_o2_ob0_md_facs."
}
