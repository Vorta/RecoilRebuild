{
  "name": "zgeometry_weiler_buffer_get_append_space",
  "description": "zGeometry_WeilerBuffer::GetAppendSpace reserves append space, grows capacity when needed, updates append pointer/count, and optionally returns the current base.",
  "address": "0x467660",
  "source_from": "src/GameZRecoil/zGeometry/zgeo_weiler.cpp",
  "smoke_tests": [
    "zgeometry_vec3array_clip_leaf_helpers_smoke"
  ],
  "known_limits": [
    "realloc is provider behavior. tier S verification is deferred to the coherent zGeometry Weiler source-cluster pass."
  ],
  "functional_evidence": [
    "Binary Ninja frontier for 0x467660 shows no authored callees and only realloc provider behavior.",
    "The native clip leaf smoke verifies growth by appendCount + 0x10, out-base reporting, returned append slot, count update, and append pointer advancement with and without reallocation."
  ]
}
