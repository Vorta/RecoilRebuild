{
  "address": "0x415c90",
  "name": "hud_sensor_map_node_update_cached_bounds",
  "description": "Returns cached bounds when requested, otherwise recomputes X/Z bounds from the node point array while preserving the prior maxY behavior.",
  "source_from": "src/Battlesport/map.cpp",
  "smoke_tests": [
    "hud_sensor_map_node_basics_smoke"
  ],
  "known_limits": [
    "No isolated VC COFF byte comparison is recorded; tier S verification is deferred to the coherent HudSensorTracker/map.cpp source-cluster pass.",
    "Binary Ninja records this routine as GetOrRecomputeCachedBounds; source keeps the clearer UpdateCachedBounds spelling already used by local callers."
  ],
  "functional_evidence": [
    "Binary Ninja assembly for 0x415c90 validates cached-copy versus recompute paths; the native smoke covers recomputation from two points and copying existing cached bounds."
  ]
}
