{
  "name": "hud_ui_numeric_text_input_scalar_deleting_destructor",
  "description": "HudUiNumericTextInput::ScalarDeletingDestructor runs the core destructor, optionally deletes the object when bit 0 is set, and returns the original this pointer.",
  "address": "0x4b4a90",
  "source_from": "src/GameZRecoil/zUI/zui.cpp",
  "smoke_tests": [
    "zhud_numeric_text_input_base_constructor_smoke"
  ],
  "known_limits": [
    "tier S verification remains deferred to the HudUiNumericTextInput source-cluster pass."
  ],
  "functional_evidence": [
    "Binary Ninja assembly/decompilation for 0x4b4a90 validates calling HudUiNumericTextInput::Destructor, testing flags bit 0, calling operator delete only when that bit is set, and returning the original this pointer.",
    "Native smoke zhud_numeric_text_input_base_constructor_smoke verifies the non-deleting path resets raw keyboard capture and subobject dispatch state, and verifies the deleting path on a heap object returns the original pointer while clearing raw keyboard capture."
  ],
  "vc5_attempt": ""
}
