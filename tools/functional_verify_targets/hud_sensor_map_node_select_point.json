{
  "name": "hud_sensor_map_node_select_point",
  "description": "HudSensorMapNode::SelectPoint selects a point by in-range index and clears selection on out-of-range input.",
  "address": "0x415b10",
  "source_from": "src/Battlesport/map.cpp",
  "smoke_tests": [
    "hud_sensor_map_node_basics_smoke"
  ],
  "known_limits": [
    "tier S verification is deferred to a coherent HudSensor map-node helper source-cluster pass."
  ],
  "functional_evidence": [
    "Binary Ninja assembly for 0x415b10 validates thiscall ECX=self, stack pointIndex, signed lower-bound and pointCount upper-bound checks, selectedPointIndex write at offset 0x14, points pointer load at offset 0x0c, 12-byte point stride, null return on invalid input, and stdcall-style callee stack cleanup.",
    "The native HudSensorMapNode basics smoke verifies valid point selection, returned point address, and invalid-index selection clearing."
  ]
}
