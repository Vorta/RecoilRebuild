{
  "name": "zdeclient_crater_execute",
  "description": "zDEClient_Crater::Execute handles crater event relay gating, packet population, and host/local routing for crater feature events.",
  "address": "0x433ad0",
  "source_from": "src/Battlesport/RecoilApp.cpp",
  "smoke_tests": [
    "zdeclient_crater_execute_smoke"
  ],
  "known_limits": [
    "tier S verification was not attempted in this session. Final byte matching should be handled with the coherent zDEClient/GameNet crater relay source-cluster pass."
  ],
  "functional_evidence": [
    "Binary Ninja assembly for 0x433ad0 validates the x87 radius compare against 0.0, non-positive radius negation with return 1, local-player root ownership gate through g_GameStateOrMapTable->playerState->rootNode, pkt0F payload player key, material-slot index, center, and radius writes, host local NetRelayCallback dispatch, non-host reliable send, and zero return on non-negation paths.",
    "The native zDEClient crater execute smoke verifies non-positive radius normalization, owner-mismatch no-op behavior, non-host reliable send packet contents without changing event flags, and host local relay through NetRelayCallback with packet population."
  ]
}
