{
  "name": "zclass_window_new",
  "description": "zClass_Window::gwWindowNew allocates a window node, initializes window data from the active render region, inserts it into bucket 14, and returns the node.",
  "address": "0x44f7a0",
  "source_from": "src/GameZRecoil/zClass/Window.c",
  "smoke_tests": [
    "zclass_window_new_smoke"
  ],
  "known_limits": [
    "No isolated VC COFF byte comparison is recorded for this constructor helper; tier S verification is deferred to a coherent zClass window/source-cluster pass."
  ],
  "functional_evidence": [
    "Binary Ninja assembly for 0x44f7a0 validates AllocNodeFromFreeList, null allocation reporting and null return, class id 3, calloc of 0xf8 window data bytes, default render width/height 1, buffer index -1, active renderer state capture, debug printf, bucket 14 insertion, DeleteNodeByType cleanup on insert failure, and node return.",
    "The native smoke verifies successful window allocation, class id and default data fields, active renderer framebuffer/width/height/bpp capture, bucket 14 membership, window property helpers, clear-polygon helper behavior, delete cleanup, and empty-free-list failure."
  ],
  "vc5_attempt": ""
}
