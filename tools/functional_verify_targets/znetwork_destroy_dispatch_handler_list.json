{
  "name": "znetwork_destroy_dispatch_handler_list",
  "description": "zNetwork::DestroyDispatchHandlerList unlinks and deletes every dispatch-handler list node, deletes the sentinel, and clears the global list pointer and count.",
  "address": "0x48bff0",
  "source_from": "src/GameZRecoil/zNetwork/znet_dplay.cpp",
  "smoke_tests": [
    "znetwork_dispatch_handler_list_smoke"
  ],
  "known_limits": [
    "Current VC5SP3 byte comparison is not accepted for tier S: 0x48bff0 has 62 unmasked mismatches after 32 relocation bytes and 11 trimmed VC5 NOPs, with BN size 111 and VC5 size 112. Owner-sized byte acceptance remains blocked by sibling zNetwork dispatch-handler list rows.",
    "The source keeps a defensive null-sentinel guard for native-smoke harness cleanup after repeated real atexit registrations; Binary Ninja shows the retail body assumes the sentinel is valid on entry."
  ],
  "functional_evidence": [
    "Binary Ninja disassembly for 0x48bff0 has no current warnings and validates traversal from g_zNetwork_DispatchHandlerListSentinel->next until the sentinel, unlinking each node through prev/next, deleting each node with operator delete, decrementing g_zNetwork_DispatchHandlerListCount for each removed node, deleting the sentinel, clearing g_zNetwork_DispatchHandlerListSentinel, and clearing the count.",
    "The dependency frontier for 0x48bff0 reports no source blockers; its only direct callee is the accepted operator delete provider boundary.",
    "Native smoke znetwork_dispatch_handler_list_smoke verifies non-empty shutdown through a manually linked node, checks that the global sentinel pointer and count are cleared, and separately covers the shared dispatch-list sentinel/count lifecycle used by this callback."
  ],
  "vc5_attempt": "python tools/recoil.py verify vc5 znetwork_destroy_dispatch_handler_list"
}
