{
  "name": "zvideo_fxpass3_element_draw",
  "description": "zVideoFxPass3Element::Draw dispatches the common draw-base slot, publishes parent pass-3 surface state, and invokes the element pass callback for configured input rectangles.",
  "address": "0x4bdb60",
  "source_from": "src/GameZRecoil/zVideo/zvid_main.c",
  "smoke_tests": [
    "zvideo_fxpass3_element_draw_smoke"
  ],
  "known_limits": [
    "tier S verification was not attempted in this session; defer to a coherent zVideo FxPass3 source-cluster pass."
  ],
  "functional_evidence": [
    "Binary Ninja assembly/decompilation for 0x4bdb60 validates the initial vtable +0x08 draw-base dispatch, parent-config capture from offset 0x08, optional zVideo::Fx_SetSurfaceState call from config offsets 0x18/0x1c/0x20/0x24, two input-rect iterations at config offsets 0x10 and 0x14, clipRectOrNull writes at element offset 0x34, vtable +0x74 pass callback dispatches, and final restoration to input rect 0.",
    "The native draw smoke verifies the no-parent callback path, the parent path with two input rectangles and surface-state publication, and the parent path where input rect 0 is null and input rect 1 still dispatches before final clipRectOrNull is restored to null."
  ],
  "vc5_attempt": ""
}
