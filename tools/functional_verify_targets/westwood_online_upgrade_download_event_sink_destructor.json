{
  "name": "westwood_online_upgrade_download_event_sink_destructor",
  "description": "WestwoodOnlineUpgradeDownloadEventSink::Destructor restores the vtable, prepares refcount teardown state, decrements the live sink count, and deletes the embedded critical section.",
  "address": "0x4427f0",
  "source_from": "src/Battlesport/WOL.cpp",
  "smoke_tests": [
    "westwood_online_upgrade_download_event_sink_destructor_smoke"
  ],
  "known_limits": [
    "Tier C only; no VC COFF byte-comparison target exists yet, so tier S remains open.",
    "The wider download event-sink vtable is still under reconstruction; this target verifies the destructor body directly."
  ],
  "functional_evidence": [
    "Binary Ninja disassembly for 0x4427f0 validates a thiscall destructor that installs g_WestwoodOnlineUpgradeDownloadEventSink_Vtbl, sets m_refCountAndLock.refCount to 1, calls imported InterlockedDecrement on g_WestwoodOnlineUpgradeApiInitState.eventSinkLiveCount, deletes the embedded critical section, and returns void.",
    "Native smoke westwood_online_upgrade_download_event_sink_destructor_smoke initializes the embedded critical section, calls the real destructor, then verifies vtable identity, teardown refcount 1, and the decremented live sink count."
  ],
  "vc5_attempt": ""
}
