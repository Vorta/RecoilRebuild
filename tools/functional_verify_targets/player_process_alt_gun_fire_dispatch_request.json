{
  "name": "player_process_alt_gun_fire_dispatch_request",
  "description": "Player::ProcessAltGunDispatchRequest selects the active alt fire slot, consumes dispatch requests, gates ammo, dispatches aux/continuous/discrete alt-gun fire paths, applies recoil/ammo side effects, and counts local dispatches.",
  "address": "0x43c190",
  "source_from": "src/Battlesport/player.cpp",
  "smoke_tests": [
    "player_process_alt_gun_fire_dispatch_request_smoke"
  ],
  "known_limits": [
    "Binary Ninja assembly/HLIL inspection in the current session verified the SelectAltGunFirePointAndSlot call, altGunFireHeldFlag early local counter exit, altGunDispatchRequested clear, ammo <= 0 local trigger-inactive warning gate, activeAltBankIndex aux-effect path, OptCatalog flag bit-1 continuous path, attachState launch-vs-simple selector, local force-feedback pulse scaled by damage/66, recoil slot offset 1.5 gate, infinite-ammo sentinel skip, ammo decrement/clamp, and local primaryGunDispatchCount increment.",
    "The native smoke covers successful local discrete dispatch through the simple projectile helper, dispatch-request consumption, active slot recoil write, ammo decrement, local dispatch count, and held-fire early local count without consuming the pending dispatch flag.",
    "No VC object-byte manifest covers 0x43c190 yet; tier S verification is deferred to the Player alt-gun/source-cluster pass."
  ],
  "vc5_attempt": "python tools/recoil.py verify vc5 0x43c190"
}
