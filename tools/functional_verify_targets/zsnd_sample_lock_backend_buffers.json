{
  "name": "zsnd_sample_lock_backend_buffers",
  "description": "zSndSample::LockBackendBuffers dispatches backend buffer lock calls for DirectSound and A3D.",
  "address": "0x4a34e0",
  "source_from": "src/GameZRecoil/zSound/zsnd_create.cpp",
  "smoke_tests": [
    "zsnd_sample_backend_buffer_lock_unlock_smoke",
    "zsnd_backend_error_reporters_smoke"
  ],
  "known_limits": [
    "VC5SP3 vc5_o2_ob1_gx_facs compiles the source but does not reproduce the original bytes for this wrapper; review build/vc5-verify/zsnd_sample_backend_buffer_lock_unlock/verify/4a34e0_byte_triage.txt before a tier S pass."
  ],
  "tier_s_evidence": [
    "Binary Ninja for 0x4a34e0 returns zero when createGuard is nonzero, dispatches active backend 0 and 1 to vtable slot 0x2c with the caller lock pointers and zero flags, reports nonzero DirectSound errors with source line 0x1ec, reports nonzero A3D errors with source line 0x1e3, and returns one only when the backend result is zero.",
    "The native smoke covers DirectSound and A3D lock success, DirectSound and A3D lock failure report paths, createGuard skip, and unsupported-backend fallback success."
  ],
  "vc5_attempt": "python tools/recoil.py verify vc5 zsnd_sample_backend_buffer_lock_unlock"
}
