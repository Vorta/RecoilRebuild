{
  "name": "zmath_vec3_direction_to",
  "description": "zMath::Vec3DirectionTo writes the normalized direction from one point to another and returns the original distance.",
  "address": "0x4729b0",
  "source_from": "src/GameZRecoil/zMath/zmth_main.c",
  "vc5_attempt": "python tools/recoil.py verify vc5 0x4729b0 --build-root build/vc5-verify-4729b0-final failed under vc5_o2_ob0_md_facs with 57 unmasked byte mismatches, 4 relocation bytes masked, 14 trailing VC NOPs trimmed, BN size 63, VC size 64; a four-profile sweep over vc5_o2_ob0_facs, vc5_o2_ob0_md_facs, vc5_o2_ob1_gx_facs, and vc5_o2_ob1_md_gx_facs also tied at 57.",
  "smoke_tests": [
    "zmath_vec3_direction_to_smoke"
  ],
  "known_limits": [
    "The implementation relies on the accepted zMath::Vec3Normalize dependency for zero-length behavior and returned distance.",
    "Tier S remains blocked: the 2026-06-05 source uses named dx/dy/dz delta locals before calling zMath::Vec3Normalize, but VC5SP3 still emits a frameless body instead of the retail EBP-frame saved-pointer setup and x87 x/y/z subtraction/store schedule."
  ],
  "functional_evidence": [
    "Binary Ninja assembly for 0x4729b0 validates ECX as the source point, EDX as the destination point, the output pointer as the stack argument, component subtraction as to - from, storing x/y/z to the output vector, calling zMath::Vec3Normalize on that output, returning the pre-normalization distance in st0, and callee cleanup of the output pointer.",
    "The native zMath smoke verifies a 3-4-5 direction normalizes to (0.6, 0.8, 0.0) with distance 5.0 and that identical endpoints produce zero distance with a zero output vector."
  ],
  "vc5_attempts": [
    "2026-06-05: vc5_o2_ob0_md_facs leaves 57 unmasked byte mismatches for the accepted dx/dy/dz local source shape (BN 63 bytes, VC 64 bytes, 4 relocation bytes masked, 14 trailing VC NOPs trimmed).",
    "2026-06-05: profile sweep over vc5_o2_ob0_facs, vc5_o2_ob0_md_facs, vc5_o2_ob1_gx_facs, and vc5_o2_ob1_md_gx_facs also ties at 57 unmasked byte mismatches.",
    "2026-06-05: a rejected reverse-local declaration probe reached 56 unmasked byte mismatches, but it was not accepted because it made source order less plausible while preserving the same frameless compiler shape."
  ]
}
