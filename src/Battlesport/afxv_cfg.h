#pragma once

// Canonical MFC _CUSTOM hook used only by the About declaration boundary.
// AFXVER_.H defines _AFX_ENABLE_INLINES before including this file; remove it
// so about.h can expose the required provider bodies at their recovered source
// positions.
#if defined(RECOIL_MFC42_DELAY_AFX_CORE_INLINES)
#undef _AFX_ENABLE_INLINES
#endif
