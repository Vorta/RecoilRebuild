/*
 * Ledger source placeholder for the zvid_ddd3d.c physical block.
 *
 * The preserved zVideo implementation is currently included through
 * zvid_main.c via zvid_impl.h. Splitting the D3D block from shared video
 * globals and helpers needs a follow-up source-order pass.
 */
