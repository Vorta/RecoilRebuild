/*
 * Ledger source placeholder for the zvid_dd.c physical block.
 *
 * The preserved zVideo implementation is currently included through
 * zvid_main.c via zvid_impl.h. Splitting the DirectDraw block from shared
 * video globals and helpers needs a follow-up source-order pass.
 */
