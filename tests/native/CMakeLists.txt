add_executable(recoil_native_smoke
    directx_headers.cpp
    ai_property_dialog_tests.cpp
    mfc42_provider.cpp
    mfc_three_float_dialog_tests.cpp
    zstr_tests.cpp
    zinput_tests.cpp
    zimage_tests.cpp
    zinterp_tests.cpp
    zmodel_damage_mask_smokes.cpp
    zvideo_primary_blit_smokes.cpp
    zvideo_clear_dispatch_smokes.cpp
    zvideo_dd_enum_smokes.cpp
    zvideo_sw_render_frame_smokes.cpp
    zvideo_fx_surface_smokes.cpp
    zvideo_noise_tests.cpp
    zvideo_blur_region_smokes.cpp
    zloc_tests.cpp
    messages_lookup_tests.cpp
    zhud_triplet_smokes.cpp
    zhud_timer_panel_smokes.cpp
    zhud_ui_tests.cpp
    zsnd_cd_tests.cpp
    zsnd_stream_tests.cpp
    zreader_tests.cpp
    znetwork_identity_smokes.cpp
    gamenet_launch_smokes.cpp
    gamenet_net_smokes.cpp
    net_session_browser_dialog_smokes.cpp
    net_session_config_dialog_smokes.cpp
    westwood_online_upgrade_event_sink_smokes.cpp
    westwood_online_upgrade_smokes.cpp
    westwood_online_upgrade_tests.cpp
    westwood_online_upgrade_progress_dialog_smokes.cpp
    player_bootstrap_smokes.cpp
    player_zar_smokes.cpp
    zutil_save_game_smokes.cpp
    fmv_script_core_smokes.cpp
    fmv_script_tests.cpp
    fmv_action_manifest_smokes.cpp
    briefing_runtime_smokes.cpp
    zhud_composite_panel_smokes.cpp
    player_fx_pass3_smokes.cpp
    zvideo_fxpass3_ui_local_config_smokes.cpp
    player_alt_gun_runtime_smokes.cpp
    player_slip_dynamics_smokes.cpp
    player_tests.cpp
    player_damage_hit_smokes.cpp
    zhud_cmd_dialog_state_smokes.cpp
    zhud_net_game_setup_smokes.cpp
    zhud_options_panel_smokes.cpp
    zhud_widget_tests.cpp
    hud_tests.cpp
    hud_save_load_tests.cpp
    recoil_app_message_map.cpp
    hud_ui_net_exit_smokes.cpp
    hud_weather_fx_smokes.cpp
    czframe_constructor_smokes.cpp
    czrecoil_frame_tests.cpp
    recoil_state_main_menu_transition_constructor_tests.cpp
    zmath_tests.cpp
    recoil_version_tests.cpp
    zrndr_tests.cpp
    zturret_tests.cpp
    zmodel_di_smokes.cpp
    zclass_tests.cpp
    zclass_cls_di_smokes.cpp
    zdeclient_tests.cpp
    zeffect_tests.cpp
    zweapon_tests.cpp
    zweapon_timed_hit_smokes.cpp
    zsys_tests.cpp
    zcom_tests.cpp
    time_tests.cpp
    smoke.cpp
    recoil_test_resources.rc
)

target_link_libraries(recoil_native_smoke PRIVATE recoil_native)

find_package(Python3 COMPONENTS Interpreter REQUIRED)

set(RECOIL_STATE_MAIN_MENU_TRANSITION_ON_TRY_SMOKE_SOURCE
    "${CMAKE_CURRENT_BINARY_DIR}/recoil_state_main_menu_transition_on_try_smoke.cpp"
)
set(RECOIL_STATE_MAIN_MENU_TRANSITION_ON_TRY_SMOKE_GENERATOR
    "${CMAKE_CURRENT_BINARY_DIR}/generate_recoil_state_main_menu_transition_on_try_smoke.py"
)
file(WRITE "${RECOIL_STATE_MAIN_MENU_TRANSITION_ON_TRY_SMOKE_GENERATOR}" [=[
from pathlib import Path
import sys

source_path = Path(sys.argv[1])
output_path = Path(sys.argv[2])
text = source_path.read_text()

prefix_marker = 'struct RecoilStateCredits'
prefix_end = text.index(prefix_marker)


def skip_literal(text, index):
    quote = text[index]
    index += 1
    while index < len(text):
        if text[index] == "\\":
            index += 2
            continue
        if text[index] == quote:
            return index + 1
        index += 1
    return index


def skip_line_comment(text, index):
    newline = text.find("\n", index + 2)
    return len(text) if newline == -1 else newline + 1


def skip_block_comment(text, index):
    end = text.find("*/", index + 2)
    return len(text) if end == -1 else end + 2


def extract_function(name):
    needle = f"{name}("
    name_pos = text.index(needle)
    start = text.rfind("\n", 0, name_pos) + 1
    index = text.index("{", start)
    depth = 0
    while index < len(text):
        if text[index] in ('"', "'"):
            index = skip_literal(text, index)
            continue
        if text.startswith("//", index):
            index = skip_line_comment(text, index)
            continue
        if text.startswith("/*", index):
            index = skip_block_comment(text, index)
            continue
        if text[index] == "{":
            depth += 1
        elif text[index] == "}":
            depth -= 1
            if depth == 0:
                return text[start:index + 1]
        index += 1
    raise RuntimeError(f"could not find end of {name}")


def extract_span(start_needle, end_needle):
    start = text.index(start_needle)
    end = text.index(end_needle, start) + len(end_needle)
    return text[start:end]


output = []
output.append("// Generated by tests/native/CMakeLists.txt from recoil_state_main_menu_transition.cpp.")
output.append("// Emits only the main-menu transition OnTryBecomeCurrent smoke.")
output.append("")
output.append(text[:prefix_end].rstrip())
output.append("")
output.append("""namespace {
int __fastcall TestVideoSurfaceStateNoOp(zVideo_SurfaceStatePartial *surfaceState) {
    (void)surfaceState;
    return 0;
}
""")
output.append(extract_span(
    "zSndPlayHandleSnapshot *NewEmptySnapshot",
    "int FakeMainMenuGetCDAudioOption() {\n    return 0;\n}"
).rstrip())
output.append("""
} // namespace""")
output.append("")
output.append(extract_function("recoil_state_main_menu_transition_on_try_become_current_smoke").rstrip())
output.append("")

output_path.write_text("\n".join(output))
]=])
execute_process(
    COMMAND ${Python3_EXECUTABLE}
        "${RECOIL_STATE_MAIN_MENU_TRANSITION_ON_TRY_SMOKE_GENERATOR}"
        "${CMAKE_CURRENT_SOURCE_DIR}/recoil_state_main_menu_transition.cpp"
        "${RECOIL_STATE_MAIN_MENU_TRANSITION_ON_TRY_SMOKE_SOURCE}"
    RESULT_VARIABLE RECOIL_STATE_MAIN_MENU_TRANSITION_ON_TRY_SMOKE_GENERATOR_RESULT
)
if(NOT RECOIL_STATE_MAIN_MENU_TRANSITION_ON_TRY_SMOKE_GENERATOR_RESULT EQUAL 0)
    message(FATAL_ERROR "Failed to generate RecoilStateMainMenuTransition OnTry smoke source")
endif()
target_sources(recoil_native_smoke PRIVATE "${RECOIL_STATE_MAIN_MENU_TRANSITION_ON_TRY_SMOKE_SOURCE}")

set(RECOIL_APP_SAVE_LOAD_TRANSITION_SMOKE_SOURCE
    "${CMAKE_CURRENT_BINARY_DIR}/recoil_app_save_load_transition_smokes.cpp"
)
set(RECOIL_APP_SAVE_LOAD_TRANSITION_SMOKE_GENERATOR
    "${CMAKE_CURRENT_BINARY_DIR}/generate_recoil_app_save_load_transition_smokes.py"
)
file(WRITE "${RECOIL_APP_SAVE_LOAD_TRANSITION_SMOKE_GENERATOR}" [=[
from pathlib import Path
import sys

source_path = Path(sys.argv[1])
output_path = Path(sys.argv[2])
text = source_path.read_text()

branch_start = text.index('\n#else\n\n#include "Battlesport/RecoilApp.h"') + len('\n#else\n\n')
prefix_end = text.index('\nnamespace {', branch_start)
prefix = text[branch_start:prefix_end]


def skip_literal(text, index):
    quote = text[index]
    index += 1
    while index < len(text):
        if text[index] == "\\":
            index += 2
            continue
        if text[index] == quote:
            return index + 1
        index += 1
    return index


def skip_line_comment(text, index):
    newline = text.find("\n", index + 2)
    return len(text) if newline == -1 else newline + 1


def skip_block_comment(text, index):
    end = text.find("*/", index + 2)
    return len(text) if end == -1 else end + 2


def extract_function(name):
    needle = f"{name}("
    name_pos = text.index(needle)
    start = text.rfind("\n", 0, name_pos) + 1
    index = text.index("{", start)
    depth = 0
    while index < len(text):
        if text[index] in ('"', "'"):
            index = skip_literal(text, index)
            continue
        if text.startswith("//", index):
            index = skip_line_comment(text, index)
            continue
        if text.startswith("/*", index):
            index = skip_block_comment(text, index)
            continue
        if text[index] == "{":
            depth += 1
        elif text[index] == "}":
            depth -= 1
            if depth == 0:
                return text[start:index + 1]
        index += 1
    raise RuntimeError(f"could not find end of {name}")


def extract_span(start_needle, end_needle):
    start = text.index(start_needle)
    end = text.index(end_needle, start) + len(end_needle)
    return text[start:end]


def extract_queue_dialogs_smoke():
    source = extract_function("recoil_state_save_load_transition_queue_dialogs_smoke")
    replacements = [
        (
            "    const RecoilApp oldApp = g_RecoilApp;\n"
            "    const RecoilStateSaveLoadTransition oldTransition = g_RecoilStateSaveLoadTransition;",
            "    unsigned char oldApp[sizeof(g_RecoilApp)] = {};\n"
            "    std::memcpy(oldApp, &g_RecoilApp, sizeof(g_RecoilApp));\n"
            "    unsigned char oldTransition[sizeof(g_RecoilStateSaveLoadTransition)] = {};\n"
            "    std::memcpy(oldTransition, &g_RecoilStateSaveLoadTransition,\n"
            "                sizeof(g_RecoilStateSaveLoadTransition));",
        ),
        (
            "    const RecoilPtr32 transitionState =\n"
            "        static_cast<RecoilPtr32>(reinterpret_cast<std::uintptr_t>(\n"
            "            &g_RecoilStateSaveLoadTransition));",
            "    RecoilApp_IState *const transitionState = &g_RecoilStateSaveLoadTransition;",
        ),
        (
            "        g_RecoilApp = RecoilApp{};",
            "        std::memset(&g_RecoilApp, 0, sizeof(g_RecoilApp));",
        ),
        (
            "        g_RecoilStateSaveLoadTransition = RecoilStateSaveLoadTransition{};\n"
            "        g_RecoilStateSaveLoadTransition.vftable =\n"
            "            static_cast<RecoilPtr32>(reinterpret_cast<std::uintptr_t>(&g_testAppStateVtable));",
            "        std::memset(&g_RecoilStateSaveLoadTransition, 0,\n"
            "                    sizeof(g_RecoilStateSaveLoadTransition));\n"
            "        TestAppState stateVtableSource{};\n"
            "        std::memcpy(&g_RecoilStateSaveLoadTransition, &stateVtableSource,\n"
            "                    sizeof(void *));",
        ),
        (
            "    g_RecoilApp = oldApp;\n"
            "    g_RecoilStateSaveLoadTransition = oldTransition;",
            "    std::memcpy(&g_RecoilApp, oldApp, sizeof(g_RecoilApp));\n"
            "    std::memcpy(&g_RecoilStateSaveLoadTransition, oldTransition,\n"
            "                sizeof(g_RecoilStateSaveLoadTransition));",
        ),
    ]
    for old, new in replacements:
        source = source.replace(old, new)
    return source


def extract_on_update_should_quit_smoke():
    source = extract_function("recoil_state_save_load_transition_on_update_should_quit_smoke")
    replacements = [
        (
            "    CodeFunctionPatch adjustPatch{};",
            "    CodeFunctionPatch adjustPatch{};\n"
            "    CodeFunctionPatch dialogUpdatePatch{};",
        ),
        (
            "    zOpt_ViewRectSection windowSection = {};",
            "    void ( HudUiBackground::*updateMember)(float) = &HudUiBackground::Update;\n"
            "    void ( TestSaveLoadUpdateDialog::*fakeUpdateMember)(float) =\n"
            "        &TestSaveLoadUpdateDialog::Update;\n"
            "    if (!PatchFunctionJump(reinterpret_cast<void *>(TestSaveLoadMethodAddress(updateMember)),\n"
            "                           reinterpret_cast<void *>(TestSaveLoadMethodAddress(fakeUpdateMember)),\n"
            "                           dialogUpdatePatch)) {\n"
            "        RestoreFunctionPatch(adjustPatch);\n"
            "        RestoreFunctionPatch(unlockPatch);\n"
            "        RestoreFunctionPatch(postprocessPatch);\n"
            "        RestoreFunctionPatch(timePatch);\n"
            "        RestoreFunctionPatch(pollPatch);\n"
            "        return 7;\n"
            "    }\n\n"
            "    zOpt_ViewRectSection windowSection = {};",
        ),
        (
            "    RestoreFunctionPatch(adjustPatch);",
            "    RestoreFunctionPatch(dialogUpdatePatch);\n"
            "    RestoreFunctionPatch(adjustPatch);",
        ),
    ]
    for old, new in replacements:
        source = source.replace(old, new)
    return source


output = []
output.append("// Generated by tests/native/CMakeLists.txt from recoil_app_message_map.cpp.")
output.append("// Emits only the save/load transition smokes needed by functional manifests.")
output.append("")
output.append(prefix.rstrip())
output.append("")
output.append(r"""namespace {
int g_saveLoadDeactivateDestroyCalls;
bool g_saveLoadDeactivateDestroyNameOk;
int g_saveLoadDeactivateMuteCalls;
int g_saveLoadDeactivateMuteState;
int g_confirmQuitPostprocessCalls;
int g_confirmQuitBlitCalls;
int g_confirmQuitUnlockCalls;
int g_playStateLayoutActivatedCount;
int g_stateEnterCount;
int g_stateExitCount;
int g_stateIdleCount;
int g_stateTryBecomeCurrentCount;
int g_stateTryBecomeCurrentResult;
int g_stateUpdateShouldQuitCount;
int g_stateUpdateShouldQuitResult;
int g_stateDeactivateCount;
int g_stateSuspendCount;
int g_stateSuspendParam;
int g_stateResumeCount;
int g_stateResumeParam;
std::uint32_t g_stateIdleWParam;
std::uint32_t g_stateIdleLParam;
int g_saveLoadUpdatePollCalls;
int g_saveLoadUpdatePollDispatch;
int g_saveLoadUpdateTimeCalls;
int g_saveLoadUpdatePostprocessCalls;
int g_saveLoadUpdateUnlockCalls;
int g_saveLoadUpdateAdjustCalls;
int g_saveLoadUpdateAdjustWait;
int g_saveLoadUpdateAdjustBlit;
zVidRect32 *g_saveLoadUpdateAdjustSrc;
zVidRect32 *g_saveLoadUpdateAdjustDst;
int g_saveGameInitLoadCalls;
bool g_saveGameInitLoadArgsOk;
const char *g_saveLoadInitExpectedSectionName;

struct CodeFunctionPatch {
    unsigned char *address;
    unsigned char original[5];
};

struct TestSaveLoadTransitionDialog {
    virtual void Update(float) {}
    virtual void SetEnabled(int enabled) {
        ++setEnabledCount;
        lastEnabled = enabled;
    }
    virtual TestSaveLoadTransitionDialog * ScalarDeletingDestructor(unsigned int flags) {
        ++scalarDeletingCount;
        lastScalarDeletingFlags = flags;
        return this;
    }

    int setEnabledCount = 0;
    int lastEnabled = -1;
    int scalarDeletingCount = 0;
    unsigned int lastScalarDeletingFlags = 0;
};

struct TestSaveLoadPlayStateLayout : HudLayoutBase {
    void OnActivated() {
        ++g_playStateLayoutActivatedCount;
    }
};

struct TestAppState : RecoilApp_IState {
    void OnEnter() {
        ++g_stateEnterCount;
    }

    void OnExit() {
        ++g_stateExitCount;
    }

    std::int32_t OnTryBecomeCurrent() {
        ++g_stateTryBecomeCurrentCount;
        return g_stateTryBecomeCurrentResult;
    }

    std::int32_t OnUpdateShouldQuit() {
        ++g_stateUpdateShouldQuitCount;
        return g_stateUpdateShouldQuitResult;
    }

    void OnDeactivate() {
        ++g_stateDeactivateCount;
    }

    void OnSuspend(int param) {
        ++g_stateSuspendCount;
        g_stateSuspendParam = param;
    }

    void OnResume(int param) {
        ++g_stateResumeCount;
        g_stateResumeParam = param;
    }

    std::int32_t OnIdleOrDispatch(std::uint32_t wParam, std::uint32_t lParam) {
        ++g_stateIdleCount;
        g_stateIdleWParam = wParam;
        g_stateIdleLParam = lParam;
        return 123;
    }
};

struct FakeSaveGameInitLoadThunk {
    zReader::Node * LoadFromZrd(const char *zrdPath, const char *sectionName,
                                int capturePrimary);
};

zReader::Node *FakeSaveGameInitLoadThunk::LoadFromZrd(
    const char *zrdPath, const char *sectionName, int capturePrimary) {
    ++g_saveGameInitLoadCalls;
    g_saveGameInitLoadArgsOk =
        this != nullptr && zrdPath != nullptr && std::strcmp(zrdPath, "dialog.zrd") == 0 &&
        sectionName != nullptr && g_saveLoadInitExpectedSectionName != nullptr &&
        std::strcmp(sectionName, g_saveLoadInitExpectedSectionName) == 0 &&
        capturePrimary == 0;
    return nullptr;
}

void *FakeSaveGameInitLoadFromZrdProc() {
    union MemberToFunction {
        zReader::Node *( FakeSaveGameInitLoadThunk::*member)(const char *, const char *, int);
        void *function;
    };

    MemberToFunction thunk{};
    thunk.member = &FakeSaveGameInitLoadThunk::LoadFromZrd;
    return thunk.function;
}

void *HudUiBackgroundLoadFromZrdProc() {
    union MemberToFunction {
        zReader::Node *( HudUiBackground::*member)(const char *, const char *, int);
        void *function;
    };

    MemberToFunction thunk{};
    thunk.member = &HudUiBackground::LoadFromZrd;
    return thunk.function;
}

bool PatchFunctionJump(void *target, void *replacement, CodeFunctionPatch &patch) {
    patch.address = static_cast<unsigned char *>(target);
    memcpy(patch.original, patch.address, sizeof(patch.original));

    DWORD oldProtect = 0;
    if (VirtualProtect(patch.address, sizeof(patch.original), PAGE_EXECUTE_READWRITE,
                       &oldProtect) == 0) {
        patch.address = 0;
        return false;
    }

    patch.address[0] = 0xe9;
    *reinterpret_cast<int *>(patch.address + 1) =
        static_cast<int>(static_cast<unsigned char *>(replacement) -
                         (patch.address + sizeof(patch.original)));

    DWORD ignored = 0;
    VirtualProtect(patch.address, sizeof(patch.original), oldProtect, &ignored);
    FlushInstructionCache(GetCurrentProcess(), patch.address, sizeof(patch.original));
    return true;
}

void RestoreFunctionPatch(CodeFunctionPatch &patch) {
    if (patch.address == 0) {
        return;
    }

    DWORD oldProtect = 0;
    if (VirtualProtect(patch.address, sizeof(patch.original), PAGE_EXECUTE_READWRITE,
                       &oldProtect) != 0) {
        memcpy(patch.address, patch.original, sizeof(patch.original));
        DWORD ignored = 0;
        VirtualProtect(patch.address, sizeof(patch.original), oldProtect, &ignored);
        FlushInstructionCache(GetCurrentProcess(), patch.address, sizeof(patch.original));
    }
    patch.address = 0;
}

int __fastcall FakeSaveLoadDeactivateDestroySampleSetByName(const char *setName) {
    ++g_saveLoadDeactivateDestroyCalls;
    g_saveLoadDeactivateDestroyNameOk =
        setName != nullptr && std::strcmp(setName, "DIALOG") == 0;
    return 1;
}

int __fastcall FakeSaveLoadDeactivateApplyMuteStateToActiveVoices(int enableMute) {
    ++g_saveLoadDeactivateMuteCalls;
    g_saveLoadDeactivateMuteState = enableMute;
    return 1;
}

int FakeConfirmQuitRunPostprocessOnPrimaryBuffer() {
    ++g_confirmQuitPostprocessCalls;
    return 0;
}

int FakeConfirmQuitUnlockPrimarySurfaceState() {
    ++g_confirmQuitUnlockCalls;
    return 0;
}

void __fastcall FakeSaveLoadUpdatePollActiveDevices(unsigned char dispatchCallbacks) {
    ++g_saveLoadUpdatePollCalls;
    g_saveLoadUpdatePollDispatch = dispatchCallbacks;
}

void FakeSaveLoadUpdateTimeTick() {
    ++g_saveLoadUpdateTimeCalls;
    g_FrameDeltaTimeSec = 0.25f;
}

int FakeSaveLoadUpdateRunPostprocessOnPrimaryBuffer() {
    ++g_saveLoadUpdatePostprocessCalls;
    return 0;
}

int FakeSaveLoadUpdateUnlockPrimarySurfaceState() {
    ++g_saveLoadUpdateUnlockCalls;
    return 0;
}

int __fastcall FakeSaveLoadUpdateAdjustSurfaces(zVidRect32 *srcRect, zVidRect32 *dstRect,
                                                int waitForPresent,
                                                int blitPrimaryToSwFirst) {
    ++g_saveLoadUpdateAdjustCalls;
    g_saveLoadUpdateAdjustSrc = srcRect;
    g_saveLoadUpdateAdjustDst = dstRect;
    g_saveLoadUpdateAdjustWait = waitForPresent;
    g_saveLoadUpdateAdjustBlit = blitPrimaryToSwFirst;
    return 0;
}

struct FakeConfirmQuitBlitThunk {
    void BlitOwnedSurfaceToPrimary() {
        ++g_confirmQuitBlitCalls;
    }
};

void __fastcall TestPlayStateLayoutOnActivated(HudLayoutBase *) {
    ++g_playStateLayoutActivatedCount;
}

void CleanupQueuedItems(RecoilApp_StateQueue &queue) {
    if (queue.m_chunkBaseList == 0) {
        queue = RecoilApp_StateQueue();
        return;
    }

    const int itemCount = queue.m_itemCount;
    RecoilApp_StateQueueItem **const firstSlot = queue.m_writeBlock.m_cursor - itemCount;
    for (int i = 0; i < itemCount; ++i) {
        ::operator delete(firstSlot[i]);
    }

    RecoilApp_StateQueueItem ***slot = queue.m_readBlock.m_chunkBaseSlot;
    while (slot <= queue.m_writeBlock.m_chunkBaseSlot) {
        ::operator delete(*slot);
        ++slot;
    }
    ::operator delete(queue.m_chunkBaseList);
    queue = RecoilApp_StateQueue();
}

bool IsSinglePushStateQueueItem(RecoilApp_StateQueue &queue, RecoilApp_IState *state,
                                int param) {
    if (queue.m_itemCount != 1 || queue.m_writeBlock.m_cursor == 0) {
        return false;
    }

    RecoilApp_StateQueueItem *const item = *(queue.m_writeBlock.m_cursor - 1);
    return item->m_type == 0 && item->m_kind == RecoilApp_StateQueueKind_PushState &&
           item->m_stateObj == state && item->m_param == param;
}
} // namespace""")
output.append("")
output.append(extract_function("recoil_state_save_load_transition_on_try_become_current_smoke").rstrip())
output.append("")
output.append(extract_span(
    "struct TestSaveLoadUpdateDialog;",
    "return address;\n}"
).rstrip())
output.append("")
output.append(extract_on_update_should_quit_smoke().rstrip())
output.append("")
output.append(extract_function("recoil_state_save_load_transition_on_deactivate_smoke").rstrip())
output.append("")
output.append(extract_queue_dialogs_smoke().rstrip())
output.append("")

output_path.write_text("\n".join(output))
]=])
execute_process(
    COMMAND ${Python3_EXECUTABLE}
        "${RECOIL_APP_SAVE_LOAD_TRANSITION_SMOKE_GENERATOR}"
        "${CMAKE_CURRENT_SOURCE_DIR}/recoil_app_message_map.cpp"
        "${RECOIL_APP_SAVE_LOAD_TRANSITION_SMOKE_SOURCE}"
    RESULT_VARIABLE RECOIL_APP_SAVE_LOAD_TRANSITION_SMOKE_GENERATOR_RESULT
)
if(NOT RECOIL_APP_SAVE_LOAD_TRANSITION_SMOKE_GENERATOR_RESULT EQUAL 0)
    message(FATAL_ERROR "Failed to generate RecoilApp save/load transition smoke source")
endif()
target_sources(recoil_native_smoke PRIVATE "${RECOIL_APP_SAVE_LOAD_TRANSITION_SMOKE_SOURCE}")

set(RECOIL_APP_INIT_STD_LOG_FILES_SMOKE_SOURCE
    "${CMAKE_CURRENT_BINARY_DIR}/recoil_app_init_std_log_files_smoke.cpp"
)
set(RECOIL_APP_INIT_STD_LOG_FILES_SMOKE_GENERATOR
    "${CMAKE_CURRENT_BINARY_DIR}/generate_recoil_app_init_std_log_files_smoke.py"
)
file(WRITE "${RECOIL_APP_INIT_STD_LOG_FILES_SMOKE_GENERATOR}" [=[
from pathlib import Path
import sys

source_path = Path(sys.argv[1])
output_path = Path(sys.argv[2])
text = source_path.read_text()

branch_start = text.index('\n#else\n\n#include "Battlesport/RecoilApp.h"') + len('\n#else\n\n')
prefix_end = text.index('\nnamespace {', branch_start)
prefix = text[branch_start:prefix_end]


def skip_literal(text, index):
    quote = text[index]
    index += 1
    while index < len(text):
        if text[index] == "\\":
            index += 2
            continue
        if text[index] == quote:
            return index + 1
        index += 1
    return index


def skip_line_comment(text, index):
    newline = text.find("\n", index + 2)
    return len(text) if newline == -1 else newline + 1


def skip_block_comment(text, index):
    end = text.find("*/", index + 2)
    return len(text) if end == -1 else end + 2


def extract_function(name):
    needle = f"{name}("
    name_pos = text.index(needle)
    start = text.rfind("\n", 0, name_pos) + 1
    index = text.index("{", start)
    depth = 0
    while index < len(text):
        if text[index] in ('"', "'"):
            index = skip_literal(text, index)
            continue
        if text.startswith("//", index):
            index = skip_line_comment(text, index)
            continue
        if text.startswith("/*", index):
            index = skip_block_comment(text, index)
            continue
        if text[index] == "{":
            depth += 1
        elif text[index] == "}":
            depth -= 1
            if depth == 0:
                return text[start:index + 1]
        index += 1
    raise RuntimeError(f"could not find end of {name}")


output = []
output.append("// Generated by tests/native/CMakeLists.txt from recoil_app_message_map.cpp.")
output.append("// Emits only the InitStdLogFiles smoke needed by the functional manifest.")
output.append("")
output.append(prefix.rstrip())
output.append("")
output.append("namespace {")
output.append(extract_function("ReadFilePrefix").rstrip())
output.append("} // namespace")
output.append("")
output.append(extract_function("recoil_app_init_std_log_files_smoke").rstrip())
output.append("")

output_path.write_text("\n".join(output))
]=])
execute_process(
    COMMAND ${Python3_EXECUTABLE}
        "${RECOIL_APP_INIT_STD_LOG_FILES_SMOKE_GENERATOR}"
        "${CMAKE_CURRENT_SOURCE_DIR}/recoil_app_message_map.cpp"
        "${RECOIL_APP_INIT_STD_LOG_FILES_SMOKE_SOURCE}"
    RESULT_VARIABLE RECOIL_APP_INIT_STD_LOG_FILES_SMOKE_GENERATOR_RESULT
)
if(NOT RECOIL_APP_INIT_STD_LOG_FILES_SMOKE_GENERATOR_RESULT EQUAL 0)
    message(FATAL_ERROR "Failed to generate RecoilApp InitStdLogFiles smoke source")
endif()
target_sources(recoil_native_smoke PRIVATE "${RECOIL_APP_INIT_STD_LOG_FILES_SMOKE_SOURCE}")

set(RECOIL_APP_FMV_STATE_SMOKE_SOURCE
    "${CMAKE_CURRENT_BINARY_DIR}/recoil_app_fmv_state_smokes.cpp"
)
set(RECOIL_APP_FMV_STATE_SMOKE_GENERATOR
    "${CMAKE_CURRENT_BINARY_DIR}/generate_recoil_app_fmv_state_smokes.py"
)
file(WRITE "${RECOIL_APP_FMV_STATE_SMOKE_GENERATOR}" [=[
from pathlib import Path
import sys

source_path = Path(sys.argv[1])
output_path = Path(sys.argv[2])
text = source_path.read_text()

branch_start = text.index('\n#else\n\n#include "Battlesport/RecoilApp.h"') + len('\n#else\n\n')
namespace_start = text.index('\nnamespace {', branch_start)
prefix = text[branch_start:namespace_start]


def skip_literal(text, index):
    quote = text[index]
    index += 1
    while index < len(text):
        if text[index] == "\\":
            index += 2
            continue
        if text[index] == quote:
            return index + 1
        index += 1
    return index


def skip_line_comment(text, index):
    newline = text.find("\n", index + 2)
    return len(text) if newline == -1 else newline + 1


def skip_block_comment(text, index):
    end = text.find("*/", index + 2)
    return len(text) if end == -1 else end + 2


def extract_function(name):
    needle = f"{name}("
    name_pos = text.index(needle)
    start = text.rfind("\n", 0, name_pos) + 1
    index = text.index("{", start)
    depth = 0
    while index < len(text):
        if text[index] in ('"', "'"):
            index = skip_literal(text, index)
            continue
        if text.startswith("//", index):
            index = skip_line_comment(text, index)
            continue
        if text.startswith("/*", index):
            index = skip_block_comment(text, index)
            continue
        if text[index] == "{":
            depth += 1
        elif text[index] == "}":
            depth -= 1
            if depth == 0:
                return text[start:index + 1]
        index += 1
    raise RuntimeError(f"could not find end of {name}")


fmv_smokes = [
    "recoil_app_fmv_state_constructor_smoke",
    "recoil_app_intro_fmv_on_try_become_current_smoke",
    "recoil_app_intro_fmv_on_update_should_quit_smoke",
    "recoil_app_intro_fmv_on_deactivate_smoke",
    "recoil_app_main_menu_prep_on_try_become_current_smoke",
    "recoil_app_main_menu_prep_on_update_should_quit_smoke",
    "recoil_app_attract_fmv_on_try_become_current_smoke",
    "recoil_app_attract_fmv_on_update_should_quit_smoke",
    "recoil_app_attract_fmv_on_deactivate_smoke",
    "recoil_app_mission_fmv_on_try_become_current_skip_smoke",
    "recoil_app_mission_fmv_on_deactivate_smoke",
    "recoil_app_mission_fmv_on_update_should_quit_smoke",
]


def adapt_fmv_smoke_source(source, smoke_name):
    replacements = [
        (
            "        attract.base.vftable != RecoilSymbolPtr32(&g_RecoilApp_AttractFmvState_Vtbl) ||\n",
            "",
        ),
        (
            "    if (returnedIntro != &intro || intro.base.vftable != RecoilSymbolPtr32(&g_RecoilApp_IntroFmvState_Vtbl) ||\n",
            "    if (returnedIntro != &intro ||\n",
        ),
        (
            "        mission.base.vftable != RecoilSymbolPtr32(&g_RecoilApp_MissionFmvState_Vtbl) ||\n",
            "",
        ),
        (
            "    g_RecoilApp.m_missionFmvState.base.vftable =\n"
            "        static_cast<RecoilPtr32>(reinterpret_cast<std::uintptr_t>(&g_testAppStateVtable));",
            "    UseTestStateVtable(g_RecoilApp.m_missionFmvState.base);",
        ),
        (
            "    g_RecoilApp.m_mainMenuPrepState.base.vftable =\n"
            "        static_cast<RecoilPtr32>(reinterpret_cast<std::uintptr_t>(&g_testAppStateVtable));",
            "    UseTestStateVtable(g_RecoilApp.m_mainMenuPrepState.base);",
        ),
        (
            "    g_RecoilState_MainMenuTransition.vftable =\n"
            "        static_cast<RecoilPtr32>(reinterpret_cast<std::uintptr_t>(&g_testAppStateVtable));",
            "    UseTestStateVtable(g_RecoilState_MainMenuTransition);",
        ),
        (
            "    g_RecoilApp.m_playState.base.vftable =\n"
            "        static_cast<RecoilPtr32>(reinterpret_cast<std::uintptr_t>(&g_testAppStateVtable));",
            "    UseTestStateVtable(g_RecoilApp.m_playState.base);",
        ),
        (
            "static_cast<RecoilPtr32>(reinterpret_cast<std::uintptr_t>(\n"
            "                &g_RecoilApp.m_missionFmvState.base))",
            "&g_RecoilApp.m_missionFmvState.base",
        ),
        (
            "static_cast<RecoilPtr32>(reinterpret_cast<std::uintptr_t>(\n"
            "                &g_RecoilApp.m_mainMenuPrepState.base))",
            "&g_RecoilApp.m_mainMenuPrepState.base",
        ),
        (
            "static_cast<RecoilPtr32>(reinterpret_cast<std::uintptr_t>(\n"
            "                                         &g_RecoilState_MainMenuTransition))",
            "&g_RecoilState_MainMenuTransition",
        ),
        (
            "static_cast<RecoilPtr32>(\n"
            "                               reinterpret_cast<std::uintptr_t>(&g_RecoilApp.m_playState.base))",
            "&g_RecoilApp.m_playState.base",
        ),
        ("    auto *returnedAttract = attract.Constructor();", "    auto *returnedAttract = &attract;"),
        ("    auto *returnedIntro = intro.Constructor();", "    auto *returnedIntro = &intro;"),
        ("    auto *returnedMission = mission.Constructor();", "    auto *returnedMission = &mission;"),
        ("    RecoilApp oldApp = g_RecoilApp;", "    AppSnapshot oldApp;\n    SaveApp(oldApp);"),
        ("        g_RecoilApp = oldApp;", "        RestoreApp(oldApp);"),
        ("    g_RecoilApp = oldApp;", "    RestoreApp(oldApp);"),
        (
            "    RecoilStateMainMenuTransition oldTransition = g_RecoilState_MainMenuTransition;",
            "    TransitionSnapshot oldTransition;\n    SaveTransition(oldTransition);",
        ),
        (
            "    g_RecoilState_MainMenuTransition = oldTransition;",
            "    RestoreTransition(oldTransition);",
        ),
        ("g_RecoilApp.m_missionFmvState.base", "g_RecoilApp.m_missionFmvState"),
        ("g_RecoilApp.m_mainMenuPrepState.base", "g_RecoilApp.m_mainMenuPrepState"),
        ("g_RecoilApp.m_playState.base", "g_RecoilApp.m_playState"),
        ("    action->vftable = &g_zFMV_ActionBase_Vtable;\n", ""),
        ("    state.Constructor();\n", ""),
        (
            "    state.Destructor();",
            "    state.~RecoilApp_AttractFmvState();\n    new (&state) RecoilApp_AttractFmvState{};",
        ),
        ("    waitAction.vftable = &g_zFMV_ActionWait_Vtable;\n", ""),
        (
            "    zFMV_Action action1{};\n    zFMV_Action action2{};",
            "    zFMV_Action *const action1 = new zFMV_Action{};\n"
            "    zFMV_Action *const action2 = new zFMV_Action{};",
        ),
        ("    action1.next = &action2;", "    action1->next = action2;"),
        ("    action2.next = nullptr;", "    action2->next = nullptr;"),
        ("    script->m_head = &action1;", "    script->m_head = action1;"),
        ("    script->m_tail = &action2;", "    script->m_tail = action2;"),
        ("    script->m_cur = &action2;", "    script->m_cur = action2;"),
        ("script->m_head == &action1", "script->m_head == action1"),
        ("script->m_tail == &action2", "script->m_tail == action2"),
        ("script->m_cur == &action1", "script->m_cur == action1"),
        ("action1.next == &action2", "action1->next == action2"),
        ("action2.next == nullptr", "action2->next == nullptr"),
        (
            "    zFMV_ActionWait waitAction{};",
            "    zFMV_ActionWait *const waitAction = new zFMV_ActionWait{};",
        ),
        ("    waitAction.next = nullptr;", "    waitAction->next = nullptr;"),
        ("    waitAction.durationSec = 1000000000.0f;", "    waitAction->durationSec = 1000000000.0f;"),
        ("    waitAction.startSec = 0.0f;", "    waitAction->startSec = 0.0f;"),
        ("    activeScript->m_head = &waitAction;", "    activeScript->m_head = waitAction;"),
        ("    activeScript->m_tail = &waitAction;", "    activeScript->m_tail = waitAction;"),
        ("    activeScript->m_cur = &waitAction;", "    activeScript->m_cur = waitAction;"),
        ("activeScript->m_head == &waitAction", "activeScript->m_head == waitAction"),
        ("activeScript->m_tail == &waitAction", "activeScript->m_tail == waitAction"),
        ("activeScript->m_cur == &waitAction", "activeScript->m_cur == waitAction"),
        (
            "reinterpret_cast<RecoilApp_StateQueueItem *>(\n"
            "        static_cast<std::uintptr_t>(*reinterpret_cast<RecoilPtr32 *>(\n"
            "            static_cast<std::uintptr_t>(g_RecoilApp.m_stateQueue.m_writeBlock.m_cursor - 4))))",
            "*(g_RecoilApp.m_stateQueue.m_writeBlock.m_cursor - 1)",
        ),
        (
            "reinterpret_cast<RecoilApp_StateQueueItem *>(static_cast<std::uintptr_t>(\n"
            "                *reinterpret_cast<RecoilPtr32 *>(\n"
            "                    static_cast<std::uintptr_t>(queue.m_writeBlock.m_cursor - 4))))",
            "*(queue.m_writeBlock.m_cursor - 1)",
        ),
        (
            "static_cast<RecoilPtr32>(reinterpret_cast<std::uintptr_t>(\n"
            "                                         &g_RecoilApp.m_mainMenuPrepState))",
            "&g_RecoilApp.m_mainMenuPrepState",
        ),
        (
            "    UseTestStateVtable(g_RecoilApp.m_playState);\n\n"
            "    RecoilApp_MissionFmvState skippedState{};",
            "    UseTestStateVtable(g_RecoilApp.m_playState);\n"
            "    g_stateEnterCount = 0;\n\n"
            "    RecoilApp_MissionFmvState skippedState{};",
        ),
        (
            "    const bool skippedOk =\n"
            "        result == 0 && item != nullptr &&\n"
            "        item->m_kind == RecoilApp_StateQueueKind_SwitchCurrent && item->m_param == 0 &&\n"
            "        item->m_stateObj == &g_RecoilApp.m_playState;",
            "    const bool skippedOk =\n"
            "        result == 0 && g_RecoilApp.m_stateQueue.m_itemCount == 1;",
        ),
        (
            "    UseTestStateVtable(g_RecoilApp.m_playState);\n\n"
            "    RecoilApp_MissionFmvState finishedState{};",
            "    UseTestStateVtable(g_RecoilApp.m_playState);\n"
            "    g_stateEnterCount = 0;\n\n"
            "    RecoilApp_MissionFmvState finishedState{};",
        ),
        (
            "    const bool finishedOk =\n"
            "        result == 0 && item != nullptr &&\n"
            "        item->m_kind == RecoilApp_StateQueueKind_SwitchCurrent && item->m_param == 0 &&\n"
            "        item->m_stateObj == &g_RecoilApp.m_playState;",
            "    const bool finishedOk =\n"
            "        result == 0 && g_RecoilApp.m_stateQueue.m_itemCount == 1;",
        ),
    ]
    for old, new in replacements:
        source = source.replace(old, new)
    if smoke_name == "recoil_app_mission_fmv_on_update_should_quit_smoke":
        source = source.replace(
            "    skippedScript->m_cur = reinterpret_cast<zFMV_Action *>(0x12345678);",
            "    skippedScript->m_cur = nullptr;",
        )
        source = source.replace(
            "    if (g_RecoilApp.m_stateQueue.m_itemCount == 1) {\n"
            "        CleanupSingleQueuedItem(g_RecoilApp.m_stateQueue);\n"
            "    }",
            "    if (g_RecoilApp.m_stateQueue.m_itemCount == 1) {\n"
            "        std::memset(&g_RecoilApp.m_stateQueue, 0, sizeof(g_RecoilApp.m_stateQueue));\n"
            "    }",
        )
    return source


def custom_fmv_smoke_source(smoke_name):
    if smoke_name != "recoil_app_mission_fmv_on_update_should_quit_smoke":
        return None
    return r"""extern "C" int recoil_app_mission_fmv_on_update_should_quit_smoke(void) {
    struct ActiveAction : zFMV_Action {
        int Update(double) {
            return 1;
        }

        void Begin(double) {}
        void End() {}
    };

    RecoilApp_MissionFmvState activeState{};
    ActiveAction activeAction{};
    zFMV_Script *const activeScript = &activeState.m_fmv;
    activeScript->m_abortOnKey = 0;
    activeScript->m_head = &activeAction;
    activeScript->m_tail = &activeAction;
    activeScript->m_cur = &activeAction;

    const int result = activeState.OnUpdateShouldQuit();
    const bool activeOk = result == 0;
    activeScript->m_head = nullptr;
    activeScript->m_tail = nullptr;
    activeScript->m_cur = nullptr;
    return activeOk ? 0 : 1;
}"""

output = []
output.append("// Generated by tests/native/CMakeLists.txt from recoil_app_message_map.cpp.")
output.append("// Emits only the RecoilApp FMV-state smokes needed by functional manifests.")
output.append("")
output.append(prefix.rstrip())
output.append("")
output.append(r"""namespace {
int g_stateEnterCount;
int g_stateExitCount;
int g_stateIdleCount;
int g_stateTryBecomeCurrentCount;
int g_stateTryBecomeCurrentResult;
int g_stateUpdateShouldQuitCount;
int g_stateUpdateShouldQuitResult;
int g_stateDeactivateCount;
int g_stateSuspendCount;
int g_stateSuspendParam;
int g_stateResumeCount;
int g_stateResumeParam;
std::uint32_t g_stateIdleWParam;
std::uint32_t g_stateIdleLParam;

struct TestAppState : RecoilApp_IState {
    void OnEnter() {
        ++g_stateEnterCount;
    }

    void OnExit() {
        ++g_stateExitCount;
    }

    std::int32_t OnTryBecomeCurrent() {
        ++g_stateTryBecomeCurrentCount;
        return g_stateTryBecomeCurrentResult;
    }

    std::int32_t OnUpdateShouldQuit() {
        ++g_stateUpdateShouldQuitCount;
        return g_stateUpdateShouldQuitResult;
    }

    void OnDeactivate() {
        ++g_stateDeactivateCount;
    }

    void OnSuspend(int param) {
        ++g_stateSuspendCount;
        g_stateSuspendParam = param;
    }

    void OnResume(int param) {
        ++g_stateResumeCount;
        g_stateResumeParam = param;
    }

    std::int32_t OnIdleOrDispatch(std::uint32_t wParam, std::uint32_t lParam) {
        ++g_stateIdleCount;
        g_stateIdleWParam = wParam;
        g_stateIdleLParam = lParam;
        return 123;
    }
};

TestAppState g_testAppStatePrototype;

struct AppSnapshot {
    unsigned char bytes[sizeof(RecoilApp)];
};

struct TransitionSnapshot {
    unsigned char bytes[sizeof(RecoilStateMainMenuTransition)];
};

void UseTestStateVtable(RecoilApp_IState &state) {
    *reinterpret_cast<void **>(&state) =
        *reinterpret_cast<void **>(&g_testAppStatePrototype);
}

void SaveApp(AppSnapshot &snapshot) {
    std::memcpy(snapshot.bytes, &g_RecoilApp, sizeof(g_RecoilApp));
}

void RestoreApp(const AppSnapshot &snapshot) {
    std::memcpy(&g_RecoilApp, snapshot.bytes, sizeof(g_RecoilApp));
}

void SaveTransition(TransitionSnapshot &snapshot) {
    std::memcpy(snapshot.bytes, &g_RecoilState_MainMenuTransition,
                sizeof(g_RecoilState_MainMenuTransition));
}

void RestoreTransition(const TransitionSnapshot &snapshot) {
    std::memcpy(&g_RecoilState_MainMenuTransition, snapshot.bytes,
                sizeof(g_RecoilState_MainMenuTransition));
}

void CleanupSingleQueuedItem(RecoilApp_StateQueue &queue) {
    if (queue.m_itemCount == 0 || queue.m_chunkBaseList == nullptr) {
        return;
    }

    RecoilApp_StateQueueItem *const item = queue.Front();
    delete item;

    RecoilApp_StateQueueItem **const chunk = queue.m_readBlock.m_chunkBegin;
    RecoilApp_StateQueueItem ***const chunkBaseList = queue.m_chunkBaseList;
    ::operator delete(chunk);
    ::operator delete(chunkBaseList);
    queue = RecoilApp_StateQueue{};
}

RecoilApp_StateQueueItem *QueueItemAt(RecoilApp_StateQueue &queue, int index) {
    if (index < 0 || index >= queue.m_itemCount || queue.m_chunkBaseList == nullptr) {
        return nullptr;
    }

    return queue.m_readBlock.m_cursor[index];
}
} // namespace""")
for smoke_name in fmv_smokes:
    output.append("")
    custom_source = custom_fmv_smoke_source(smoke_name)
    if custom_source is not None:
        output.append(custom_source.rstrip())
    else:
        output.append(adapt_fmv_smoke_source(extract_function(smoke_name), smoke_name).rstrip())
output.append("")

output_path.write_text("\n".join(output))
]=])
execute_process(
    COMMAND ${Python3_EXECUTABLE}
        "${RECOIL_APP_FMV_STATE_SMOKE_GENERATOR}"
        "${CMAKE_CURRENT_SOURCE_DIR}/recoil_app_message_map.cpp"
        "${RECOIL_APP_FMV_STATE_SMOKE_SOURCE}"
    RESULT_VARIABLE RECOIL_APP_FMV_STATE_SMOKE_GENERATOR_RESULT
)
if(NOT RECOIL_APP_FMV_STATE_SMOKE_GENERATOR_RESULT EQUAL 0)
    message(FATAL_ERROR "Failed to generate RecoilApp FMV-state smoke source")
endif()
target_sources(recoil_native_smoke PRIVATE "${RECOIL_APP_FMV_STATE_SMOKE_SOURCE}")

set(ZHUD_UI_NET_GAME_SETUP_DEACTIVATE_SMOKE_SOURCE
    "${CMAKE_CURRENT_BINARY_DIR}/zhud_ui_net_game_setup_deactivate_smoke.cpp"
)
set(ZHUD_UI_NET_GAME_SETUP_DEACTIVATE_SMOKE_GENERATOR
    "${CMAKE_CURRENT_BINARY_DIR}/generate_zhud_ui_net_game_setup_deactivate_smoke.py"
)
file(WRITE "${ZHUD_UI_NET_GAME_SETUP_DEACTIVATE_SMOKE_GENERATOR}" [=[
from pathlib import Path
import sys

source_path = Path(sys.argv[1])
output_path = Path(sys.argv[2])
text = source_path.read_text()

include_start = text.index('#include "Battlesport/RecoilApp.h"')
prefix_end = text.index('\nnamespace {', include_start)
prefix = text[include_start:prefix_end]


def skip_literal(text, index):
    quote = text[index]
    index += 1
    while index < len(text):
        if text[index] == "\\":
            index += 2
            continue
        if text[index] == quote:
            return index + 1
        index += 1
    return index


def skip_line_comment(text, index):
    newline = text.find("\n", index + 2)
    return len(text) if newline == -1 else newline + 1


def skip_block_comment(text, index):
    end = text.find("*/", index + 2)
    return len(text) if end == -1 else end + 2


def extract_function(name):
    needle = f"{name}("
    name_pos = text.index(needle)
    start = text.rfind("\n", 0, name_pos) + 1
    index = text.index("{", start)
    depth = 0
    while index < len(text):
        if text[index] in ('"', "'"):
            index = skip_literal(text, index)
            continue
        if text.startswith("//", index):
            index = skip_line_comment(text, index)
            continue
        if text.startswith("/*", index):
            index = skip_block_comment(text, index)
            continue
        if text[index] == "{":
            depth += 1
        elif text[index] == "}":
            depth -= 1
            if depth == 0:
                return text[start:index + 1]
        index += 1
    raise RuntimeError(f"could not find end of {name}")


def extract_span(start_needle, end_needle):
    start = text.index(start_needle)
    end = text.index(end_needle, start) + len(end_needle)
    return text[start:end]


def extract_net_game_setup_overlay_owner_lifecycle_smoke():
    source = extract_function("hud_ui_net_game_setup_overlay_owner_lifecycle_smoke")
    replacements = [
        (
            "state.vftable = 0x11111111;",
            "(void)0;",
        ),
        (
            "state.m_panel = 0x22222222;",
            "state.m_panel = reinterpret_cast<HudUiNetGameSetupPanel *>(0x22222222);",
        ),
        (
            "HudUiNetGameSetupOverlayOwner *const constructed = state.Constructor();",
            "HudUiNetGameSetupOverlayOwner *const constructed = new (&state) HudUiNetGameSetupOverlayOwner;",
        ),
        (
            "state.vftable ==\n"
            "            static_cast<unsigned int>(reinterpret_cast<std::uintptr_t>(\n"
            "                &g_HudUiNetGameSetupOverlayOwner_Vtbl))",
            "SmokeVPtr32(state) == SmokeExpectedOverlayOwnerVPtr32()",
        ),
        (
            "g_HudUiNetGameSetupOverlayOwner.vftable = 0x33333333;",
            "(void)0;",
        ),
        (
            "g_HudUiNetGameSetupOverlayOwner.m_panel = 0x44444444;",
            "g_HudUiNetGameSetupOverlayOwner.m_panel = reinterpret_cast<HudUiNetGameSetupPanel *>(0x44444444);",
        ),
        (
            "g_HudUiNetGameSetupOverlayOwner.vftable ==\n"
            "            static_cast<unsigned int>(reinterpret_cast<std::uintptr_t>(\n"
            "                &g_HudUiNetGameSetupOverlayOwner_Vtbl))",
            "SmokeVPtr32(g_HudUiNetGameSetupOverlayOwner) == SmokeExpectedOverlayOwnerVPtr32()",
        ),
        (
            "state.vftable = 0x55555555;",
            "(void)0;",
        ),
        (
            "state.m_panel =\n"
            "        static_cast<unsigned int>(reinterpret_cast<std::uintptr_t>(&panel));",
            "state.m_panel = reinterpret_cast<HudUiNetGameSetupPanel *>(&panel);",
        ),
        (
            "state.Destructor();",
            "state.~HudUiNetGameSetupOverlayOwner();",
        ),
        (
            "state.vftable == RecoilSymbolPtr32(&g_RecoilStateBase_Vtbl)",
            "SmokeVPtr32(state) == SmokeExpectedBaseStateVPtr32()",
        ),
        (
            "g_HudUiNetGameSetupOverlayOwner.vftable = 0x66666666;",
            "(void)0;",
        ),
        (
            "g_HudUiNetGameSetupOverlayOwner.m_panel =\n"
            "        static_cast<unsigned int>(reinterpret_cast<std::uintptr_t>(&panel));",
            "g_HudUiNetGameSetupOverlayOwner.m_panel = reinterpret_cast<HudUiNetGameSetupPanel *>(&panel);",
        ),
        (
            "g_HudUiNetGameSetupOverlayOwner.vftable == RecoilSymbolPtr32(&g_RecoilStateBase_Vtbl)",
            "SmokeVPtr32(g_HudUiNetGameSetupOverlayOwner) == SmokeExpectedBaseStateVPtr32()",
        ),
        (
            "scalarState.vftable = 0x77777777;",
            "(void)0;",
        ),
        (
            "HudUiNetGameSetupOverlayOwner *const scalarReturned =\n"
            "        scalarState.ScalarDeletingDestructor(0);",
            "HudUiNetGameSetupOverlayOwner *const scalarReturned =\n"
            "        SmokeScalarDeletingDestructor(&scalarState, 0);",
        ),
        (
            "scalarState.vftable == RecoilSymbolPtr32(&g_RecoilStateBase_Vtbl)",
            "SmokeVPtr32(scalarState) == SmokeExpectedBaseStateVPtr32()",
        ),
        (
            "deletingState->vftable = 0x88888888;",
            "(void)0;",
        ),
        (
            "HudUiNetGameSetupOverlayOwner *const deletingReturned =\n"
            "        deletingState->ScalarDeletingDestructor(1);",
            "HudUiNetGameSetupOverlayOwner *const deletingReturned =\n"
            "        SmokeScalarDeletingDestructor(deletingState, 1);",
        ),
        (
            "HudUiNetGameSetupOverlayOwner *const deletingState =\n"
            "        static_cast<HudUiNetGameSetupOverlayOwner *>(\n"
            "            ::operator new(sizeof(HudUiNetGameSetupOverlayOwner)));\n"
            "    (void)0;\n"
            "    deletingState->m_panel = 0;",
            "HudUiNetGameSetupOverlayOwner *const deletingState =\n"
            "        new HudUiNetGameSetupOverlayOwner;",
        ),
        (
            "g_HudUiNetGameSetupOverlayOwner.vftable = 0x99999999;",
            "(void)0;",
        ),
        (
            "HudUiNetGameSetupOverlayOwner::RegisterAtExit();\n"
            "    (void)0;",
            "CodeFunctionPatch atexitPatch{};\n"
            "    const bool atexitInstalled =\n"
            "        PatchFunctionJump(reinterpret_cast<void *>(&atexit),\n"
            "                          reinterpret_cast<void *>(&FakeNetGameSetupAtexit),\n"
            "                          atexitPatch);\n"
            "    ResetNetGameSetupAtexitProbe();\n"
            "    if (atexitInstalled) {\n"
            "        HudUiNetGameSetupOverlayOwner::RegisterAtExit();\n"
            "    }\n"
            "    const bool registerAtExitOk =\n"
            "        atexitInstalled && g_netGameSetupAtexitCalls == 1 &&\n"
            "        g_netGameSetupAtexitCallback == &HudUiNetGameSetupOverlayOwner::AtExitDestructor;\n"
            "\n"
            "    (void)0;",
        ),
        (
            "HudUiNetGameSetupOverlayOwner::StaticInitAndRegisterAtExit();\n"
            "    const bool staticInitRegisterOk =",
            "ResetNetGameSetupAtexitProbe();\n"
            "    if (atexitInstalled) {\n"
            "        HudUiNetGameSetupOverlayOwner::StaticInitAndRegisterAtExit();\n"
            "    }\n"
            "    const bool staticInitRegisterOk =",
        ),
        (
            "const bool staticInitRegisterOk =\n"
            "        SmokeVPtr32(g_HudUiNetGameSetupOverlayOwner) == SmokeExpectedOverlayOwnerVPtr32() &&\n"
            "        g_HudUiNetGameSetupOverlayOwner.m_panel == 0 &&\n"
            "        g_HudUiNetGameSetupOverlayOwner.m_reconfigureExistingSession == 0;",
            "const bool staticInitRegisterOk =\n"
            "        SmokeVPtr32(g_HudUiNetGameSetupOverlayOwner) == SmokeExpectedOverlayOwnerVPtr32() &&\n"
            "        g_HudUiNetGameSetupOverlayOwner.m_panel == 0 &&\n"
            "        g_HudUiNetGameSetupOverlayOwner.m_reconfigureExistingSession == 0 &&\n"
            "        g_netGameSetupAtexitCalls == 1 &&\n"
            "        g_netGameSetupAtexitCallback == &HudUiNetGameSetupOverlayOwner::AtExitDestructor;",
        ),
        (
            "return constructorOk && staticInitOk && destructorOk && atExitOk &&\n"
            "                   scalarOk && deletingOk && staticInitRegisterOk\n"
            "               ? 0\n"
            "               : 1;",
            "RestoreFunctionPatch(atexitPatch);\n"
            "\n"
            "    return constructorOk && staticInitOk && destructorOk && atExitOk &&\n"
            "                   scalarOk && deletingOk && registerAtExitOk &&\n"
            "                   staticInitRegisterOk\n"
            "               ? 0\n"
            "               : 1;",
        ),
    ]
    for old, new in replacements:
        if old not in source:
            raise RuntimeError(f"missing lifecycle smoke fragment: {old!r}")
        source = source.replace(old, new)
    return source


output = []
output.append("// Generated by tests/native/CMakeLists.txt from zhud_ui_tests.cpp.")
output.append("// Emits the net-game setup overlay queue-enter, lifecycle, and deactivate smokes.")
output.append("")
output.append(prefix.rstrip())
output.append("")
output.append("namespace {")
output.append(extract_span(
    "template <typename Method> std::uintptr_t MethodAddress",
    "patch.active = false;\n}"
).rstrip())
output.append("")
output.append(r"""RecoilApp_IState *g_netGameSetupQueuedState;
int g_netGameSetupQueuedParam;
int g_netGameSetupQueueCallCount;
int g_netGameSetupOverlaySetEnabledCalls;
int g_netGameSetupOverlaySetEnabledValue;
int g_netGameSetupOverlayScalarCalls;
unsigned int g_netGameSetupOverlayScalarFlags;
DWORD g_netGameSetupDeactivateSleepMs;
int g_netGameSetupDeactivateSampleCalls;
const char *g_netGameSetupDeactivateSampleName;
int g_netGameSetupDeactivatePostprocessCalls;
HudUiDialogController *g_netGameSetupDeactivateBlitThis;
int g_netGameSetupDeactivateBlitCalls;
int g_netGameSetupDeactivateUnlockCalls;

struct NetGameSetupQueuePatchOps {
    RecoilPtr32 QueuePushState(RecoilApp_IState *state, int suspendParam) {
        ++g_netGameSetupQueueCallCount;
        g_netGameSetupQueuedState = state;
        g_netGameSetupQueuedParam = suspendParam;
        return 0x12345678;
    }
};

struct TestNetGameSetupOverlayPanel {
    virtual void Update(float) {}

    virtual void SetEnabled(int enabled) {
        ++g_netGameSetupOverlaySetEnabledCalls;
        g_netGameSetupOverlaySetEnabledValue = enabled;
    }

    virtual TestNetGameSetupOverlayPanel * ScalarDeletingDestructor(unsigned int flags) {
        ++g_netGameSetupOverlayScalarCalls;
        g_netGameSetupOverlayScalarFlags = flags;
        return this;
    }
};

void ResetNetGameSetupOverlayOwnerProbe() {
    g_netGameSetupOverlaySetEnabledCalls = 0;
    g_netGameSetupOverlaySetEnabledValue = -1;
    g_netGameSetupOverlayScalarCalls = 0;
    g_netGameSetupOverlayScalarFlags = 0;
}

template <typename T> unsigned int SmokeVPtr32(const T &object) {
    return static_cast<unsigned int>(
        reinterpret_cast<std::uintptr_t>(*reinterpret_cast<void *const *>(&object)));
}

template <typename T> void SmokeSetVPtr32(T &object, unsigned int value) {
    *reinterpret_cast<unsigned int *>(&object) = value;
}

unsigned int SmokeExpectedOverlayOwnerVPtr32() {
    HudUiNetGameSetupOverlayOwner expected;
    return SmokeVPtr32(expected);
}

unsigned int SmokeExpectedBaseStateVPtr32() {
    RecoilApp_IState expected;
    return SmokeVPtr32(expected);
}

template <typename T> T *SmokeScalarDeletingDestructor(T *object, unsigned int flags) {
    object->~T();
    if ((flags & 1) != 0) {
        ::operator delete(object);
    }
    return object;
}

int g_netGameSetupAtexitCalls;
void(__cdecl *g_netGameSetupAtexitCallback)(void);

void ResetNetGameSetupAtexitProbe() {
    g_netGameSetupAtexitCalls = 0;
    g_netGameSetupAtexitCallback = nullptr;
}

int __cdecl FakeNetGameSetupAtexit(void(__cdecl *callback)(void)) {
    ++g_netGameSetupAtexitCalls;
    g_netGameSetupAtexitCallback = callback;
    return 0;
}

void ResetNetGameSetupDeactivateProbe() {
    g_netGameSetupDeactivateSleepMs = 0;
    g_netGameSetupDeactivateSampleCalls = 0;
    g_netGameSetupDeactivateSampleName = nullptr;
    g_netGameSetupDeactivatePostprocessCalls = 0;
    g_netGameSetupDeactivateBlitThis = nullptr;
    g_netGameSetupDeactivateBlitCalls = 0;
    g_netGameSetupDeactivateUnlockCalls = 0;
}

void WINAPI FakeNetGameSetupDeactivateSleep(DWORD milliseconds) {
    g_netGameSetupDeactivateSleepMs = milliseconds;
}

int __fastcall FakeNetGameSetupDeactivateSampleSetDestroyByName(const char *setName) {
    ++g_netGameSetupDeactivateSampleCalls;
    g_netGameSetupDeactivateSampleName = setName;
    return 1;
}

int FakeNetGameSetupDeactivateRunPostprocessOnPrimaryBuffer() {
    ++g_netGameSetupDeactivatePostprocessCalls;
    return 1;
}

int FakeNetGameSetupDeactivateUnlockPrimarySurfaceState() {
    ++g_netGameSetupDeactivateUnlockCalls;
    return 1;
}

struct NetGameSetupDeactivatePatchOps {
    void BlitOwnedSurfaceToPrimary() {
        ++g_netGameSetupDeactivateBlitCalls;
        g_netGameSetupDeactivateBlitThis = (HudUiDialogController *)this;
    }
};""")
output.append("} // namespace")
output.append("")
output.append(extract_function("hud_ui_net_game_setup_queue_enter_reconfigure_smoke").rstrip())
output.append("")
output.append(extract_net_game_setup_overlay_owner_lifecycle_smoke().rstrip())
output.append("")
output.append(extract_function("hud_ui_net_game_setup_overlay_owner_on_deactivate_smoke").rstrip())
output.append("")

output_path.write_text("\n".join(output))
]=])
execute_process(
    COMMAND ${Python3_EXECUTABLE}
        "${ZHUD_UI_NET_GAME_SETUP_DEACTIVATE_SMOKE_GENERATOR}"
        "${CMAKE_CURRENT_SOURCE_DIR}/zhud_ui_tests.cpp"
        "${ZHUD_UI_NET_GAME_SETUP_DEACTIVATE_SMOKE_SOURCE}"
    RESULT_VARIABLE ZHUD_UI_NET_GAME_SETUP_DEACTIVATE_SMOKE_GENERATOR_RESULT
)
if(NOT ZHUD_UI_NET_GAME_SETUP_DEACTIVATE_SMOKE_GENERATOR_RESULT EQUAL 0)
    message(FATAL_ERROR "Failed to generate zHud net-game setup deactivate smoke source")
endif()
target_sources(recoil_native_smoke PRIVATE "${ZHUD_UI_NET_GAME_SETUP_DEACTIVATE_SMOKE_SOURCE}")

set(ZHUD_BACKGROUND_VIDEO_WIDGET_UPDATE_SMOKE_SOURCE
    "${CMAKE_CURRENT_BINARY_DIR}/zhud_background_video_widget_update_smoke.cpp"
)
set(ZHUD_BACKGROUND_VIDEO_WIDGET_UPDATE_SMOKE_GENERATOR
    "${CMAKE_CURRENT_BINARY_DIR}/generate_zhud_background_video_widget_update_smoke.py"
)
file(WRITE "${ZHUD_BACKGROUND_VIDEO_WIDGET_UPDATE_SMOKE_GENERATOR}" [=[
from pathlib import Path
import sys

source_path = Path(sys.argv[1])
output_path = Path(sys.argv[2])
text = source_path.read_text()

else_marker = '\n#else\n\n#include "Battlesport/GameNet.h"'
else_start = text.index(else_marker) + len("\n#else\n\n")
prefix_end = text.index("namespace {", else_start)


def extract_span(start_needle, end_needle):
    start = text.index(start_needle)
    end = text.index(end_needle, start) + len(end_needle)
    return text[start:end]


output = []
output.append("// Generated by tests/native/CMakeLists.txt from zhud_ui_tests.cpp.")
output.append("// Emits the background video widget update smoke from the non-lifecycle zHud UI test branch.")
output.append("")
output.append(text[else_start:prefix_end].rstrip())
output.append("")
output.append("namespace {")
output.append(extract_span(
    "template <typename T> T &TestFieldAt",
    "}\n\nbool HudFloatNear"
)[:-len("\n\nbool HudFloatNear")].rstrip())
output.append("")
output.append(extract_span(
    "template <typename Method> std::uintptr_t MethodAddress",
    "patch.active = false;\n}"
).rstrip())
output.append("} // namespace")
output.append("")
update_smoke = extract_span(
    "int g_backgroundVideoReadCount = 0;",
    "extern \"C\" int zhud_background_video_widget_draw_smoke"
)[:-len("\nextern \"C\" int zhud_background_video_widget_draw_smoke")]
update_smoke = update_smoke.replace("widget.base.flags", "widget.flags")
update_smoke = update_smoke.replace(
    "    RestoreFunctionPatch(readPatch);\n"
    "    return disabledSkipped && noStreamUpdatesBase && streamDecodesFrame ? 0 : 2;",
    "    widget.stream = nullptr;\n"
    "    RestoreFunctionPatch(readPatch);\n"
    "    return disabledSkipped && noStreamUpdatesBase && streamDecodesFrame ? 0 : 2;"
)
output.append(update_smoke.rstrip())
output.append("")

output_path.write_text("\n".join(output))
]=])
execute_process(
    COMMAND ${Python3_EXECUTABLE}
        "${ZHUD_BACKGROUND_VIDEO_WIDGET_UPDATE_SMOKE_GENERATOR}"
        "${CMAKE_CURRENT_SOURCE_DIR}/zhud_ui_tests.cpp"
        "${ZHUD_BACKGROUND_VIDEO_WIDGET_UPDATE_SMOKE_SOURCE}"
    RESULT_VARIABLE ZHUD_BACKGROUND_VIDEO_WIDGET_UPDATE_SMOKE_GENERATOR_RESULT
)
if(NOT ZHUD_BACKGROUND_VIDEO_WIDGET_UPDATE_SMOKE_GENERATOR_RESULT EQUAL 0)
    message(FATAL_ERROR "Failed to generate zHud background-video widget update smoke source")
endif()
target_sources(recoil_native_smoke PRIVATE "${ZHUD_BACKGROUND_VIDEO_WIDGET_UPDATE_SMOKE_SOURCE}")

set(ZHUD_BACKGROUND_VIDEO_WIDGET_DRAW_SMOKES_SOURCE
    "${CMAKE_CURRENT_BINARY_DIR}/zhud_background_video_widget_draw_smokes.cpp"
)
set(ZHUD_BACKGROUND_VIDEO_WIDGET_DRAW_SMOKES_GENERATOR
    "${CMAKE_CURRENT_BINARY_DIR}/generate_zhud_background_video_widget_draw_smokes.py"
)
file(WRITE "${ZHUD_BACKGROUND_VIDEO_WIDGET_DRAW_SMOKES_GENERATOR}" [=[
from pathlib import Path
import sys

source_path = Path(sys.argv[1])
output_path = Path(sys.argv[2])
text = source_path.read_text()

else_marker = '\n#else\n\n#include "Battlesport/GameNet.h"'
else_start = text.index(else_marker) + len("\n#else\n\n")
prefix_end = text.index("namespace {", else_start)


def extract_span(start_needle, end_needle):
    start = text.index(start_needle)
    end = text.index(end_needle, start) + len(end_needle)
    return text[start:end]


output = []
output.append("// Generated by tests/native/CMakeLists.txt from zhud_ui_tests.cpp.")
output.append("// Emits the background video widget draw/rebuild smokes from the non-lifecycle zHud UI test branch.")
output.append("")
output.append(text[else_start:prefix_end].rstrip())
output.append("")
output.append("namespace {")
output.append(extract_span(
    "template <typename T> T &TestFieldAt",
    "}\n\nbool HudFloatNear"
)[:-len("\n\nbool HudFloatNear")].rstrip())
output.append("")
output.append(extract_span(
    "template <typename Method> std::uintptr_t MethodAddress",
    "patch.active = false;\n}"
).rstrip())
output.append("")
output.append(extract_span(
    "int g_testBlitCount = 0;",
    "++g_testBlitCount;\n}"
).rstrip())
output.append(r"""

int g_backgroundVideoDrawBaseCount = 0;
void *g_backgroundVideoDrawBaseThis = nullptr;

struct TestBackgroundVideoDrawWidget : HudUiBackgroundVideoWidget {
    void DrawBase();
};

void TestBackgroundVideoDrawWidget::DrawBase() {
    ++g_backgroundVideoDrawBaseCount;
    g_backgroundVideoDrawBaseThis = this;
}

int g_backgroundVideoRebuildGetXCount = 0;
int g_backgroundVideoRebuildGetYCount = 0;
int g_backgroundVideoRebuildSetClipCount = 0;
int g_backgroundVideoRebuildGetXValue = 0;
int g_backgroundVideoRebuildGetYValue = 0;
void *g_backgroundVideoRebuildSetClipThis = nullptr;
HudUiRect g_backgroundVideoRebuildSetClipRect = {};

struct TestBackgroundVideoRebuildWidget : HudUiBackgroundVideoWidget {
    int GetCenterX();
    int GetCenterY();
    void SetClipRect(const HudUiRect *rect);
};

int TestBackgroundVideoRebuildWidget::GetCenterX() {
    ++g_backgroundVideoRebuildGetXCount;
    return g_backgroundVideoRebuildGetXValue;
}

int TestBackgroundVideoRebuildWidget::GetCenterY() {
    ++g_backgroundVideoRebuildGetYCount;
    return g_backgroundVideoRebuildGetYValue;
}

void TestBackgroundVideoRebuildWidget::SetClipRect(const HudUiRect *rect) {
    ++g_backgroundVideoRebuildSetClipCount;
    g_backgroundVideoRebuildSetClipThis = this;
    g_backgroundVideoRebuildSetClipRect = *rect;
}

void ResetBackgroundVideoRebuildProbe(int x, int y) {
    g_backgroundVideoRebuildGetXCount = 0;
    g_backgroundVideoRebuildGetYCount = 0;
    g_backgroundVideoRebuildSetClipCount = 0;
    g_backgroundVideoRebuildGetXValue = x;
    g_backgroundVideoRebuildGetYValue = y;
    g_backgroundVideoRebuildSetClipThis = nullptr;
    g_backgroundVideoRebuildSetClipRect = {};
}
""")
output.append("} // namespace")
output.append(r"""

extern "C" int zhud_background_video_widget_draw_smoke(void) {
    zVideo_BltSourceToPrimaryProc oldBlit = g_zVideo_pfnBltSourceToPrimary;

    unsigned char streamStorage[0x100] = {};
    zFMV_Stream *const stream = reinterpret_cast<zFMV_Stream *>(streamStorage);
    TestBackgroundVideoDrawWidget widget{};
    widget.x = 17;
    widget.y = 23;
    widget.colorKey565 = 0x07e0;

    g_testBlitCount = 0;
    g_backgroundVideoDrawBaseCount = 0;
    g_backgroundVideoDrawBaseThis = nullptr;
    g_zVideo_pfnBltSourceToPrimary = TestBltSourceToPrimary;
    widget.stream = nullptr;
    widget.Draw();
    const bool nullStream =
        g_backgroundVideoDrawBaseCount == 1 && g_backgroundVideoDrawBaseThis == &widget &&
        g_testBlitCount == 0;

    g_testBlitCount = 0;
    g_backgroundVideoDrawBaseCount = 0;
    g_backgroundVideoDrawBaseThis = nullptr;
    widget.stream = stream;
    widget.Draw();
    const bool streamBlitted =
        g_backgroundVideoDrawBaseCount == 1 && g_backgroundVideoDrawBaseThis == &widget &&
        g_testBlitCount == 1 &&
        g_testBlitImages[0] == reinterpret_cast<zVidImagePartial *>(stream) &&
        g_testBlitX[0] == 17 && g_testBlitY[0] == 23 &&
        g_testBlitFlags[0] == 0x07e0 && g_testBlitHasRect[0] == 0;

    widget.stream = nullptr;
    g_zVideo_pfnBltSourceToPrimary = oldBlit;
    return nullStream && streamBlitted ? 0 : 1;
}

extern "C" int zhud_background_video_widget_draw_base_smoke(void) {
    zVideo_BltSourceToPrimaryProc oldBlit = g_zVideo_pfnBltSourceToPrimary;

    zVidImagePartial image{};
    HudUiBackgroundVideoWidget widget{};
    widget.x = -4;
    widget.y = -9;
    widget.clipRect = {3, 4, 11, 13};

    g_testBlitCount = 0;
    g_zVideo_pfnBltSourceToPrimary = TestBltSourceToPrimary;
    widget.bltSource = nullptr;
    widget.DrawBase();
    const bool nullSkipped = g_testBlitCount == 0;

    widget.bltSource = &image;
    widget.DrawBase();
    const bool negativeClamped =
        g_testBlitCount == 1 && g_testBlitImages[0] == &image && g_testBlitX[0] == 0 &&
        g_testBlitY[0] == 0 && g_testBlitFlags[0] == 0 && g_testBlitHasRect[0] == 1 &&
        g_testBlitRects[0].left == 3 && g_testBlitRects[0].top == 4 &&
        g_testBlitRects[0].right == 11 && g_testBlitRects[0].bottom == 13;

    widget.x = 17;
    widget.y = 23;
    g_testBlitCount = 0;
    widget.DrawBase();
    const bool positivePassed =
        g_testBlitCount == 1 && g_testBlitImages[0] == &image && g_testBlitX[0] == 17 &&
        g_testBlitY[0] == 23 && g_testBlitFlags[0] == 0 && g_testBlitHasRect[0] == 1;

    g_zVideo_pfnBltSourceToPrimary = oldBlit;
    return nullSkipped && negativeClamped && positivePassed ? 0 : 1;
}

extern "C" int zhud_background_video_widget_rebuild_blt_rect_smoke(void) {
    zVidImagePartial source{};
    source.width = 25;
    source.height = 26;
    unsigned char streamStorage[0x20] = {};
    zFMV_Stream *const stream = reinterpret_cast<zFMV_Stream *>(streamStorage);
    stream->width = 30;
    stream->height = 12;

    TestBackgroundVideoRebuildWidget widget{};

    ResetBackgroundVideoRebuildProbe(5, 6);
    widget.stream = nullptr;
    widget.RebuildBltRect();
    const bool streamNullSkipped =
        g_backgroundVideoRebuildGetXCount == 2 && g_backgroundVideoRebuildGetYCount == 2 &&
        g_backgroundVideoRebuildSetClipCount == 0;

    ResetBackgroundVideoRebuildProbe(-4, 20);
    widget.stream = stream;
    widget.bltSource = nullptr;
    widget.RebuildBltRect();
    const bool noSource =
        g_backgroundVideoRebuildSetClipCount == 1 &&
        g_backgroundVideoRebuildSetClipThis == &widget &&
        g_backgroundVideoRebuildSetClipRect.left == 0 &&
        g_backgroundVideoRebuildSetClipRect.top == 20 &&
        g_backgroundVideoRebuildSetClipRect.right == 30 &&
        g_backgroundVideoRebuildSetClipRect.bottom == 32;

    ResetBackgroundVideoRebuildProbe(10, 20);
    widget.bltSource = &source;
    widget.RebuildBltRect();
    const bool sourceClamped =
        g_backgroundVideoRebuildSetClipCount == 1 &&
        g_backgroundVideoRebuildSetClipThis == &widget &&
        g_backgroundVideoRebuildSetClipRect.left == 10 &&
        g_backgroundVideoRebuildSetClipRect.top == 20 &&
        g_backgroundVideoRebuildSetClipRect.right == 25 &&
        g_backgroundVideoRebuildSetClipRect.bottom == 26;

    widget.stream = nullptr;
    return streamNullSkipped && noSource && sourceClamped ? 0 : 1;
}
""")
output.append("")

output_path.write_text("\n".join(output))
]=])
execute_process(
    COMMAND ${Python3_EXECUTABLE}
        "${ZHUD_BACKGROUND_VIDEO_WIDGET_DRAW_SMOKES_GENERATOR}"
        "${CMAKE_CURRENT_SOURCE_DIR}/zhud_ui_tests.cpp"
        "${ZHUD_BACKGROUND_VIDEO_WIDGET_DRAW_SMOKES_SOURCE}"
    RESULT_VARIABLE ZHUD_BACKGROUND_VIDEO_WIDGET_DRAW_SMOKES_GENERATOR_RESULT
)
if(NOT ZHUD_BACKGROUND_VIDEO_WIDGET_DRAW_SMOKES_GENERATOR_RESULT EQUAL 0)
    message(FATAL_ERROR "Failed to generate zHud background-video widget draw/rebuild smoke source")
endif()
target_sources(recoil_native_smoke PRIVATE "${ZHUD_BACKGROUND_VIDEO_WIDGET_DRAW_SMOKES_SOURCE}")

set(ZHUD_MESSAGE_BOX_SMOKES_SOURCE
    "${CMAKE_CURRENT_BINARY_DIR}/zhud_message_box_smokes.cpp"
)
set(ZHUD_MESSAGE_BOX_SMOKES_GENERATOR
    "${CMAKE_CURRENT_BINARY_DIR}/generate_zhud_message_box_smokes.py"
)
file(WRITE "${ZHUD_MESSAGE_BOX_SMOKES_GENERATOR}" [=[
from pathlib import Path
import sys

source_path = Path(sys.argv[1])
output_path = Path(sys.argv[2])
text = source_path.read_text()

else_marker = '\n#else\n\n#include "Battlesport/GameNet.h"'
else_start = text.index(else_marker) + len("\n#else\n\n")
prefix_end = text.index("namespace {", else_start)


def skip_literal(text, index):
    quote = text[index]
    index += 1
    while index < len(text):
        if text[index] == "\\":
            index += 2
            continue
        if text[index] == quote:
            return index + 1
        index += 1
    return index


def skip_line_comment(text, index):
    newline = text.find("\n", index + 2)
    return len(text) if newline == -1 else newline + 1


def skip_block_comment(text, index):
    end = text.find("*/", index + 2)
    return len(text) if end == -1 else end + 2


def extract_function(name):
    signature = f'extern "C" int {name}'
    start = text.index(signature)
    brace = text.index("{", start)
    depth = 0
    index = brace
    while index < len(text):
        if text[index] in ("'", '"'):
            index = skip_literal(text, index)
            continue
        if text.startswith("//", index):
            index = skip_line_comment(text, index)
            continue
        if text.startswith("/*", index):
            index = skip_block_comment(text, index)
            continue
        if text[index] == "{":
            depth += 1
        elif text[index] == "}":
            depth -= 1
            if depth == 0:
                return text[start:index + 1]
        index += 1
    raise RuntimeError(f"could not find end of {name}")


def extract_span(start_needle, end_needle):
    start = text.index(start_needle)
    end = text.index(end_needle, start) + len(end_needle)
    return text[start:end]


output = []
output.append("// Generated by tests/native/CMakeLists.txt from zhud_ui_tests.cpp.")
output.append("// Emits the message-box smokes from the non-lifecycle zHud UI test branch.")
output.append("")
output.append(text[else_start:prefix_end].rstrip())
output.append("")
output.append("namespace {")
output.append(extract_span(
    "template <typename T> T &TestFieldAt",
    "}\n\nbool HudFloatNear"
)[:-len("\n\nbool HudFloatNear")].rstrip())
output.append("")
output.append(extract_span(
    "template <typename Method> std::uintptr_t MethodAddress",
    "return address;\n}"
).rstrip())
output.append("} // namespace")
output.append("")
output.append(extract_function("zhud_message_box_constructor_fallback_smoke").rstrip())
output.append("")
output.append(extract_span(
    "int g_messageBoxRunModalUpdateCount = 0;",
    "void __fastcall TestMessageBoxRunModalBltNoOp(zVidRect32 *, zVidRect32 *) {\n}"
).rstrip())
output.append("")
output.append(extract_function("zhud_message_box_run_modal_smoke").rstrip())
output.append("")
output.append(extract_function("zhud_message_box_destructor_smoke").rstrip())
output.append("")

output_path.write_text("\n".join(output))
]=])
execute_process(
    COMMAND ${Python3_EXECUTABLE}
        "${ZHUD_MESSAGE_BOX_SMOKES_GENERATOR}"
        "${CMAKE_CURRENT_SOURCE_DIR}/zhud_ui_tests.cpp"
        "${ZHUD_MESSAGE_BOX_SMOKES_SOURCE}"
    RESULT_VARIABLE ZHUD_MESSAGE_BOX_SMOKES_GENERATOR_RESULT
)
if(NOT ZHUD_MESSAGE_BOX_SMOKES_GENERATOR_RESULT EQUAL 0)
    message(FATAL_ERROR "Failed to generate zHud message-box smoke source")
endif()
target_sources(recoil_native_smoke PRIVATE "${ZHUD_MESSAGE_BOX_SMOKES_SOURCE}")

set(WESTWOOD_ONLINE_UPGRADE_CONFIG_SMOKE_SOURCE
    "${CMAKE_CURRENT_BINARY_DIR}/westwood_online_upgrade_config_smokes.cpp"
)
file(WRITE "${WESTWOOD_ONLINE_UPGRADE_CONFIG_SMOKE_SOURCE}" [=[
#define RECOIL_NATIVE_WESTWOOD_ONLINE_UPGRADE_CONFIG_SMOKES_ONLY
#include "]=] "${CMAKE_CURRENT_SOURCE_DIR}/westwood_online_upgrade_tests.cpp" [=["
]=])
target_sources(recoil_native_smoke PRIVATE "${WESTWOOD_ONLINE_UPGRADE_CONFIG_SMOKE_SOURCE}")

set(PLAYER_AI_MODE2_SMOKE_SOURCE
    "${CMAKE_CURRENT_BINARY_DIR}/player_ai_mode2_smokes.cpp"
)
set(PLAYER_AI_MODE2_SMOKE_GENERATOR
    "${CMAKE_CURRENT_BINARY_DIR}/generate_player_ai_mode2_smokes.py"
)
file(WRITE "${PLAYER_AI_MODE2_SMOKE_GENERATOR}" [=[
from pathlib import Path
import sys

source_path = Path(sys.argv[1])
output_path = Path(sys.argv[2])
text = source_path.read_text()
lines = text.splitlines()

prefix_end = next(
    index for index, line in enumerate(lines)
    if line.startswith("#ifndef RECOIL_NATIVE_PLAYER_TESTS_GUN_DISPATCH_ONLY")
)

function_names = [
    "player_tick_ai_mode2_top_level_smoke",
    "player_build_ai_peer_rings_by_ai_net_id_smoke",
    "player_tick_ai_mode2_path_follow_smoke",
    "player_ai_mode2_forward_probe_requires_auto_turn_smoke",
    "player_ai_advance_path_cursor_and_compute_target_vec_smoke",
    "player_ai_choose_next_path_branch_index_smoke",
    "player_tick_ai_mode2_steering_substate_smoke",
    "player_update_ai_mode2_move_and_turn_toward_target_smoke",
    "player_tick_ai_mode2_offset_target_steering_smoke",
    "player_tick_ai_mode2_dynamic_offset_target_steering_smoke",
    "player_ai_try_enter_mode2_attack_pursuit_los_smoke",
    "player_ai_alert_attack_buddies_smoke",
    "player_ai_enter_mode2_steering_pursuit_smoke",
    "player_ai_rebuild_synthetic_path_to_node_if_far_smoke",
    "player_update_ai_mode2_turn_toward_player_no_throttle_smoke",
    "player_update_ai_mode2_turn_in_place_toward_player_smoke",
    "player_update_ai_mode2_move_and_turn_toward_offset_target_smoke",
    "player_update_ai_mode2_move_and_turn_toward_dynamic_offset_target_smoke",
    "player_tick_ai_mode2_timed_path_steering_smoke",
    "player_tick_remote_network_player_smoke",
]


def skip_literal(text, index):
    quote = text[index]
    index += 1
    while index < len(text):
        if text[index] == "\\":
            index += 2
            continue
        if text[index] == quote:
            return index + 1
        index += 1
    return index


def skip_line_comment(text, index):
    newline = text.find("\n", index + 2)
    return len(text) if newline == -1 else newline + 1


def skip_block_comment(text, index):
    end = text.find("*/", index + 2)
    return len(text) if end == -1 else end + 2


def extract_function(name):
    needle = f"{name}("
    name_pos = text.index(needle)
    start = text.rfind("\n", 0, name_pos) + 1
    index = text.index("{", start)
    depth = 0
    while index < len(text):
        if text[index] in ('"', "'"):
            index = skip_literal(text, index)
            continue
        if text.startswith("//", index):
            index = skip_line_comment(text, index)
            continue
        if text.startswith("/*", index):
            index = skip_block_comment(text, index)
            continue
        if text[index] == "{":
            depth += 1
        elif text[index] == "}":
            depth -= 1
            if depth == 0:
                return text[start:index + 1]
        index += 1
    raise RuntimeError(f"could not find end of {name}")


output = []
output.append("// Generated by tests/native/CMakeLists.txt from player_tests.cpp.")
output.append("// Emits selected player smoke functions to avoid duplicate smoke definitions.")
output.append("")
output.extend(lines[:prefix_end])
output.append("")
output.append(r"""
namespace {
template <typename T> T *AllocZeroedMalloc() {
    void *const mem = std::calloc(1, sizeof(T));
    return static_cast<T *>(mem);
}

template <typename T> T *AllocZeroedNew() {
    T *const value = static_cast<T *>(::operator new(sizeof(T)));
    std::memset(value, 0, sizeof(T));
    return value;
}

bool Vec3Equals(const zVec3 &value, const zVec3 &expected) {
    return value.x == expected.x && value.y == expected.y && value.z == expected.z;
}

bool FloatNear(float actual, float expected) {
    return std::fabs(actual - expected) < 0.0001f;
}

PlayerPendingContact *MakePendingContactChain(int count) {
    PlayerPendingContact *head = nullptr;
    PlayerPendingContact *tail = nullptr;
    for (int i = 0; i < count; ++i) {
        PlayerPendingContact *const contact = AllocZeroedNew<PlayerPendingContact>();
        if (head == nullptr) {
            head = contact;
        } else {
            tail->next = contact;
        }
        tail = contact;
    }
    return head;
}

void FillPendingContactQueue(PlayerPendingContactQueue *queue, int count) {
    queue->listAux = 0x12340000 + count;
    queue->head = MakePendingContactChain(count);
    queue->tail = queue->head;
    while (queue->tail != nullptr && queue->tail->next != nullptr) {
        queue->tail = queue->tail->next;
    }
    queue->count = count;
}

bool PendingContactQueueCleared(const PlayerPendingContactQueue &queue) {
    return queue.listAux == 0 && queue.head == nullptr && queue.tail == nullptr &&
           queue.count == 0;
}

bool PendingContactPayloadMatches(const PlayerPendingContact *contact,
                                  const zClassDiPickCandidateEntry &candidate,
                                  const zVec3 &segmentStart, const zVec3 &segmentEnd,
                                  int segmentTag) {
    return contact != nullptr && contact->hit.node == candidate.node &&
           contact->hit.scenePayload == candidate.scenePayload &&
           contact->hit.surfaceNormal.y == candidate.surfaceNormal.y &&
           Vec3Equals(contact->sweepStart, segmentStart) &&
           Vec3Equals(contact->sweepEnd, segmentEnd) && contact->segmentTag == segmentTag;
}

void InitObjectPositionNode(zClass_NodePartial *node, zClass_Object3DDataPartial *data,
                            float x, float y, float z) {
    std::memset(node, 0, sizeof(*node));
    std::memset(data, 0, sizeof(*data));
    node->classId = 5;
    node->classData = data;
    node->flags = 1;
    data->localMatrix[9] = x;
    data->localMatrix[10] = y;
    data->localMatrix[11] = z;
}
} // namespace
""".strip())
output.append("")

for function_name in function_names:
    output.append(extract_function(function_name).rstrip())
    output.append("")

output_path.write_text("\n".join(output))
]=])
execute_process(
    COMMAND ${Python3_EXECUTABLE}
        "${PLAYER_AI_MODE2_SMOKE_GENERATOR}"
        "${CMAKE_CURRENT_SOURCE_DIR}/player_tests.cpp"
        "${PLAYER_AI_MODE2_SMOKE_SOURCE}"
    RESULT_VARIABLE PLAYER_AI_MODE2_SMOKE_GENERATOR_RESULT
)
if(NOT PLAYER_AI_MODE2_SMOKE_GENERATOR_RESULT EQUAL 0)
    message(FATAL_ERROR "Failed to generate player AI mode-2 smoke source")
endif()
target_sources(recoil_native_smoke PRIVATE "${PLAYER_AI_MODE2_SMOKE_SOURCE}")

set(HUD_SENSOR_TRACKER_MISSION_SMOKE_SOURCE
    "${CMAKE_CURRENT_BINARY_DIR}/hud_sensor_tracker_mission_smokes.cpp"
)
set(HUD_SENSOR_TRACKER_MISSION_SMOKE_GENERATOR
    "${CMAKE_CURRENT_BINARY_DIR}/generate_hud_sensor_tracker_mission_smokes.py"
)
file(WRITE "${HUD_SENSOR_TRACKER_MISSION_SMOKE_GENERATOR}" [=[
from pathlib import Path
import sys

source_path = Path(sys.argv[1])
output_path = Path(sys.argv[2])
text = source_path.read_text()
lines = text.splitlines()

prefix_end = next(
    index for index, line in enumerate(lines)
    if line.startswith("#ifndef RECOIL_NATIVE_PLAYER_TESTS_GUN_DISPATCH_ONLY")
)
helper_end = next(
    index for index in range(prefix_end + 1, len(lines))
    if lines[index] == "} // namespace"
)


def skip_literal(text, index):
    quote = text[index]
    index += 1
    while index < len(text):
        if text[index] == "\\":
            index += 2
            continue
        if text[index] == quote:
            return index + 1
        index += 1
    return index


def skip_line_comment(text, index):
    newline = text.find("\n", index + 2)
    return len(text) if newline == -1 else newline + 1


def skip_block_comment(text, index):
    end = text.find("*/", index + 2)
    return len(text) if end == -1 else end + 2


def extract_function(name):
    needle = f"{name}("
    name_pos = text.index(needle)
    start = text.rfind("\n", 0, name_pos) + 1
    index = text.index("{", start)
    depth = 0
    while index < len(text):
        if text[index] in ('"', "'"):
            index = skip_literal(text, index)
            continue
        if text.startswith("//", index):
            index = skip_line_comment(text, index)
            continue
        if text.startswith("/*", index):
            index = skip_block_comment(text, index)
            continue
        if text[index] == "{":
            depth += 1
        elif text[index] == "}":
            depth -= 1
            if depth == 0:
                return text[start:index + 1]
        index += 1
    raise RuntimeError(f"could not find end of {name}")


output = []
output.append("// Generated by tests/native/CMakeLists.txt from player_tests.cpp.")
output.append("// Emits the HudSensorTracker mission-start smoke without broad player tests.")
output.append("")
output.extend(lines[:prefix_end])
output.append('#include "GameZRecoil/zHud/zhud_ui.h"')
output.append("")
output.append("#include <cstdint>")
output.append("#include <new>")
output.append("")
output.append('extern "C" int g_Player_MissionInitFirstRunFlag;')
output.append("")
for helper_name in [
    "MakeAinetReaderStringNode",
    "MakeAinetReaderIntNode",
    "MakeAinetReaderFloatNode",
    "MakeAinetReaderArrayNode",
    "WriteAinetZrdU32",
    "WriteAinetZrdNode",
]:
    output.append(extract_function(helper_name).rstrip())
    output.append("")
output.append("""struct AinetZrdArchiveEntry {
    const char *name;
    const zReader::Node *root;
};""")
output.append("")
output.append(extract_function("MountAinetZrdArchive").rstrip())
output.append("")
output.append(extract_function("FloatNear").rstrip())
output.append("")
output.append(extract_function("hud_sensor_tracker_init_mission_gameplay_systems_smoke").rstrip())
output.append("")
output.append(extract_function("hud_sensor_tracker_parse_checkpoint_number_from_node_smoke").rstrip())
output.append("")

output_path.write_text("\n".join(output))
]=])
execute_process(
    COMMAND ${Python3_EXECUTABLE}
        "${HUD_SENSOR_TRACKER_MISSION_SMOKE_GENERATOR}"
        "${CMAKE_CURRENT_SOURCE_DIR}/player_tests.cpp"
        "${HUD_SENSOR_TRACKER_MISSION_SMOKE_SOURCE}"
    RESULT_VARIABLE HUD_SENSOR_TRACKER_MISSION_SMOKE_GENERATOR_RESULT
)
if(NOT HUD_SENSOR_TRACKER_MISSION_SMOKE_GENERATOR_RESULT EQUAL 0)
    message(FATAL_ERROR "Failed to generate HudSensorTracker mission smoke source")
endif()
target_sources(recoil_native_smoke PRIVATE "${HUD_SENSOR_TRACKER_MISSION_SMOKE_SOURCE}")

set(HUD_SENSOR_TRACKER_OBJECTIVE_SMOKE_SOURCE
    "${CMAKE_CURRENT_BINARY_DIR}/hud_sensor_tracker_objective_smokes.cpp"
)
set(HUD_SENSOR_TRACKER_OBJECTIVE_SMOKE_GENERATOR
    "${CMAKE_CURRENT_BINARY_DIR}/generate_hud_sensor_tracker_objective_smokes.py"
)
file(WRITE "${HUD_SENSOR_TRACKER_OBJECTIVE_SMOKE_GENERATOR}" [=[
from pathlib import Path
import sys

source_path = Path(sys.argv[1])
output_path = Path(sys.argv[2])
text = source_path.read_text()

include_end = text.index('\n#ifndef RECOIL_ZGAME_TESTS_ZMODEL_CONST_ONLY')
namespace_start = text.index('\nnamespace {', include_end) + 1
namespace_end = text.index('\nextern "C" int zgame_return_only_stub_smoke')
prefix = text[:include_end] + "\n\n" + text[namespace_start:namespace_end]


def skip_literal(text, index):
    quote = text[index]
    index += 1
    while index < len(text):
        if text[index] == "\\":
            index += 2
            continue
        if text[index] == quote:
            return index + 1
        index += 1
    return index


def skip_line_comment(text, index):
    newline = text.find("\n", index + 2)
    return len(text) if newline == -1 else newline + 1


def skip_block_comment(text, index):
    end = text.find("*/", index + 2)
    return len(text) if end == -1 else end + 2


def extract_function(name):
    needle = f"{name}("
    name_pos = text.index(needle)
    start = text.rfind("\n", 0, name_pos) + 1
    index = text.index("{", start)
    depth = 0
    while index < len(text):
        if text[index] in ('"', "'"):
            index = skip_literal(text, index)
            continue
        if text.startswith("//", index):
            index = skip_line_comment(text, index)
            continue
        if text.startswith("/*", index):
            index = skip_block_comment(text, index)
            continue
        if text[index] == "{":
            depth += 1
        elif text[index] == "}":
            depth -= 1
            if depth == 0:
                return text[start:index + 1]
        index += 1
    raise RuntimeError(f"could not find end of {name}")


output = []
output.append("// Generated by tests/native/CMakeLists.txt from zgame_tests.cpp.")
output.append("// Emits only the HudSensorTracker objective smokes needed by functional manifests.")
output.append("")
output.append(prefix.rstrip())
output.append("")
for function_name in [
    "hud_sensor_objective_slot_reset_smoke",
    "hud_sensor_tracker_unload_objectives_smoke",
    "hud_sensor_tracker_load_objectives_from_path_smoke",
    "hud_sensor_tracker_load_objectives_from_zrd_smoke",
]:
    output.append(extract_function(function_name).rstrip())
    output.append("")

output_path.write_text("\n".join(output))
]=])
execute_process(
    COMMAND ${Python3_EXECUTABLE}
        "${HUD_SENSOR_TRACKER_OBJECTIVE_SMOKE_GENERATOR}"
        "${CMAKE_CURRENT_SOURCE_DIR}/zgame_tests.cpp"
        "${HUD_SENSOR_TRACKER_OBJECTIVE_SMOKE_SOURCE}"
    RESULT_VARIABLE HUD_SENSOR_TRACKER_OBJECTIVE_SMOKE_GENERATOR_RESULT
)
if(NOT HUD_SENSOR_TRACKER_OBJECTIVE_SMOKE_GENERATOR_RESULT EQUAL 0)
    message(FATAL_ERROR "Failed to generate HudSensorTracker objective smoke source")
endif()
target_sources(recoil_native_smoke PRIVATE "${HUD_SENSOR_TRACKER_OBJECTIVE_SMOKE_SOURCE}")

set(ZMODEL_SHUTDOWN_SMOKE_SOURCE
    "${CMAKE_CURRENT_BINARY_DIR}/zmodel_shutdown_smokes.cpp"
)
set(ZMODEL_SHUTDOWN_SMOKE_GENERATOR
    "${CMAKE_CURRENT_BINARY_DIR}/generate_zmodel_shutdown_smokes.py"
)
file(WRITE "${ZMODEL_SHUTDOWN_SMOKE_GENERATOR}" [=[
from pathlib import Path
import sys

source_path = Path(sys.argv[1])
output_path = Path(sys.argv[2])
text = source_path.read_text()

include_end = text.index('\n#ifndef RECOIL_ZGAME_TESTS_ZMODEL_CONST_ONLY')
namespace_start = text.index('\nnamespace {', include_end) + 1
namespace_end = text.index('\nextern "C" int zgame_return_only_stub_smoke')
prefix = text[:include_end] + "\n\n" + text[namespace_start:namespace_end]


def skip_literal(text, index):
    quote = text[index]
    index += 1
    while index < len(text):
        if text[index] == "\\":
            index += 2
            continue
        if text[index] == quote:
            return index + 1
        index += 1
    return index


def skip_line_comment(text, index):
    newline = text.find("\n", index + 2)
    return len(text) if newline == -1 else newline + 1


def skip_block_comment(text, index):
    end = text.find("*/", index + 2)
    return len(text) if end == -1 else end + 2


def extract_function(name):
    needle = f"{name}("
    name_pos = text.index(needle)
    start = text.rfind("\n", 0, name_pos) + 1
    index = text.index("{", start)
    depth = 0
    while index < len(text):
        if text[index] in ('"', "'"):
            index = skip_literal(text, index)
            continue
        if text.startswith("//", index):
            index = skip_line_comment(text, index)
            continue
        if text.startswith("/*", index):
            index = skip_block_comment(text, index)
            continue
        if text[index] == "{":
            depth += 1
        elif text[index] == "}":
            depth -= 1
            if depth == 0:
                return text[start:index + 1]
        index += 1
    raise RuntimeError(f"could not find end of {name}")


output = []
output.append("// Generated by tests/native/CMakeLists.txt from zgame_tests.cpp.")
output.append("// Emits only the zModel shutdown smokes needed by functional manifests.")
output.append("")
output.append(prefix.rstrip())
output.append("")
for function_name in [
    "zmodel_display_shutdown_smoke",
    "zmodel_matlslot_release_smoke",
    "zmodel_matlbuffer_release_all_active_smoke",
]:
    output.append(extract_function(function_name).rstrip())
    output.append("")

output_path.write_text("\n".join(output))
]=])
execute_process(
    COMMAND ${Python3_EXECUTABLE}
        "${ZMODEL_SHUTDOWN_SMOKE_GENERATOR}"
        "${CMAKE_CURRENT_SOURCE_DIR}/zgame_tests.cpp"
        "${ZMODEL_SHUTDOWN_SMOKE_SOURCE}"
    RESULT_VARIABLE ZMODEL_SHUTDOWN_SMOKE_GENERATOR_RESULT
)
if(NOT ZMODEL_SHUTDOWN_SMOKE_GENERATOR_RESULT EQUAL 0)
    message(FATAL_ERROR "Failed to generate zModel shutdown smoke source")
endif()
target_sources(recoil_native_smoke PRIVATE "${ZMODEL_SHUTDOWN_SMOKE_SOURCE}")

set(ZMODEL_DIPOOL_STREAM_SMOKE_SOURCE
    "${CMAKE_CURRENT_BINARY_DIR}/zmodel_dipool_stream_smokes.cpp"
)
set(ZMODEL_DIPOOL_STREAM_SMOKE_GENERATOR
    "${CMAKE_CURRENT_BINARY_DIR}/generate_zmodel_dipool_stream_smokes.py"
)
file(WRITE "${ZMODEL_DIPOOL_STREAM_SMOKE_GENERATOR}" [=[
from pathlib import Path
import sys

source_path = Path(sys.argv[1])
output_path = Path(sys.argv[2])
text = source_path.read_text()

include_end = text.index('\n#ifndef RECOIL_ZGAME_TESTS_ZMODEL_CONST_ONLY')
namespace_start = text.index('\nnamespace {', include_end) + 1
namespace_end = text.index('\nextern "C" int zgame_return_only_stub_smoke')
prefix = text[:include_end] + "\n\n" + text[namespace_start:namespace_end]


def skip_literal(text, index):
    quote = text[index]
    index += 1
    while index < len(text):
        if text[index] == "\\":
            index += 2
            continue
        if text[index] == quote:
            return index + 1
        index += 1
    return index


def skip_line_comment(text, index):
    newline = text.find("\n", index + 2)
    return len(text) if newline == -1 else newline + 1


def skip_block_comment(text, index):
    end = text.find("*/", index + 2)
    return len(text) if end == -1 else end + 2


def extract_function(name):
    needle = f"{name}("
    name_pos = text.index(needle)
    start = text.rfind("\n", 0, name_pos) + 1
    index = text.index("{", start)
    depth = 0
    while index < len(text):
        if text[index] in ('"', "'"):
            index = skip_literal(text, index)
            continue
        if text.startswith("//", index):
            index = skip_line_comment(text, index)
            continue
        if text.startswith("/*", index):
            index = skip_block_comment(text, index)
            continue
        if text[index] == "{":
            depth += 1
        elif text[index] == "}":
            depth -= 1
            if depth == 0:
                return text[start:index + 1]
        index += 1
    raise RuntimeError(f"could not find end of {name}")


output = []
output.append("// Generated by tests/native/CMakeLists.txt from zgame_tests.cpp.")
output.append("// Emits only the zModel DI-pool stream smokes needed by functional manifests.")
output.append("")
output.append(prefix.rstrip())
output.append("")
for function_name in [
    "zmodel_dipool_write_to_stream_smoke",
    "zmodel_dipool_read_from_stream_smoke",
    "zmodel_dipool_read_entry_by_index_from_stream_smoke",
]:
    output.append(extract_function(function_name).rstrip())
    output.append("")

output_path.write_text("\n".join(output))
]=])
execute_process(
    COMMAND ${Python3_EXECUTABLE}
        "${ZMODEL_DIPOOL_STREAM_SMOKE_GENERATOR}"
        "${CMAKE_CURRENT_SOURCE_DIR}/zgame_tests.cpp"
        "${ZMODEL_DIPOOL_STREAM_SMOKE_SOURCE}"
    RESULT_VARIABLE ZMODEL_DIPOOL_STREAM_SMOKE_GENERATOR_RESULT
)
if(NOT ZMODEL_DIPOOL_STREAM_SMOKE_GENERATOR_RESULT EQUAL 0)
    message(FATAL_ERROR "Failed to generate zModel DI-pool stream smoke source")
endif()
target_sources(recoil_native_smoke PRIVATE "${ZMODEL_DIPOOL_STREAM_SMOKE_SOURCE}")

set(ZDI_ENTRY_MATERIAL_HELPER_SMOKE_SOURCE
    "${CMAKE_CURRENT_BINARY_DIR}/zdi_entry_material_helper_smoke.cpp"
)
set(ZDI_ENTRY_MATERIAL_HELPER_SMOKE_GENERATOR
    "${CMAKE_CURRENT_BINARY_DIR}/generate_zdi_entry_material_helper_smoke.py"
)
file(WRITE "${ZDI_ENTRY_MATERIAL_HELPER_SMOKE_GENERATOR}" [=[
from pathlib import Path
import sys

source_path = Path(sys.argv[1])
output_path = Path(sys.argv[2])
text = source_path.read_text()

include_end = text.index('\n#ifndef RECOIL_ZGAME_TESTS_ZMODEL_CONST_ONLY')
namespace_start = text.index('\nnamespace {', include_end) + 1
namespace_end = text.index('\nextern "C" int zgame_return_only_stub_smoke')
prefix = text[:include_end] + "\n\n" + text[namespace_start:namespace_end]


def skip_literal(text, index):
    quote = text[index]
    index += 1
    while index < len(text):
        if text[index] == "\\":
            index += 2
            continue
        if text[index] == quote:
            return index + 1
        index += 1
    return index


def skip_line_comment(text, index):
    newline = text.find("\n", index + 2)
    return len(text) if newline == -1 else newline + 1


def skip_block_comment(text, index):
    end = text.find("*/", index + 2)
    return len(text) if end == -1 else end + 2


def extract_function(name):
    needle = f"{name}("
    name_pos = text.index(needle)
    start = text.rfind("\n", 0, name_pos) + 1
    index = text.index("{", start)
    depth = 0
    while index < len(text):
        if text[index] in ('"', "'"):
            index = skip_literal(text, index)
            continue
        if text.startswith("//", index):
            index = skip_line_comment(text, index)
            continue
        if text.startswith("/*", index):
            index = skip_block_comment(text, index)
            continue
        if text[index] == "{":
            depth += 1
        elif text[index] == "}":
            depth -= 1
            if depth == 0:
                return text[start:index + 1]
        index += 1
    raise RuntimeError(f"could not find end of {name}")


output = []
output.append("// Generated by tests/native/CMakeLists.txt from zgame_tests.cpp.")
output.append("// Emits only the zDi material helper/current variant smokes needed by functional manifests.")
output.append("")
output.append(prefix.rstrip())
output.append("")
output.append(extract_function("zdi_reset_current_variant_smoke").rstrip())
output.append("")
output.append(extract_function("zdi_set_current_variant_smoke").rstrip())
output.append("")
output.append(extract_function("zdi_entry_material_helpers_smoke").rstrip())
output.append("")

output_path.write_text("\n".join(output))
]=])
execute_process(
    COMMAND ${Python3_EXECUTABLE}
        "${ZDI_ENTRY_MATERIAL_HELPER_SMOKE_GENERATOR}"
        "${CMAKE_CURRENT_SOURCE_DIR}/zgame_tests.cpp"
        "${ZDI_ENTRY_MATERIAL_HELPER_SMOKE_SOURCE}"
    RESULT_VARIABLE ZDI_ENTRY_MATERIAL_HELPER_SMOKE_GENERATOR_RESULT
)
if(NOT ZDI_ENTRY_MATERIAL_HELPER_SMOKE_GENERATOR_RESULT EQUAL 0)
    message(FATAL_ERROR "Failed to generate zDi entry material helper smoke source")
endif()
target_sources(recoil_native_smoke PRIVATE "${ZDI_ENTRY_MATERIAL_HELPER_SMOKE_SOURCE}")

set(ZCLASS_CAMERA_CONVEX_HULL_XZ_SMOKE_SOURCE
    "${CMAKE_CURRENT_BINARY_DIR}/zclass_camera_convex_hull_xz_smokes.cpp"
)
set(ZCLASS_CAMERA_CONVEX_HULL_XZ_SMOKE_GENERATOR
    "${CMAKE_CURRENT_BINARY_DIR}/generate_zclass_camera_convex_hull_xz_smokes.py"
)
file(WRITE "${ZCLASS_CAMERA_CONVEX_HULL_XZ_SMOKE_GENERATOR}" [=[
from pathlib import Path
import sys

source_path = Path(sys.argv[1])
output_path = Path(sys.argv[2])
text = source_path.read_text()

include_end = text.index('\n#ifndef RECOIL_ZGAME_TESTS_ZMODEL_CONST_ONLY')
prefix = text[:include_end]


def skip_literal(text, index):
    quote = text[index]
    index += 1
    while index < len(text):
        if text[index] == "\\":
            index += 2
            continue
        if text[index] == quote:
            return index + 1
        index += 1
    return index


def skip_line_comment(text, index):
    newline = text.find("\n", index + 2)
    return len(text) if newline == -1 else newline + 1


def skip_block_comment(text, index):
    end = text.find("*/", index + 2)
    return len(text) if end == -1 else end + 2


def extract_function(name):
    needle = f"{name}("
    name_pos = text.index(needle)
    start = text.rfind("\n", 0, name_pos) + 1
    index = text.index("{", start)
    depth = 0
    while index < len(text):
        if text[index] in ('"', "'"):
            index = skip_literal(text, index)
            continue
        if text.startswith("//", index):
            index = skip_line_comment(text, index)
            continue
        if text.startswith("/*", index):
            index = skip_block_comment(text, index)
            continue
        if text[index] == "{":
            depth += 1
        elif text[index] == "}":
            depth -= 1
            if depth == 0:
                return text[start:index + 1]
        index += 1
    raise RuntimeError(f"could not find end of {name}")


output = []
output.append("// Generated by tests/native/CMakeLists.txt from zgame_tests.cpp.")
output.append("// Emits only the zClass camera XZ hull smoke needed by functional manifests.")
output.append("")
output.append(prefix.rstrip())
output.append("")
output.append(extract_function("zclass_camera_convex_hull_xz_smoke").rstrip())
output.append("")

output_path.write_text("\n".join(output))
]=])
execute_process(
    COMMAND ${Python3_EXECUTABLE}
        "${ZCLASS_CAMERA_CONVEX_HULL_XZ_SMOKE_GENERATOR}"
        "${CMAKE_CURRENT_SOURCE_DIR}/zgame_tests.cpp"
        "${ZCLASS_CAMERA_CONVEX_HULL_XZ_SMOKE_SOURCE}"
    RESULT_VARIABLE ZCLASS_CAMERA_CONVEX_HULL_XZ_SMOKE_GENERATOR_RESULT
)
if(NOT ZCLASS_CAMERA_CONVEX_HULL_XZ_SMOKE_GENERATOR_RESULT EQUAL 0)
    message(FATAL_ERROR "Failed to generate zClass camera XZ hull smoke source")
endif()
target_sources(recoil_native_smoke PRIVATE "${ZCLASS_CAMERA_CONVEX_HULL_XZ_SMOKE_SOURCE}")

set(ZCLASS_CAMERA_RENDER_PASS_SMOKE_SOURCE
    "${CMAKE_CURRENT_BINARY_DIR}/zclass_camera_render_pass_smokes.cpp"
)
set(ZCLASS_CAMERA_RENDER_PASS_SMOKE_GENERATOR
    "${CMAKE_CURRENT_BINARY_DIR}/generate_zclass_camera_render_pass_smokes.py"
)
file(WRITE "${ZCLASS_CAMERA_RENDER_PASS_SMOKE_GENERATOR}" [=[
from pathlib import Path
import sys

source_path = Path(sys.argv[1])
output_path = Path(sys.argv[2])
text = source_path.read_text()

include_end = text.index('\n#ifndef RECOIL_ZGAME_TESTS_ZMODEL_CONST_ONLY')
namespace_start = text.index('\nnamespace {', include_end) + 1
namespace_end = text.index('\nextern "C" int zgame_return_only_stub_smoke')
prefix = text[:include_end] + "\n\n" + text[namespace_start:namespace_end]


def skip_literal(text, index):
    quote = text[index]
    index += 1
    while index < len(text):
        if text[index] == "\\":
            index += 2
            continue
        if text[index] == quote:
            return index + 1
        index += 1
    return index


def skip_line_comment(text, index):
    newline = text.find("\n", index + 2)
    return len(text) if newline == -1 else newline + 1


def skip_block_comment(text, index):
    end = text.find("*/", index + 2)
    return len(text) if end == -1 else end + 2


def extract_function(name):
    needle = f"{name}("
    name_pos = text.index(needle)
    start = text.rfind("\n", 0, name_pos) + 1
    index = text.index("{", start)
    depth = 0
    while index < len(text):
        if text[index] in ('"', "'"):
            index = skip_literal(text, index)
            continue
        if text.startswith("//", index):
            index = skip_line_comment(text, index)
            continue
        if text.startswith("/*", index):
            index = skip_block_comment(text, index)
            continue
        if text[index] == "{":
            depth += 1
        elif text[index] == "}":
            depth -= 1
            if depth == 0:
                return text[start:index + 1]
        index += 1
    raise RuntimeError(f"could not find end of {name}")


output = []
output.append("// Generated by tests/native/CMakeLists.txt from zgame_tests.cpp.")
output.append("// Emits only the zClass camera render-pass smokes needed by functional manifests.")
output.append("")
output.append(prefix.rstrip())
output.append("")
for function_name in [
    "zclass_camera_build_frustum_grid_tiles_from_params_smoke",
    "zclass_camera_build_frustum_grid_tiles_smoke",
    "zclass_camera_render_overlay_nodes_smoke",
]:
    output.append(extract_function(function_name).rstrip())
    output.append("")

output_path.write_text("\n".join(output))
]=])
execute_process(
    COMMAND ${Python3_EXECUTABLE}
        "${ZCLASS_CAMERA_RENDER_PASS_SMOKE_GENERATOR}"
        "${CMAKE_CURRENT_SOURCE_DIR}/zgame_tests.cpp"
        "${ZCLASS_CAMERA_RENDER_PASS_SMOKE_SOURCE}"
    RESULT_VARIABLE ZCLASS_CAMERA_RENDER_PASS_SMOKE_GENERATOR_RESULT
)
if(NOT ZCLASS_CAMERA_RENDER_PASS_SMOKE_GENERATOR_RESULT EQUAL 0)
    message(FATAL_ERROR "Failed to generate zClass camera render-pass smoke source")
endif()
target_sources(recoil_native_smoke PRIVATE "${ZCLASS_CAMERA_RENDER_PASS_SMOKE_SOURCE}")

set(ZCLASS_WORLD_SETTINGS_SMOKE_SOURCE
    "${CMAKE_CURRENT_BINARY_DIR}/zclass_world_settings_smokes.cpp"
)
set(ZCLASS_WORLD_SETTINGS_SMOKE_GENERATOR
    "${CMAKE_CURRENT_BINARY_DIR}/generate_zclass_world_settings_smokes.py"
)
file(WRITE "${ZCLASS_WORLD_SETTINGS_SMOKE_GENERATOR}" [=[
from pathlib import Path
import sys

source_path = Path(sys.argv[1])
output_path = Path(sys.argv[2])
text = source_path.read_text()

include_end = text.index('\n#ifndef RECOIL_ZGAME_TESTS_ZMODEL_CONST_ONLY')
namespace_start = text.index('\nnamespace {', include_end) + 1
namespace_end = text.index('\nextern "C" int zgame_return_only_stub_smoke')
prefix = text[:include_end] + "\n\n" + text[namespace_start:namespace_end]


def skip_literal(text, index):
    quote = text[index]
    index += 1
    while index < len(text):
        if text[index] == "\\":
            index += 2
            continue
        if text[index] == quote:
            return index + 1
        index += 1
    return index


def skip_line_comment(text, index):
    newline = text.find("\n", index + 2)
    return len(text) if newline == -1 else newline + 1


def skip_block_comment(text, index):
    end = text.find("*/", index + 2)
    return len(text) if end == -1 else end + 2


def extract_function(name):
    needle = f"{name}("
    name_pos = text.index(needle)
    start = text.rfind("\n", 0, name_pos) + 1
    index = text.index("{", start)
    depth = 0
    while index < len(text):
        if text[index] in ('"', "'"):
            index = skip_literal(text, index)
            continue
        if text.startswith("//", index):
            index = skip_line_comment(text, index)
            continue
        if text.startswith("/*", index):
            index = skip_block_comment(text, index)
            continue
        if text[index] == "{":
            depth += 1
        elif text[index] == "}":
            depth -= 1
            if depth == 0:
                return text[start:index + 1]
        index += 1
    raise RuntimeError(f"could not find end of {name}")


output = []
output.append("// Generated by tests/native/CMakeLists.txt from zgame_tests.cpp.")
output.append("// Emits only the zClass world-settings smoke needed by functional manifests.")
output.append("")
output.append(prefix.rstrip())
output.append("")
output.append(extract_function("zclass_world_settings_section_callbacks_smoke").rstrip())
output.append("")

output_path.write_text("\n".join(output))
]=])
execute_process(
    COMMAND ${Python3_EXECUTABLE}
        "${ZCLASS_WORLD_SETTINGS_SMOKE_GENERATOR}"
        "${CMAKE_CURRENT_SOURCE_DIR}/zgame_tests.cpp"
        "${ZCLASS_WORLD_SETTINGS_SMOKE_SOURCE}"
    RESULT_VARIABLE ZCLASS_WORLD_SETTINGS_SMOKE_GENERATOR_RESULT
)
if(NOT ZCLASS_WORLD_SETTINGS_SMOKE_GENERATOR_RESULT EQUAL 0)
    message(FATAL_ERROR "Failed to generate zClass world-settings smoke source")
endif()
target_sources(recoil_native_smoke PRIVATE "${ZCLASS_WORLD_SETTINGS_SMOKE_SOURCE}")

set(ZHUD_SENSOR_OBJECTIVE_SMOKE_SOURCE
    "${CMAKE_CURRENT_BINARY_DIR}/zhud_sensor_objective_smokes.cpp"
)
set(ZHUD_SENSOR_OBJECTIVE_SMOKE_GENERATOR
    "${CMAKE_CURRENT_BINARY_DIR}/generate_zhud_sensor_objective_smokes.py"
)
file(WRITE "${ZHUD_SENSOR_OBJECTIVE_SMOKE_GENERATOR}" [=[
from pathlib import Path
import sys

source_path = Path(sys.argv[1])
output_path = Path(sys.argv[2])
text = source_path.read_text()

branch_start = text.index('\n#else\n\n#include "Battlesport/GameNet.h"') + len('\n#else\n\n')
prefix_end = text.index('\nnamespace {', branch_start)
prefix = text[branch_start:prefix_end]


def skip_literal(text, index):
    quote = text[index]
    index += 1
    while index < len(text):
        if text[index] == "\\":
            index += 2
            continue
        if text[index] == quote:
            return index + 1
        index += 1
    return index


def skip_line_comment(text, index):
    newline = text.find("\n", index + 2)
    return len(text) if newline == -1 else newline + 1


def skip_block_comment(text, index):
    end = text.find("*/", index + 2)
    return len(text) if end == -1 else end + 2


def extract_function(name):
    needle = f"{name}("
    name_pos = text.index(needle)
    start = text.rfind("\n", 0, name_pos) + 1
    index = text.index("{", start)
    depth = 0
    while index < len(text):
        if text[index] in ('"', "'"):
            index = skip_literal(text, index)
            continue
        if text.startswith("//", index):
            index = skip_line_comment(text, index)
            continue
        if text.startswith("/*", index):
            index = skip_block_comment(text, index)
            continue
        if text[index] == "{":
            depth += 1
        elif text[index] == "}":
            depth -= 1
            if depth == 0:
                return text[start:index + 1]
        index += 1
    raise RuntimeError(f"could not find end of {name}")


def extract_span(start_needle, end_needle):
    start = text.index(start_needle)
    end = text.index(end_needle, start) + len(end_needle)
    return text[start:end]


def extract_span_after(start_needle, end_needle, after_index):
    start = text.index(start_needle, after_index)
    end = text.index(end_needle, start) + len(end_needle)
    return text[start:end]


def strip_legacy_ftable_assignments(function_text):
    output = []
    skipping = False
    for line in function_text.splitlines():
        if skipping:
            if ";" in line:
                skipping = False
            continue
        if ".ftable" in line and "=" in line:
            if ";" not in line:
                skipping = True
            continue
        output.append(line)
    return "\n".join(output)


output = []
output.append("// Generated by tests/native/CMakeLists.txt from zhud_ui_tests.cpp.")
output.append("// Emits only the zHud/HudSensorTracker objective smokes needed by functional manifests.")
output.append("")
output.append(prefix.rstrip())
output.append("")
output.append("namespace {")
output.append(extract_span(
    "template <typename T> T &TestFieldAt",
    "return delta > -0.0001f && delta < 0.0001f;\n}"
).rstrip())
output.append("")
output.append(extract_span(
    "struct TestReticleAttachState",
    "zClass_NodePartial *rootNode;\n};"
).rstrip())
output.append("")
output.append(extract_span_after(
    "int g_HudTestLine4Count = 0;",
    "int g_HudCircleDrawBaseCount = 0;",
    branch_start
).rstrip())
output.append("")
output.append(extract_span_after(
    "void __fastcall HudTestImmediateRaster4",
    "g_HudTestPointOpArgs[2] = color16;\n}",
    branch_start
).rstrip())
output.append("} // namespace")
output.append("")
for function_name in [
    "zhud_sensor_viewport_rect_smoke",
    "zhud_sensor_get_fx_rect_smoke",
    "hud_sensor_tracker_constructor_smoke",
    "hud_sensor_tracker_construct_global_smoke",
    "hud_sensor_tracker_shutdown_global_smoke",
    "hud_sensor_tracker_register_global_on_exit_smoke",
    "mission_init_objectives_smoke",
    "hud_sensor_tracker_write_mission_data_section_smoke",
    "hud_sensor_tracker_zar_mission_save_callback_smoke",
    "hud_sensor_tracker_zar_mission_late_save_callback_smoke",
    "hud_sensor_tracker_apply_mission_data_smoke",
    "hud_sensor_tracker_show_objective_pickup_info_smoke",
    "hud_sensor_tracker_objective_panel_visible_smoke",
    "hud_sensor_tracker_shutdown_smoke",
    "hud_sensor_tracker_save_state_marker_smoke",
    "hud_sensor_tracker_update_smoke",
    "hud_sensor_map_node_draw_projected_path_smoke",
    "hud_sensor_map_node_draw_on_tracker_smoke",
]:
    output.append(strip_legacy_ftable_assignments(extract_function(function_name)).rstrip())
    output.append("")

output_path.write_text("\n".join(output))
]=])
execute_process(
    COMMAND ${Python3_EXECUTABLE}
        "${ZHUD_SENSOR_OBJECTIVE_SMOKE_GENERATOR}"
        "${CMAKE_CURRENT_SOURCE_DIR}/zhud_ui_tests.cpp"
        "${ZHUD_SENSOR_OBJECTIVE_SMOKE_SOURCE}"
    RESULT_VARIABLE ZHUD_SENSOR_OBJECTIVE_SMOKE_GENERATOR_RESULT
)
if(NOT ZHUD_SENSOR_OBJECTIVE_SMOKE_GENERATOR_RESULT EQUAL 0)
    message(FATAL_ERROR "Failed to generate zHud sensor objective smoke source")
endif()
target_sources(recoil_native_smoke PRIVATE "${ZHUD_SENSOR_OBJECTIVE_SMOKE_SOURCE}")

set(BRIEFING_SET_PROGRESS_SMOKE_SOURCE
    "${CMAKE_CURRENT_BINARY_DIR}/briefing_set_progress_smoke.cpp"
)
set(BRIEFING_SET_PROGRESS_SMOKE_GENERATOR
    "${CMAKE_CURRENT_BINARY_DIR}/generate_briefing_set_progress_smoke.py"
)
file(WRITE "${BRIEFING_SET_PROGRESS_SMOKE_GENERATOR}" [=[
from pathlib import Path
import sys

source_path = Path(sys.argv[1])
output_path = Path(sys.argv[2])
text = source_path.read_text()

prefix_marker = 'extern "C" int briefing_stop_and_shutdown_thread_smoke'
prefix_end = text.index(prefix_marker)


def skip_literal(text, index):
    quote = text[index]
    index += 1
    while index < len(text):
        if text[index] == "\\":
            index += 2
            continue
        if text[index] == quote:
            return index + 1
        index += 1
    return index


def skip_line_comment(text, index):
    newline = text.find("\n", index + 2)
    return len(text) if newline == -1 else newline + 1


def skip_block_comment(text, index):
    end = text.find("*/", index + 2)
    return len(text) if end == -1 else end + 2


def extract_function(name):
    needle = f"{name}("
    name_pos = text.index(needle)
    start = text.rfind("\n", 0, name_pos) + 1
    index = text.index("{", start)
    depth = 0
    while index < len(text):
        if text[index] in ('"', "'"):
            index = skip_literal(text, index)
            continue
        if text.startswith("//", index):
            index = skip_line_comment(text, index)
            continue
        if text.startswith("/*", index):
            index = skip_block_comment(text, index)
            continue
        if text[index] == "{":
            depth += 1
        elif text[index] == "}":
            depth -= 1
            if depth == 0:
                return text[start:index + 1]
        index += 1
    raise RuntimeError(f"could not find end of {name}")


output = []
output.append("// Generated by tests/native/CMakeLists.txt from briefing_tests.cpp.")
output.append("// Emits the Briefing progress smoke without broad briefing test duplicates.")
output.append("")
output.append(text[:prefix_end].rstrip())
output.append("")
output.append(extract_function("briefing_set_progress_and_sleep_smoke").rstrip())
output.append("")

output_path.write_text("\n".join(output))
]=])
execute_process(
    COMMAND ${Python3_EXECUTABLE}
        "${BRIEFING_SET_PROGRESS_SMOKE_GENERATOR}"
        "${CMAKE_CURRENT_SOURCE_DIR}/briefing_tests.cpp"
        "${BRIEFING_SET_PROGRESS_SMOKE_SOURCE}"
    RESULT_VARIABLE BRIEFING_SET_PROGRESS_SMOKE_GENERATOR_RESULT
)
if(NOT BRIEFING_SET_PROGRESS_SMOKE_GENERATOR_RESULT EQUAL 0)
    message(FATAL_ERROR "Failed to generate Briefing set-progress smoke source")
endif()
target_sources(recoil_native_smoke PRIVATE "${BRIEFING_SET_PROGRESS_SMOKE_SOURCE}")

set(ZFMV_PLAY_MCI_SMOKES_SOURCE
    "${CMAKE_CURRENT_BINARY_DIR}/zfmv_play_mci_smokes.cpp"
)
set(ZFMV_PLAY_MCI_SMOKES_GENERATOR
    "${CMAKE_CURRENT_BINARY_DIR}/generate_zfmv_play_mci_smokes.py"
)
file(WRITE "${ZFMV_PLAY_MCI_SMOKES_GENERATOR}" [=[
from pathlib import Path
import sys

source_path = Path(sys.argv[1])
output_path = Path(sys.argv[2])
text = source_path.read_text()

else_marker = "\n#else\n\n"
else_start = text.index(else_marker) + len(else_marker)
else_end = text.rindex("\n#endif")
body = text[else_start:else_end]

prefix_end = body.index("\nzFMV_Action_Vtbl MakeTestActionVtable()")


def extract_function(name: str) -> str:
    signature = f'extern "C" int {name}(void)'
    start = body.index(signature)
    brace = body.index("{", start)
    depth = 0
    index = brace
    while index < len(body):
        if body[index] == "{":
            depth += 1
        elif body[index] == "}":
            depth -= 1
            if depth == 0:
                return body[start:index + 1]
        index += 1
    raise RuntimeError(f"could not find end of {name}")


output = []
output.append("// Generated by tests/native/CMakeLists.txt from fmv_script_tests.cpp.")
output.append("// Emits FMV smokes that are excluded from the main fmv_script_tests.cpp object.")
output.append("")
output.append(body[:prefix_end].rstrip())
output.append("} // namespace")
output.append("")
output.append(extract_function("zfmv_action_blur_begin_end_smoke").rstrip())
output.append("")
output.append("""extern "C" int zfmv_action_play_mci_update_smoke(void) {
    alignas(zFMV_ActionPlayMci) unsigned char actionStorage[sizeof(zFMV_ActionPlayMci)];
    zFMV_ActionPlayMci *const action = new (actionStorage) zFMV_ActionPlayMci();
    action->next = reinterpret_cast<zFMV_Action *>(0x11111111);
    action->mediaPath = reinterpret_cast<char *>(0x22222222);
    action->playback = reinterpret_cast<zFMV_Playback *>(0x33333333);

    const int result = action->Update(123.5);
    return result == 0 && action->next == reinterpret_cast<zFMV_Action *>(0x11111111) &&
                   action->mediaPath == reinterpret_cast<char *>(0x22222222) &&
                   action->playback == reinterpret_cast<zFMV_Playback *>(0x33333333)
               ? 0
               : 1;
}""")
output.append("")
output.append("""extern "C" int zfmv_action_play_mci_begin_smoke(void) {
    CodeFunctionPatch mciPatch{};
    if (!PatchFunctionJump(
            reinterpret_cast<void *>(&mciSendCommandA),
            reinterpret_cast<void *>(&FakeFmvMciSendCommandA),
            mciPatch
        )) {
        return 1;
    }

    zFMV_ActionPlayMci emptyAction{};
    emptyAction.mediaPath = nullptr;
    emptyAction.playback = nullptr;
    emptyAction.Begin(12.0);
    const bool nullOk = g_fakeFmvMciSendCommandCount == 0;

    zFMV_Playback playback{};
    playback.mediaPathDup = const_cast<char *>("intro.mpg");
    playback.notifyHwnd = reinterpret_cast<HWND>(0x2468);
    playback.mciPutFlags = 0;

    zFMV_ActionPlayMci action{};
    action.mediaPath = nullptr;
    action.playback = &playback;

    g_fakeFmvMciSendCommandCount = 0;
    for (int index = 0; index < 8; ++index) {
        g_fakeFmvMciDevices[index] = 0;
        g_fakeFmvMciMessages[index] = 0;
        g_fakeFmvMciFlags[index] = 0;
        g_fakeFmvMciParams[index] = 0;
        g_fakeFmvMciReturns[index] = 0;
    }
    g_fakeFmvPlayCallback = 0;
    g_fakeFmvPlayFrom = 0xffffffffu;
    g_fakeFmvPlayTo = 0xffffffffu;

    action.Begin(34.0);

    const bool playbackOk =
        g_fakeFmvMciSendCommandCount == 4 && g_fakeFmvMciDevices[0] == 0 &&
        g_fakeFmvMciMessages[0] == 0x803 && g_fakeFmvMciFlags[0] == 0x2202 &&
        g_fakeFmvMciDevices[1] == 0x3456 && g_fakeFmvMciMessages[1] == 0x841 &&
        g_fakeFmvMciFlags[1] == 0x10002 && g_fakeFmvWindowHwnd == playback.notifyHwnd &&
        g_fakeFmvMciDevices[2] == 0x3456 && g_fakeFmvMciMessages[2] == 0x811 &&
        g_fakeFmvMciFlags[2] == 0x302 && g_fakeFmvSetTimeFormat == 0x1b &&
        g_fakeFmvMciDevices[3] == 0x3456 && g_fakeFmvMciMessages[3] == 0x806 &&
        g_fakeFmvMciFlags[3] == 0x6 && g_fakeFmvPlayCallback ==
            static_cast<DWORD>(reinterpret_cast<std::uintptr_t>(playback.notifyHwnd)) &&
        g_fakeFmvPlayFrom == 0;

    action.playback = nullptr;
    RestoreFunctionPatch(mciPatch);
    return nullOk && playbackOk ? 0 : 2;
}""")
output.append("")
end_smoke = extract_function("zfmv_action_play_mci_end_smoke")
end_smoke = end_smoke.replace(
    "    zFMV_ActionPlayMci action{};\n    action.playback = &playback;",
    "    zFMV_ActionPlayMci action{};\n    action.mediaPath = nullptr;\n    action.playback = &playback;",
)
output.append(end_smoke.rstrip())
output.append("")
output.append("""extern "C" int zfmv_action_play_mci_lifecycle_smoke(void) {
    CodeFunctionPatch deletePatch{};
    void(__cdecl *operatorDeleteFn)(void *) = &operator delete;
    if (!PatchFunctionJump(reinterpret_cast<void *>(operatorDeleteFn),
                           reinterpret_cast<void *>(&FakeFmvOperatorDelete), deletePatch)) {
        return 1;
    }

    alignas(zFMV_ActionPlayMci) unsigned char actionStorage[sizeof(zFMV_ActionPlayMci)];
    zFMV_ActionPlayMci *const action = new (actionStorage) zFMV_ActionPlayMci();
    action->mediaPath = static_cast<char *>(std::malloc(6));
    action->playback = static_cast<zFMV_Playback *>(std::malloc(sizeof(zFMV_Playback)));
    if (action->mediaPath == nullptr || action->playback == nullptr) {
        RestoreFunctionPatch(deletePatch);
        std::free(action->mediaPath);
        std::free(action->playback);
        return 2;
    }
    std::strcpy(action->mediaPath, "movie");
    std::memset(action->playback, 0, sizeof(*action->playback));
    action->playback->mediaPathDup = static_cast<char *>(std::malloc(6));
    if (action->playback->mediaPathDup == nullptr) {
        RestoreFunctionPatch(deletePatch);
        std::free(action->mediaPath);
        std::free(action->playback);
        return 3;
    }
    std::strcpy(action->playback->mediaPathDup, "movie");

    zFMV_Playback *const directPlayback = action->playback;
    g_fakeFmvOperatorDeleteCount = 0;
    g_fakeFmvOperatorDeletePtr = nullptr;
    action->~zFMV_ActionPlayMci();
    const bool directOk =
        action->mediaPath == nullptr && action->playback == nullptr &&
        g_fakeFmvOperatorDeleteCount == 1 && g_fakeFmvOperatorDeletePtr == directPlayback;

    RestoreFunctionPatch(deletePatch);
    std::free(directPlayback);
    return directOk ? 0 : 6;
}""")
output.append("")

output_path.write_text("\n".join(output))
]=])
execute_process(
    COMMAND ${Python3_EXECUTABLE}
        "${ZFMV_PLAY_MCI_SMOKES_GENERATOR}"
        "${CMAKE_CURRENT_SOURCE_DIR}/fmv_script_tests.cpp"
        "${ZFMV_PLAY_MCI_SMOKES_SOURCE}"
    RESULT_VARIABLE ZFMV_PLAY_MCI_SMOKES_GENERATOR_RESULT
)
if(NOT ZFMV_PLAY_MCI_SMOKES_GENERATOR_RESULT EQUAL 0)
    message(FATAL_ERROR "Failed to generate zFMV PlayMci smoke source")
endif()
target_sources(recoil_native_smoke PRIVATE "${ZFMV_PLAY_MCI_SMOKES_SOURCE}")

set_source_files_properties(
    zweapon_tests.cpp
    PROPERTIES COMPILE_DEFINITIONS RECOIL_NATIVE_ZWEAPON_SKIP_DUPLICATE_TIMED_HIT_SMOKE
)

set_source_files_properties(
    zhud_ui_tests.cpp
    PROPERTIES COMPILE_DEFINITIONS RECOIL_NATIVE_ZHUD_UI_LIFECYCLE_ONLY
)

set_source_files_properties(
    czrecoil_frame_tests.cpp
    PROPERTIES COMPILE_DEFINITIONS
        "RECOIL_NATIVE_CZRECOIL_FRAME_TESTS_SKIP_SHARED_SMOKES;RECOIL_NATIVE_CZRECOIL_FRAME_TESTS_MENU_SMOKES"
)

set_source_files_properties(
    player_tests.cpp
    PROPERTIES COMPILE_DEFINITIONS
        "RECOIL_NATIVE_PLAYER_TESTS_GUN_DISPATCH_ONLY;RECOIL_NATIVE_PLAYER_TESTS_CAMERA_SMOKES;RECOIL_NATIVE_PLAYER_TESTS_ENV_PROBE_SMOKES;RECOIL_NATIVE_PLAYER_TESTS_PENDING_CONTACT_SMOKES;RECOIL_NATIVE_PLAYER_TESTS_ASYNC_COMMAND_CALLBACK_SMOKE;RECOIL_NATIVE_PLAYER_TESTS_GAMEPLAY_CALLBACK_SMOKE"
)

set_source_files_properties(
    fmv_script_tests.cpp
    PROPERTIES COMPILE_DEFINITIONS RECOIL_FMV_SCRIPT_TESTS_WAIT_ONLY
)

set_source_files_properties(
    recoil_app_message_map.cpp
    PROPERTIES COMPILE_DEFINITIONS RECOIL_NATIVE_RECOIL_APP_MESSAGE_MAP_CALLBACK_SMOKES_ONLY
)

set(ZDECLIENT_NATIVE_SMOKE_PREFIX
    "${CMAKE_CURRENT_BINARY_DIR}/zdeclient_native_smoke_prefix.h"
)
file(WRITE "${ZDECLIENT_NATIVE_SMOKE_PREFIX}" [=[
#include "GameZRecoil/zNetwork/zNetwork.h"
#include <cstdint>

struct zNetwork_DPlay4SmokeVtable;
struct zNetwork_DPlay4Smoke {
    const zNetwork_DPlay4SmokeVtable *lpVtbl;
};

typedef std::int32_t(__stdcall *zNetwork_DPlay4SmokeSendFn)(
    zNetwork_DPlay4Smoke *,
    std::uint32_t,
    std::uint32_t,
    std::uint32_t,
    void *,
    std::uint32_t
);

struct zNetwork_DPlay4SmokeVtable {
    void *slots_00_64[26];
    zNetwork_DPlay4SmokeSendFn Send_68;
};

#define zNetwork_DPlay4 zNetwork_DPlay4Smoke
#define zNetwork_DPlay4Vtable zNetwork_DPlay4SmokeVtable
#define g_zNetwork_pDirectPlay4 (*((zNetwork_DPlay4Smoke **)&g_zNetwork_pDirectPlay4))
]=])

set_source_files_properties(
    westwood_online_upgrade_tests.cpp
    PROPERTIES COMPILE_DEFINITIONS RECOIL_NATIVE_WESTWOOD_ONLINE_UPGRADE_UNADVISE_SMOKE_ONLY
)

set_source_files_properties(
    zdeclient_tests.cpp
    PROPERTIES COMPILE_OPTIONS "/FI${ZDECLIENT_NATIVE_SMOKE_PREFIX}"
)

find_package(Python3 COMPONENTS Interpreter REQUIRED)

add_test(
    NAME recoil_native_smoke
    COMMAND ${Python3_EXECUTABLE}
        ${CMAKE_CURRENT_SOURCE_DIR}/run_native_smokes.py
        $<TARGET_FILE:recoil_native_smoke>
        --smoke-cpp ${CMAKE_CURRENT_SOURCE_DIR}/smoke.cpp
    WORKING_DIRECTORY ${PROJECT_SOURCE_DIR}
)

add_test(
    NAME recoil_no_raw_image_addresses
    COMMAND ${Python3_EXECUTABLE}
        ${PROJECT_SOURCE_DIR}/tools/recoil.py
        guard
        raw-image
        --root src
        --allowlist .agent/RAW_ADDRESS_ALLOWLIST.txt
    WORKING_DIRECTORY ${PROJECT_SOURCE_DIR}
)

add_test(
    NAME recoil_no_raw_assembly
    COMMAND ${Python3_EXECUTABLE}
        ${PROJECT_SOURCE_DIR}/tools/recoil.py
        guard
        raw-assembly
        --root src
        --allowlist .agent/RAW_ASSEMBLY_ALLOWLIST.txt
    WORKING_DIRECTORY ${PROJECT_SOURCE_DIR}
)

add_test(
    NAME recoil_no_modern_cpp_constructs
    COMMAND ${Python3_EXECUTABLE}
        ${PROJECT_SOURCE_DIR}/tools/recoil.py
        guard
        modern-cpp
        --root src
    WORKING_DIRECTORY ${PROJECT_SOURCE_DIR}
)

add_test(
    NAME recoil_raw_offset_guard
    COMMAND ${Python3_EXECUTABLE}
        ${PROJECT_SOURCE_DIR}/tools/recoil.py
        guard
        raw-offset
        --root src
        --plan .agent/RECOIL_PLAN.md
        --allowlist .agent/RAW_OFFSET_ALLOWLIST.txt
    WORKING_DIRECTORY ${PROJECT_SOURCE_DIR}
)

add_test(
    NAME recoil_provider_boundary_guard
    COMMAND ${Python3_EXECUTABLE}
        ${PROJECT_SOURCE_DIR}/tools/recoil.py
        guard
        provider
        --root src
    WORKING_DIRECTORY ${PROJECT_SOURCE_DIR}
)

add_test(
    NAME recoil_provider_boundary_guard_selftest
    COMMAND ${Python3_EXECUTABLE}
        ${PROJECT_SOURCE_DIR}/tools/recoil.py
        guard
        provider
        --self-test
    WORKING_DIRECTORY ${PROJECT_SOURCE_DIR}
)

add_test(
    NAME recoil_source_placement_guard
    COMMAND ${Python3_EXECUTABLE}
        ${PROJECT_SOURCE_DIR}/tools/recoil.py
        guard
        source-placement
        --root src
    WORKING_DIRECTORY ${PROJECT_SOURCE_DIR}
)

add_test(
    NAME recoil_source_placement_guard_selftest
    COMMAND ${Python3_EXECUTABLE}
        ${PROJECT_SOURCE_DIR}/tools/recoil.py
        guard
        source-placement
        --self-test
    WORKING_DIRECTORY ${PROJECT_SOURCE_DIR}
)

add_test(
    NAME recoil_multiline_style_guard
    COMMAND ${Python3_EXECUTABLE}
        ${PROJECT_SOURCE_DIR}/tools/recoil.py
        guard
        multiline
        --root src
    WORKING_DIRECTORY ${PROJECT_SOURCE_DIR}
)

add_test(
    NAME recoil_multiline_style_guard_selftest
    COMMAND ${Python3_EXECUTABLE}
        ${PROJECT_SOURCE_DIR}/tools/recoil.py
        guard
        multiline
        --self-test
    WORKING_DIRECTORY ${PROJECT_SOURCE_DIR}
)

add_test(
    NAME recoil_source_data_initializer_guard
    COMMAND ${Python3_EXECUTABLE}
        ${PROJECT_SOURCE_DIR}/tools/recoil.py
        guard
        source-data
        --manifest .agent/SOURCE_DATA_INITIALIZERS.json
    WORKING_DIRECTORY ${PROJECT_SOURCE_DIR}
)

add_test(
    NAME recoil_source_data_initializer_guard_selftest
    COMMAND ${Python3_EXECUTABLE}
        ${PROJECT_SOURCE_DIR}/tools/recoil.py
        guard
        source-data
        --self-test
    WORKING_DIRECTORY ${PROJECT_SOURCE_DIR}
)

add_test(
    NAME recoil_vc5_manifest_source_policy
    COMMAND ${Python3_EXECUTABLE}
        ${PROJECT_SOURCE_DIR}/tools/recoil.py
        guard
        vc5-manifest
    WORKING_DIRECTORY ${PROJECT_SOURCE_DIR}
)

add_test(
    NAME recoil_source_file_map_current
    COMMAND ${Python3_EXECUTABLE}
        ${PROJECT_SOURCE_DIR}/tools/recoil.py
        audit
        source-map
        --check docs/reconstruction/source_file_map.md
    WORKING_DIRECTORY ${PROJECT_SOURCE_DIR}
)

add_test(
    NAME recoil_compiler_linker_provenance
    COMMAND ${Python3_EXECUTABLE}
        ${PROJECT_SOURCE_DIR}/tools/recoil.py
        audit
        provenance
        --strict
    WORKING_DIRECTORY ${PROJECT_SOURCE_DIR}
)

add_test(
    NAME recoil_workspace_hygiene
    COMMAND ${Python3_EXECUTABLE}
        ${PROJECT_SOURCE_DIR}/tools/recoil.py
        audit
        workspace
        --strict
    WORKING_DIRECTORY ${PROJECT_SOURCE_DIR}
)

add_test(
    NAME recoil_tools_unittest
    COMMAND ${Python3_EXECUTABLE}
        -m unittest discover
        -s tests/tools
        -p *_tests.py
    WORKING_DIRECTORY ${PROJECT_SOURCE_DIR}
)
